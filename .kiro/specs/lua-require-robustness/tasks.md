# Implementation Plan

実装順序は要件どおり B（可視化）→ A（モジュール解決）とする。B が先に入ることで、A の実装中に起きるロード失敗が 500 とログで可視化された状態で作業できる。

- [x] 1. テスト基盤の整備とベースライン固定
  - 両クレートのテスト共通ヘルパに、どの単一 ANSI コードページでも表現できない複数文字体系混在のディレクトリ名定数、絶対パス長が指定長を超える深いディレクトリを掘るヘルパ、フィクスチャを任意の宛先へコピーするヘルパを追加する
  - 深いディレクトリは設置ルート自体が 300 文字超になるよう構築し、`canonicalize` を使わず絶対化のみで形を保つ（CI の 8.3 短縮名対策）。後始末は一時ディレクトリの drop に任せる
  - `PASTA_DEBUG` を中和するコンストラクタガードは両クレートの共通モジュールに既存であるため新設しない。本仕様で追加・更新する独立テストバイナリが共通モジュールを宣言して既存ガードが効く形にする
  - 着手前のベースラインとして、チャンク識別子の往復検証テストと応答バイト不変テストを実行し、成功を記録する
  - 完了状態: 両クレートのテストから新ヘルパが呼び出せ、深いパス・非 ANSI パスの一時ゴーストがテスト内で構築・破棄できる。ベースラインの 2 テストが緑であることが記録されている
  - _Requirements: 6.4, 6.6, 6.7_
  - _Boundary: TestPathHelpers_

- [x] 2. 欠陥 B: ロード失敗とリクエスト処理エラーの可視化
- [x] 2.1 エラー応答ヘッダ値の単一行化
  - `X-ERROR-REASON` の値を組み立てる際、スタックトレース以降を捨て、残りの行を trim して空行を除き、半角スペース 1 個で連結する非公開ヘルパを追加する
  - 改行を含まない入力は無変換で返し、既存応答をバイト不変に保つ。候補パス列は落とさず、長さの上限も設けない。非 ASCII 文字は変換しない
  - 500 応答と 400 応答の両方がこのヘルパを通るようにする。応答の行構成・ヘッダ名・文字コード指定は変更しない
  - 完了状態: CR / LF / CRLF / タブ字下げ・スタックトレース付き・改行なしの各入力に対する単体テストが緑で、既存の応答不変テストも緑
  - _Requirements: 2.3, 3.5, 4.6, 4.11_

- [x] 2.2 リクエスト処理エラーを 500 応答として返す
  - アクタースレッドの GET 処理でリクエスト処理がエラーを返した場合、応答チャネルを drop するのではなく、エラーから生成した 500 応答文字列を返すよう変更する
  - 観測ログを「応答を返した（エラーあり）」として出し、「応答を返さなかった」ことを示す既存のログ点はそのケースに限定する
  - 応答を送れない異常（スレッド消滅・panic・タイムアウト・mailbox 不在）および NOTIFY の即時 204 は変更しない。マーシャリング側はコード無変更とし、旧契約を書いたコメントのみ新契約へ更新する
  - 完了状態: GET に対してアクタースレッドが生存している限り応答が必ず 1 回値で送られ、既存のマーシャリング・安全網・トレースの各テストが緑のまま
  - _Requirements: 4.3, 4.4, 4.8, 4.10_

- [x] 2.3 本番起動シーケンスの致命分類
  - 起動モジュールを require し、失敗時にモジュール名と致命フラグを構造化フィールドに持つ error ログを出し、起動モジュール名の文脈を付けたエラーを返す共通ヘルパを追加する
  - 利用者初期化スクリプト・SHIORI 応答モジュール・シーン辞書モジュールの 3 種すべてをこのヘルパ経由にし、失敗を致命として伝搬する（利用者初期化スクリプトの致命化は意図的な挙動変更）
  - ログのエラーフィールドには複数行・スタックトレース込みの全文を欠落なく出す。成功時の既存 debug ログは残す。起動シーケンスのコメントを新分類へ更新する
  - 本番経路の既存ローダテストを棚卸しし、利用者初期化スクリプトまたは応答モジュールのロード失敗が警告止まりであることを前提にしていたものがあれば期待値を更新する
  - 完了状態: 利用者初期化スクリプトが構文エラー／上書きした応答モジュールが実行時エラー／応答モジュールが存在しないモジュールを require、の各ケースでロードがエラーを返し、その表示文字列に起動モジュール名と根本原因の両方が含まれることを検証する統合テストが緑
  - _Requirements: 4.1, 4.5, 4.7, 4.9, 5.1, 5.2, 5.3, 5.5_

- [x] 2.4 旧経路の無言化の是正
  - SHIORI 応答モジュールを直接読み込む旧経路について、ファイルが存在するのに読み取り・実行に失敗した場合を致命として伝搬するよう変更する
  - ファイルが存在しない場合は従来どおりスキップする（旧経路は応答モジュールを任意とする軽量構築経路であるため）
  - 失敗ログは本番経路と同一の構造化フィールド構成・同一の文脈文言を用い、ファイル読み取り失敗も同じ経路で伝搬させる
  - 完了状態: 旧経路の既存テストを、応答モジュールの実行失敗でエラーを返し不在では成功する期待へ更新し、緑になる
  - _Requirements: 5.3, 5.5, 5.6_

- [x] 2.5 UTF-8 の設置パスを受け取る初期化入口の追加
  - DLL 共通仕様の UTF-8 版初期化入口を追加する。所有権規約（受け取ったハンドルを全経路で解放）と panic 封じ込めは従来の入口と同一とし、差はパスのデコード方式のみとする
  - 「UTF-8 入口で初期化済み」をプロセス全域のフラグで保持し、アクター起動へ到達した時点で立てる（ロード失敗時も立てる）。終了処理で下ろす
  - フラグが立っているとき従来の初期化入口は、受け取ったハンドルを解放したうえで何もせず成功を返す。フラグが無ければ従来どおり ANSI デコードでロードする
  - 既存のリクエスト処理・終了処理のロジックは変更しない。冒頭コメントの旧契約の記述を新契約へ更新する
  - 完了状態: 32bit / 64bit の両ビルドで新旧 2 つの入口がエクスポートされ、UTF-8 入口で初期化済みのとき従来の入口が再ロードせず成功を返す
  - _Requirements: 2.4, 2.5, 2.6_

- [x] 2.6 (P) ロード失敗可視化の本番経路検証
  - アクター起動とリクエストのマーシャリングを通した経路で、(a) 応答モジュールのロードに失敗するゴースト、(b) 利用者初期化スクリプトのロードに失敗するゴースト、(c) ロードは成功するが応答処理が複数行メッセージでエラーになるゴースト、の 3 ケースを検証する
  - 各ケースで初期化が失敗を返し、GET に 500 と単一行のエラー理由ヘッダが返り、理由にモジュール名と根本原因が含まれ、応答全体が正しいヘッダ構造で終端することを確認する。NOTIFY は 204 のままであることも確認する
  - 独立バイナリとして共通モジュールを宣言し、既存の `PASTA_DEBUG` 中和ガードとゴースト構築ヘルパを取り込む
  - プロセス全域の静的状態を使うため、ファイル内のケースは 1 本のテスト関数または共有ロックで直列化する
  - 完了状態: 上記 3 ケースを網羅する常時実行の E2E テストが緑
  - _Requirements: 4.2, 4.3, 4.4, 4.5, 4.6, 4.8, 5.5, 6.3, 6.4, 6.6_
  - _Boundary: LoadFailureVisibilityTests_
  - _Depends: 1, 2.1, 2.2, 2.3_

- [x] 2.7 (P) UTF-8 初期化入口の検証
  - 非 ANSI の設置パスを UTF-8 で渡す初期化 → GET が正常応答 → 終了処理、の一連を FFI 越しに検証する
  - 続けて UTF-8 入口での初期化後に従来の入口を呼び、成功が返りロード済み状態が壊れない（直後の GET が正常応答）ことを確認する。終了処理後の従来の入口単独では従来どおりロードすることも確認する
  - 独立バイナリとして共通モジュールを宣言し、既存の `PASTA_DEBUG` 中和ガードと非 ANSI 名の定数を取り込む。プロセス全域の静的状態を使うため 1 本のテスト関数で直列化する
  - 完了状態: 上記を網羅する常時実行のテストが緑
  - _Requirements: 2.4, 2.5, 2.6, 6.4, 6.9_
  - _Boundary: FfiLoaduTests_
  - _Depends: 1, 2.5, 3.4_
  - _Note: 実装フェーズで判明 — 非 ANSI 設置パスでの `loadu` 成功は欠陥 A の解消（3.4）が前提。テストは完成済みで、`generate_package_path_bytes()` の ANSI 変換が残る間は `Can't convert some characters to multibyte charset` で `loadu` が false を返す。アサーションは弱めず、3.4 直後に再実行して緑化を確認する（タスク 3.1 と同じく、失敗する状態を単独でコミットしない）。_

- [x] 3. 欠陥 A: 設置パスに依存しないモジュール解決
- [x] 3.1 長パス・非 ANSI 設置パスの統合テストを先行追加
  - 300 文字超の ASCII パス／非 ANSI パス／両方を兼ねるパスの 3 種の設置先それぞれで、ロードが成功し、内蔵スクリプト・利用者スクリプト・トランスパイル済みシーンモジュールの 3 層が解決されることを検証するテストを追加する
  - シーンには非 ASCII のファイル名を 1 本含め、モジュール名に非 ASCII を含む場合も解決されることを確認する
  - 同じ設置先で、解決済みモジュールのチャンク識別子が拡張長プレフィックスを含まず検索パス由来の UTF-8 文字列であること、未検出エラーの文言が非 ASCII のディレクトリ名をそのまま含むことを確認する
  - この時点では欠陥 A により失敗する。失敗する状態を単独でコミットせず、3.2〜3.4 とあわせて 1 つの変更単位として積むこと（設計の「各ステップ後に全テストが緑」を CI 上で維持するため）
  - 完了状態: 上記 3 種の設置先を網羅するテストが書かれ、実装前の失敗内容が欠陥 A に一致することが確認されている
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 1.5, 2.1, 2.2, 2.3, 3.7, 3.8, 6.1, 6.2, 6.6, 6.7_
  - _Depends: 1_

- [x] 3.2 Rust 実装の Lua ファイル searcher を新設
  - 検索パス設定文字列とモジュール名から候補パスを順に生成する純粋関数を実装する。区切りでの分割・空要素のスキップ・プレースホルダの全置換・モジュール名の区切り文字置換を標準と同じ規則で行い、パス長の判定は一切持たない
  - 候補を順に Rust 標準ライブラリで開き、最初に開けたものを採用する。候補文字列をそのままファイルを開くパス・チャンク識別子・エラーメッセージに用いる。拡張長プレフィックスの付与・短縮名変換・正規化は行わない
  - 未検出時は標準と同一書式の候補一覧文字列を返し、読み込み失敗・コンパイル失敗は Lua 文字列エラーとして送出する。数行の Lua ラッパを searcher 本体として設置し、ラッパ側でエラーを送出することで呼び出し側が受け取る値の型を標準と一致させる
  - 設置時に searcher テーブルが想定レイアウト（4 要素）であることを検証し、違反ならエラーを返す。標準の Lua ファイル searcher の位置のみを置換し、他の要素は触らない
  - 完了状態: 候補パス生成の単体テストが緑で、設置関数がレイアウト違反の VM に対してエラーを返す
  - _Requirements: 1.1, 1.3, 1.5, 1.6, 2.1, 2.3, 3.3, 4.9, 7.1_
  - _Boundary: ModuleSearcher_

- [x] 3.3 標準 searcher とのバイト同一性の契約テスト
  - 同一の検索パス設定・同一のファイル群に対し、標準 searcher の VM と新 searcher の VM を並べ、チャンクのソース名・短縮ソース名・チャンクへ渡される引数がバイト単位で一致することを 2 つのファイル名パターンの両方について検証する
  - 未検出・構文エラー・実行時エラーの 3 種について、保護付き呼び出しが受け取る値の型と文言がバイト単位で一致することを検証する
  - 同名モジュールが複数の検索パスにあるとき優先順位の高い側が勝つこと、先に登録済みのモジュールが searcher を経由せず解決されることを検証する
  - 完了状態: これらの比較テストが緑で、チャンク識別子の不変性に対する恒久ゲートとして常時実行される
  - _Requirements: 1.5, 3.1, 3.2, 3.3, 3.4, 4.9, 6.6_
  - _Boundary: ModuleSearcherTests_

- [x] 3.4 検索パス設定の UTF-8 化と searcher の設置
  - 検索パス設定を ANSI バイト列へ変換せず、生成された UTF-8 文字列を無変換で設定するよう変更する。設定は従来どおり上書きとし、C モジュール用の設定は変更しない
  - 同じ初期化処理で新 searcher を設置する。呼び出し元 2 箇所は変更しない。ANSI 変換に起因する失敗経路を持たないようにする
  - 検索パスの ANSI バイト列を生成していたメソッドと、それだけを検証していたテストを撤去する。UTF-8 版の生成メソッドとそのテストは変更しない
  - ANSI 前提で書かれていた既存の検索パステストを UTF-8 契約へ更新し、ドキュメントコメントの ANSI 記述も更新する
  - 完了状態: 3.1 で追加した長パス・非 ANSI の統合テストがすべて緑になる（欠陥 A の解消）
  - _Requirements: 1.4, 2.1, 2.2, 3.1, 3.2, 3.8_
  - _Depends: 3.1, 3.2_

- [x] 3.5 チャンク識別子とソースマップの無回帰検証
  - 既存のチャンク識別子往復検証テストで、VM に新 searcher を設置したうえでトランスパイル → require → 行フック → ソースマップ照合の往復が通ることを確認する
  - 非 ANSI のディレクトリ名配下の設置ケースを追加し、行フックのソース名がソースマップのキーへ解決されることを確認する
  - 当該テストは共通モジュールを宣言していない独立バイナリであるため、宣言を追加して既存の `PASTA_DEBUG` 中和ガードと非 ANSI 名の定数を取り込む
  - 完了状態: ブレークポイント解決とコールスタック表示の照合が ASCII パス・非 ASCII パスの両方で緑
  - _Requirements: 3.6, 3.8, 6.4_
  - _Boundary: ChunkNameValidationTests_
  - _Depends: 1, 3.4_

- [x] 3.6 (P) 長パス・非 ANSI 設置での応答バイト一致の E2E 検証
  - 長パスかつ非 ANSI の設置先と通常の設置先の 2 つで、同一のリクエスト列に対する応答がバイト単位で一致することを本番のリクエスト経路で検証する
  - 独立バイナリとして共通モジュールを宣言し、既存の `PASTA_DEBUG` 中和ガードとゴースト構築ヘルパを取り込む
  - 完了状態: 上記を検証する常時実行の E2E テストが緑で、既存の応答バイト不変ゲートも緑のまま
  - _Requirements: 1.2, 2.2, 3.5, 6.1, 6.2, 6.4, 6.6_
  - _Boundary: PathRobustnessE2eTests_
  - _Depends: 1, 3.4_

- [x] 4. CI とマニュアル
- [x] 4.1 CI のテストをターゲット指定で実行する
  - ビルドワークフローのテスト実行に matrix のターゲット指定を付け、32bit ジョブが 32bit バイナリとしてテストを実行するようにする。64bit ジョブは従来と同じ内容を明示ターゲットで実行する
  - 本仕様と無関係な 32bit 固有の失敗が多数表面化した場合は、その時点で開発者へ報告して縮退の判断を仰ぐ
  - 完了状態: 両ジョブがそれぞれのターゲットでワークスペース全体のテストを実行し、緑になる
  - _Requirements: 6.6, 7.1, 7.3_
  - _Boundary: CiWorkflow_

- [x] 4.2 (P) マニュアルへの起動シーケンス・制限・切り分け手順の追記
  - リファレンス節に新規 1 ページを追加し、(1) モジュール検索パスの優先順位とファイル名パターン・設置パスの長さや文字種に依存しないこと・検索パス設定を UTF-8 として解釈すること、(2) 本番起動シーケンスの順序と各モジュールのロード失敗時の扱い（致命／継続）、を記載する
  - 同ページに既知の制限として、UTF-8 の初期化入口を提供しないホストでは ANSI コードページ外の文字を含む設置パスを扱えないこと、ゴースト作者コードが直接呼ぶ Lua 標準のファイル入出力が長パス・ANSI 外のパスを扱えないこと（永続化は専用モジュールを使うこと）を明示する
  - 同ページに、ゴーストが起動しない・喋らないときの切り分け手順（ホストの通信ログでの 500 応答のエラー理由ヘッダの確認、ログファイルの致命行と複数行メッセージの確認、典型原因の読み方）を記載する
  - 利用者初期化スクリプトの致命化という挙動変更、および検索パス設定を UTF-8 として解釈する挙動変更も明記する。目次へページを追加し、既存のトラブルシューティングページ冒頭に本ページへの誘導を 1 段落加える
  - 完了状態: mdBook のビルドが通り、新ページが目次から辿れ、上記 4 節がすべて記載されている
  - _Requirements: 5.4, 6.5, 6.8, 6.10_
  - _Boundary: BookStartupPage_

- [x] 4.3 (P) ランタイム README の検索パス節の更新
  - 検索パスの説明を UTF-8 契約（ANSI 変換をしないこと）と起動モジュールのロード失敗の扱いに合わせて更新する
  - 完了状態: README の当該節が実装後の契約と一致し、ANSI 前提の記述が残っていない
  - _Requirements: 5.4_
  - _Boundary: pasta_lua README_

- [x] 5. 最終検証
- [x] 5.1 32bit / 64bit 両ターゲットでの全テスト緑と既存ゲートの確認
  - ワークスペース全体のテストを 32bit / 64bit の両ターゲットで実行し、すべて緑であることを確認する
  - 既存の応答バイト不変ゲート・未使用イベントのバイト不変ゲート・FFI セッションの E2E、およびアクターの安全網に関する既存テスト群が無変更で緑であることを確認する
  - 完了状態: 両ターゲットで失敗 0 件、上記の既存ゲートがすべて緑
  - _Requirements: 3.5, 4.10, 7.1, 7.2_
  - _Depends: 2.6, 2.7, 3.3, 3.5, 3.6, 4.1_

- [x]* 5.2 実機ホストでの確認とマニュアルへの反映
  - 実機のホストで、(a) ANSI コードページ外の文字を含む 260 文字超のフォルダへ設置したゴーストが起動して喋ること、(b) 利用者初期化スクリプトを壊したゴーストで初期化失敗後にホストがリクエストを送るか・500 のエラー理由ヘッダがホストのログでどう見えるか、を確認する
  - 自動テストで代替できないホスト側挙動の確認であり、合否ゲートではなく記録を目的とする。(b) の結果をマニュアルの切り分け手順へ反映する
  - 完了状態: 確認結果が記録され、(b) の結果がマニュアルの切り分け手順に反映されている
  - _Requirements: 6.8_
  - _Boundary: BookStartupPage_
  - _Depends: 4.2, 5.1_

## Implementation Notes

- cargo の build/test は同一コマンド内で `NoDefaultCurrentDirectoryInExePath` を外すこと。外さないと mlua-sys(LuaJIT) のビルドが exit 101 で死ぬ（PowerShell: `Remove-Item Env:\NoDefaultCurrentDirectoryInExePath -ErrorAction SilentlyContinue;` / Bash: `unset NoDefaultCurrentDirectoryInExePath;`）。
- `cargo fmt` はクレート単位でも無関係ファイル約 105 件を書き換える（本リポジトリは rustfmt clean ではない）。実行しないこと。
- テスト実行で `crates/pasta_lua/tests/fixtures/sample.generated.lua` が改行差分のみで dirty になることがある。内容は同一なのでコミットしない。
- タスク 2.7 は欠陥 A の解消（3.4）に依存することが実装フェーズで判明した。完成済みのテスト本体はスクラッチパッドへ退避し、3.4 完了直後に `crates/pasta_shiori/tests/ffi_loadu_test.rs` へ戻して緑化を確認する。`loadu` は `crates/pasta_shiori/src/lib.rs` から再公開されていないため、テストは `unsafe extern "C" { fn loadu(...) -> bool; }` で出荷シンボルを直接宣言している。
- タスク 3.6 の実測で、長パス設置先ではテストゴーストの `SHIORI.unload` 内 `io.open` が narrow API の制約で失敗することを確認した（`if f then` で握られており応答・teardown には影響しない）。タスク 4.2 の「既知の制限」節で `io.open` の長パス非対応を書く際、机上ではなく実測として引用できる。
- タスク 4.1 の実測: `cargo test --all` を i686 / x86_64 の両ターゲットでローカル実行し、いずれも 95 テストバイナリ・2180 passed・0 failed・exit 0 で件数まで完全一致した（要件 7.1 の裏付け）。CI ランナー固有の差（ロケール実効値・8.3 短縮名の %TEMP%）のみ未検証のため、初回 CI 実行の x86 ジョブ結果は要確認。
- タスク 5.1 の最終検証結果（全タスク完了後に実施）: `cargo test --all --target x86_64-pc-windows-msvc` と `--target i686-pc-windows-msvc` がいずれも exit 0・95 テストバイナリ・2180 passed・0 failed で、件数まで完全一致（要件 7.1 / 7.2）。名指しの既存ゲートも両ターゲットで緑: byte_invariant 4 / kick_unused_byte_invariant 2 / ffi_extern_session_e2e 1 / actor_marshaling 8 / actor_test_harness 6 / actor_tracing_seams 5 / actor_teardown 3 / actor_kick 4。このうち本仕様で変更したのは actor_marshaling_test.rs（タスク 2.2 でケース追加）のみで、他はすべて無変更のまま緑（要件 3.5 / 4.10）。
- 仕様レベル検証（kiro-validate-impl）の結果: 要件 50 項目中 46 COVERED / 4 PARTIAL / 0 UNCOVERED、境界監査 CLEAN、critical findings なし。PARTIAL のうち 4.7 / 5.3（起動失敗ログの `module` / `fatal=true` / 複数行全文）は検証深度の不足だったため、`startup_fatal_test::startup_failure_log_carries_module_fatal_and_multiline_cause` を追加して解消した（マニュアルの切り分け手順が `fatal=true` の grep を指示しているため、フィールド名の改名を検出できる形で固定する必要がある）。残る PARTIAL は 4.9 の I/O 原因（searcher 経路は Windows では実質到達不能）と 4.10 の panic / リクエストデコード失敗の 204（いずれも本仕様が触っていない無変更コード）で、回帰ではなく既存のカバレッジ不足である。
- `tracing-test` の `logs_contain` はキャプチャをスパン名でフィルタするため複数行フィールドの 2 行目以降が見えない。要件 4.7 の「欠落なく」を検証するにはスコープ付きの fmt subscriber で生の出力を取る必要がある。
- タスク 5.2 の実測（SSP 2.8.98.3000 / Windows 11・NTFS システムボリューム・8.3 名生成有効）。
  - (a) は**条件を分解して検証した**。「260 文字超 かつ 非 ANSI」を 1 つのフォルダで満たす設置先は、ホスト側のゴースト登録時点でパスが 8.3 短縮名（285 文字 → 104 文字）へ置き換わり、ランタイムには短く ANSI 表現可能なパスしか届かなかった。短縮はホストの SHIORI 呼び出し時ではなく登録時に起きている（SSP の `history.dat` に短縮形のまま保存されていた）。そのため次の 2 経路に分けた。
    - 長パス: 8.3 名として既に正当なディレクトリ名（8 文字・大文字 ASCII・ドット無し）を 18 段重ね、短縮名エイリアスが生成されない設置先を作る。`load_dir` は 210 文字で無変換のまま到達し、`require` の解決先が 260 文字（`pasta.shiori.entry` 自身）および 265 文字（`pasta.shiori.event.kick`）になるゴーストが `fatal` なしで起動した。`MAX_PATH` は終端を含む 260 なので実用上限は 259 文字であり、いずれも旧実装の narrow `fopen` では開けない長さである。
    - 非 ANSI: CP932 に存在しない文字（ハングル・絵文字）を含む 77 文字の設置先。`load_dir` は無変換で到達し、`package.path` も非 ANSI を保ったまま設定され、`fatal` なしで起動した。
    - 「両方同時」の literal な条件は、8.3 名生成を無効化したボリューム等でなければこのホストでは再現できない。当該環境（非管理者・単一 NTFS システムボリューム）では検証不能として記録する。
  - (b) の結果: `scripts/main.lua` に構文エラーを置くと（`scripts` は `profile/pasta/pasta_scripts` より先に探索されるため既定の `main.lua` を覆い隠す）、`module="main" fatal=true` の error 行に原因全文とスタックトレースが残り、`load` は false を返した。**SSP はその後リクエストを一切送らず**、ホスト側にはファイルとして何も残らなかった（`STACKTRC.TXT` も未更新）。したがって 500 応答の `X-ERROR-REASON` は SSP 環境では発動しない。マニュアル第 4 節の切り分け手順は、ログファイルを手順 1、500 応答をホスト依存の手順 2 へ入れ替えた。
  - 切り分けの過程で、成功経路のログが `load` / `loadu` のどちらで初期化されたかを記録していないことが判明したため、`load_impl` に `entry` と `loaded` を出す 1 行を追加した（コミット `01a4c4a5` / `54b73ce8`）。ログのファイル振り分けは load_dir のスレッドローカル文脈で決まるため、FFI 入口スレッドでは `LoadDirGuard` を張り直さないとログファイルに届かない。`ffi_loadu_test` で両入口について固定済み。
