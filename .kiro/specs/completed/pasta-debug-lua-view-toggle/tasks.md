# Implementation Plan

- [ ] 1. 基盤: 再描画手段の実証（spike ゲート）
- [x] 1.1 停止中 `stopped` 再送による即時再描画の実証
  - 実 DAP-over-TCP の最小シナリオで、停止中に `stopped` イベントを再送した際、クライアントが stackTrace を再フェッチし現在フレームの提示が更新されることを確認する
  - 確実な再描画手段（`stopped` 再送）を確定し、不成立なら代替（`invalidated` ＋ クライアント capability ゲート）への切替判断を下す
  - 実証結果と確定した手段を research.md に追記する（以降のタスク着手判断のエビデンス）
  - _Requirements: 3.3_

- [ ] 2. バックエンド制御経路（コア）
- [x] 2.1 (P) 設定カスタムリクエストのデコード・受理応答・イベント構築
  - `pasta/sourcePresentation` リクエストを認識し、`mode`（`pasta`/`lua`）を解釈して制御経路へ載せる
  - 受理確認の応答（現在モードのエコー）を返し、認識できないモード値は無変更で現在モードを返す
  - 現在モードを通知するカスタムイベント Value を構築するヘルパを用意する
  - 本タスクの所有範囲は「デコード・応答・イベント構築」までとし、状態更新やセッション転送（handle_inbound 結線）はタスク 3.1 が担う
  - 単体テストで set／不正値の受理応答とイベント Value 生成が緑になる（観測可能）
  - _Requirements: 1.1, 1.2, 1.3, 1.4_
  - _Boundary: DAP custom request handler（decode/response/event 構築）_

- [x] 2.2 (P) 停止中再描画コマンドの処理
  - 提示モード再描画コマンドを停止ループの inspect 系操作として処理し、停止中は現在の停止（理由・スレッド）を再送して待機を継続、実行中は無視する
  - 専用スナップショット状態は新設せず、停止ループが保持する停止理由・スレッドを再利用する
  - 単体テストで「停止中＝停止再送」「実行中＝無視」が緑になる（観測可能）
  - _Requirements: 3.3, 5.3_
  - _Boundary: RefreshPresentation handler_
  - _Depends: 1.1_

- [ ] 3. バックエンド統合
- [x] 3.1 提示モード切替の適用・即時反映・push 通知の結線
  - 設定リクエスト受理時（handle_inbound）に提示モード状態を更新し、レゾルバ差し替えを再実行して以後の stackTrace/source を新モード座標で提示する
  - 受理応答送出後に再描画コマンドをセッションへ転送し、停止中の即時再描画を起動する
  - attach 処理完了時（解決済み初期モード）とモード変更時に、現在モードのカスタムイベントを送出する
  - 実行中（非停止）の切替は次の停止で新モードが提示されることを確認する
  - 結合テストで「切替→stackTrace が新座標」「attach／切替でイベント送出」が緑になる（観測可能）
  - _Requirements: 1.1, 1.2, 1.5, 2.5, 2.6, 3.1, 3.2, 3.4, 3.5, 4.2, 4.3_
  - _Boundary: 統合（handle_inbound 結線・状態更新・レゾルバ差し替え・イベント送出）_
  - _Depends: 2.1, 2.2_

- [ ] 4. VSCode フロントエンド
- [x] 4.1 (P) 提示トグルの純ロジックと単体テスト
  - 現在モードの反転、設定リクエストのペイロード生成、応答／イベント body からのモード抽出、ステータスバー表示文字列の生成を vscode 非依存の純ロジックとして実装する
  - 単体テストで反転・ペイロード・body 解釈（不正値含む）・表示文字列が緑になる（観測可能・直接実行で検証）
  - 本タスクはマニフェスト（package.json）を変更しない。テスト実行スクリプトへの登録はタスク 4.2 が担う
  - _Requirements: 1.4, 2.3, 2.5, 2.6_
  - _Boundary: sourcePresentationToggle_

- [x] 4.2 (P) 拡張マニフェストのコマンド・メニュー・テスト登録
  - 提示トグルコマンドをコマンドパレットに登録し、デバッグツールバーにトグルボタンを追加する
  - 非 pasta セッション／非アクティブ時はトグル UI を提示しない条件を付与する
  - タスク 4.1 の単体テスト（設計既知のパス）をテスト実行スクリプトへ登録する
  - 拡張をパッケージし、pasta デバッグ中のみツールバーボタンとパレットコマンドが現れることを確認する（観測可能）
  - _Requirements: 2.1, 2.2, 2.4_
  - _Boundary: VSCode manifest（package.json）_

- [x] 4.3 拡張の結線（トグル発行・常時表示・クリック導線）
  - トグルコマンドがアクティブな pasta セッションへ設定リクエストを送出し、非該当時は無操作で利用者へ周知する
  - 現在モードのカスタムイベントを購読してステータスバーへ常時表示し、切替で更新する
  - ステータスバー項目をクリックでもトグル可能にし、セッション中のみ表示する
  - 既存の attach 接続・診断・ハイライト登録を損なわないことを確認する
  - 実セッションでツールバー／パレット／クリックの3導線が同じ切替を起こし、表示が追従することを確認する（観測可能）
  - _Requirements: 2.1, 2.2, 2.3, 2.4, 2.5, 2.6, 6.4_
  - _Boundary: extension wiring_
  - _Depends: 3.1, 4.1, 4.2_

- [ ] 5. 検証
- [x] 5.1 実 DAP-over-TCP の往復 E2E
  - 実行時トグル E2E の新規テストファイルを作成し、テストランナーへ登録する
  - `.pasta` 行 BP で停止した状態から `.lua` 提示へ切替え、停止位置・スタックトレースが `.lua` 座標で提示され即時再描画されることを検証する
  - `.lua`→`.pasta` へ戻し提示が `.pasta` 座標へ復帰することを検証する
  - 切替の前後で `.pasta` 行 BP が有効であり続けることを検証する
  - E2E テストが緑になる（観測可能）
  - _Requirements: 7.1, 7.2, 7.3, 3.3, 6.3_
  - _Depends: 3.1_

- [x] 5.2 初期モード解決と実行時上書きの検証
  - attach 引数指定時はその初期モードが適用され、未指定時は環境変数＞設定ファイル＞既定の解決結果が初期モードになることを検証する（既存解決の検証であり再実装はしない）
  - 実行時トグルが初期モードを上書きし、以後その上書き後モードが採用されることを検証する
  - attach 後の push イベントが解決済み初期モードを報告することを検証する
  - テストが緑になる（観測可能）
  - _Requirements: 4.1, 4.2, 4.3, 4.4_
  - _Depends: 5.1_

- [x] 5.3 提示モードとステップ粒度整合の検証
  - `.pasta` モードで `.pasta` 粒度、`.lua` モードで `.lua` 粒度のステップになることを検証する
  - 停止中に切替えた後の次ステップ以降が切替後粒度で進むこと、コルーチンを跨いでも継続することを検証する
  - テストが緑になる（観測可能）
  - _Requirements: 5.1, 5.2, 5.3, 5.4_
  - _Depends: 5.1_

- [x] 5.4 無回帰の検証
  - トグル未使用セッションが初期解決どおりに動作することを検証する
  - デバッグ無効（OFF）経路でトグル関連処理が一切走らず、バイト不変・ゼロコストが維持されることを検証する
  - 既存の attach 接続・診断・ハイライト等の拡張機能が損なわれないことを確認する
  - テストが緑になる（観測可能）
  - _Requirements: 6.1, 6.2, 6.3, 6.4_
  - _Depends: 5.1, 4.3_

- [ ] 6. ドキュメント
- [x] 6.1 (P) マニュアル デバッグ章の更新
  - 「提示モードの切替」節を、デバッグ中に VSCode のコマンド／ツールバーボタン／ステータスバークリックで提示モードを切り替える手順へ更新する
  - 初期モード指定（attach 引数／環境変数／設定ファイル）と実行時トグルによる上書きの関係を説明する
  - マニュアルをビルドして当該節が新手順を反映していることを確認する（観測可能）
  - _Requirements: 8.1, 8.2_
  - _Boundary: book manual_

## Implementation Notes

- 4.3: extension.ts のトグルコマンドの `customRequest` 呼び出しを try/catch で包み、送出失敗時に `showErrorMessage` で通知するよう追加（design Error Handling 準拠）。表示はイベント駆動のため追跡モード・ステータスバーは失敗時も不変。validate-impl で解消済み。
