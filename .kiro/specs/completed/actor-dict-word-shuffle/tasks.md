# 実装タスク: actor-dict-word-shuffle

## タスクリスト

- [x] 1. `entry()` の不正二重登録を除去する
  - `ACTOR_WORD_BUILDER_IMPL.entry()` 内の `actor[key]` テーブルへの書き込みブロック全体（`if not self._actor[self._key]` で始まるブロック）を削除する
  - `self._word_builder:entry(...)` 呼び出し（Rust `WordTable` への登録）のみを残す
  - 空値チェック（`if #values > 0`）およびメソッドチェーン（`return self`）は維持する
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 2.1_

- [x] 2. アクタースコープ単語のシャッフル動作を E2E テストで検証する
- [x] 2.1 (P) シャッフル＆順次消費をプロキシ経由で検証するテストを追加する
  - アクター辞書に 3 値の単語を定義したフィクスチャを用意する（例: `＠通常：\s[0]、\s[100]、\s[200]`）
  - `set_word_selector()` でモックセレクタを注入し、決定論的な順序制御を行う
  - `PROXY_IMPL.word()` を経由した呼び出し（`ACT:word('通常')` 相当）で 3 値すべてが順次消費されることを確認する
  - これにより `actor[key]` への不正書き込み再導入もリグレッションとして検知可能になる
  - 本タスクは Task 1 の完了後に実施する
  - _Requirements: 1.1, 1.2, 1.3, 2.1, 3.1_
- [x] 2.2 (P) アクタースコープからグローバルへのフォールバックを検証するテストを追加する
  - アクター辞書に存在しない単語を参照したときにグローバル単語に正しくフォールバックすることを確認する
  - 既存の `test_actor_word_scope_resolution` の NOTE アノテーションを削除し、完全なテストに昇格させる
  - 本タスクは Task 1 の完了後に実施する
  - _Requirements: 3.2_
- [x] 2.3 (P) 単一値アクター単語の後方互換性を確認するテストを追加する
  - アクター辞書に単一値定義された単語（例: `＠照れ：\s[1]`）が常に同じ値を返すことを確認する
  - 本タスクは Task 1 の完了後に実施する
  - _Requirements: 1.4, 3.3_

- [x] 3. (P) アクター辞書仕様ドキュメントにシャッフル契約を明記する
  - `11-actor-dictionary.md` の 11.4 節（アクタースコープと単語参照の統合）に「アクター単語はシャッフルされる」を仕様上の契約として追記する
  - 複数値定義時のシャッフル＆順次消費が 4.1.4 スコープ解決アルゴリズムと同一であることへの相互参照リンクを追加する
  - 全候補消費後に再シャッフルが行われること、および単一値の場合は常に同一値が返ることを記述する
  - 本タスクはコード変更（Task 1, 2）と独立して実施可能
  - _Requirements: 4.1, 4.2_
