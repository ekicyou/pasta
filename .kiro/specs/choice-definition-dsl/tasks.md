# Implementation Plan

- [ ] 1. 選択肢DSL構文の定義
- [x] 1.1 選択肢マーカーの文法規則を追加する
  - `＠？`/`@?` + target ID の組み合わせをパーサが認識できる文法規則を追加する
  - 省略形（`＠？target`）と括弧形（`＠？target「表示テキスト」`）の両形式を受け付ける
  - グローバルシーンおよびローカルシーン内の行として配置できる
  - target 欠落時にパースエラーが報告される（1.5 対応）
  - 文法規則が追加され `local_scene_item` に組み込まれた状態が完了の目安
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 1.5_
  - _Boundary: pasta_dsl/grammar_

- [ ] 1.2 選択肢ノードのAST構造体とパーサを追加する
  - 選択肢1行を表すデータ構造（target 文字列、任意の表示テキスト）を定義する
  - `LocalSceneItem` 列挙型に選択肢バリアントを追加する
  - `cue_cmd_line` パース実装を参考に選択肢行のパース関数を実装する
  - グローバル・ローカル両シーンループから選択肢行のパースを呼び出せる
  - パーステスト（省略形・括弧形・半角形式・グローバル/ローカル配置・target欠落エラー）が全通過した状態が完了の目安
  - _Requirements: 1.1, 1.2, 1.3, 1.4, 1.5_
  - _Boundary: pasta_dsl/ast, pasta_dsl/parser_
  - _Depends: 1.1_

- [ ] 2. トランスパイラでの選択肢コード生成 (P)
- [ ] 2.1 選択肢行を Lua 呼び出しへ変換する
  - 選択肢ノードのトランスパイル処理を追加する
  - 省略形は target を display として使い、括弧形は括弧内テキストを display として使う Lua 呼び出しを生成する
  - 複数選択肢行は各行に対して個別の Lua 呼び出しを生成する
  - target/display の Lua 文字列リテラル安全エスケープを実施する
  - コード生成テストで省略形・括弧形・複数行の出力が期待 Lua コードと一致した状態が完了の目安
  - _Requirements: 2.1, 2.2, 2.3_
  - _Boundary: pasta_lua/code_gen_
  - _Depends: 1.2_

- [ ] 2.2 タイムアウト指定コマンドを選択肢タイムアウト呼び出しへ変換する
  - `!select(秒数)` cue コマンドを選択肢タイムアウト設定の Lua 呼び出しに変換する
  - 引数なしのタイムアウト指定は「タイムアウトなし」を意味する Lua 呼び出しに変換する
  - `select` 以外の cue コマンドは従来どおり無視する
  - コード生成テストで秒数あり・秒数なしの出力が期待 Lua コードと一致した状態が完了の目安
  - _Requirements: 4.1, 4.2, 4.3_
  - _Boundary: pasta_lua/code_gen_

- [ ] 3. コアランタイムの選択肢 API 追加 (P)
- [ ] 3.1 グローバルシーン記録フィールドをランタイム状態に追加する
  - 選択肢コールバックルーティング用に「最後に実行したグローバルシーン名」の保存領域をランタイムの状態管理に追加する
  - 初期値は nil（未記録状態）
  - Lua ユニットテストで nil 初期化が確認できた状態が完了の目安
  - _Requirements: 3.4_
  - _Boundary: pasta_lua/core_runtime_

- [ ] 3.2 選択肢トークン蓄積メソッドを追加する
  - 選択肢1件の情報（target・display）を構造化トークンとして蓄積するメソッドを追加する
  - タイムアウト秒数を構造化トークンとして蓄積するメソッドを追加する
  - どちらのトークンもさくらスクリプト文字列を含まず構造化データのみ保持する
  - Lua ユニットテストで各メソッド呼び出し後にトークンリストへ期待する構造体が追加された状態が完了の目安
  - _Requirements: 2.1, 2.2, 4.1, 4.3_
  - _Boundary: pasta_lua/core_runtime_

- [ ] 3.3 シーン実行開始時にグローバルシーン名を記録する
  - シーン初期化処理の先頭で、実行中シーンがグローバルシーンであればその名前をランタイム状態に記録する
  - 記録はシーン実行開始時に行う（プレイバック完了後の nil 化を防ぐため）
  - Lua ユニットテストでグローバルシーン実行後に記録フィールドが期待するシーン名になった状態が完了の目安
  - Lua ユニットテストでローカルシーン実行後も `STORE.last_global_scene` が以前のグローバルシーン名のまま保持されている状態が確認できること
  - _Requirements: 3.4_
  - _Boundary: pasta_lua/core_runtime_

- [ ] 4. SHIORI さくらスクリプト変換と選択結果ルーティング
- [ ] 4.1 (P) choice トークンをさくらスクリプトへ変換する
  - 選択肢トークンを `\![*]\q[display,target]` 形式のさくらスクリプトに変換する処理を追加する
  - タイムアウトトークンを `\![set,choicetimeout,ミリ秒]` 形式に変換する（秒×1000、nil→0）
  - display テキスト内の `\q[...]` デリミタ（`,` `]` `\`）をエスケープするローカルヘルパ関数を `sakura_builder.lua` 内に実装する（Rust ネイティブモジュールの修正不要）
  - Lua ユニットテストで choice/choice_timeout トークンが期待さくらスクリプト文字列に変換される状態が完了の目安
  - Lua ユニットテストで `]` `,` `\` を含む display テキストが正しくエスケープされた状態が完了の目安
  - _Requirements: 2.1, 2.2, 4.1, 4.3_
  - _Boundary: pasta_lua/shiori_runtime_
  - _Depends: 3.2_

- [ ] 4.2 (P) OnChoiceSelectEx 自動ルーティングハンドラを実装する
  - `OnChoiceSelectEx` 受信時に選択IDでシーンを前方一致検索して自動実行するハンドラを新規ファイルで実装する
  - 選択 Reference インデックスはハンドラ内で定数化して後から変更しやすくする
  - 明示的な OnChoiceSelectEx シーンが存在する場合は自動ルーティングより優先する
  - 複数シーンがマッチした場合はシャッフル＆順次消費方式で1つを選択して実行する
  - 非マッチ時は 204 委譲する
  - グローバルシーン名記録を検索スコープに利用し、ローカル→グローバルの順で前方一致検索する
  - Lua ユニットテストでマッチ実行・複数マッチシャッフル・非マッチ204・ローカル→グローバル検索・明示ハンドラ優先の各ケースが通過した状態が完了の目安
  - _Requirements: 3.1, 3.2, 3.3, 3.4, 3.5_
  - _Boundary: pasta_lua/shiori_event_
  - _Depends: 3.1, 3.3_

- [ ] 4.3 OnChoiceSelectEx ハンドラをイベントシステムに登録する
  - 4.2 で実装したルータを既定の OnChoiceSelectEx ハンドラとして登録する
  - 既存のイベント登録パターン（boot.lua 準拠）に従って登録する
  - 既存イベント登録処理との副作用がないことを確認する
  - ルータが実際に OnChoiceSelectEx イベントで発火する統合確認が完了の目安
  - _Requirements: 3.1, 3.5_
  - _Boundary: pasta_lua/shiori_event_
  - _Depends: 4.2_

- [ ] 5. サンプルゴーストへの選択肢デモ追加
- [ ] 5.1 選択肢デモ辞書を追加する
  - hello-pasta ゴーストに省略形・括弧形の選択肢、コールバックシーン、タイムアウト指定を含むデモ辞書を追加する
  - デモをビルド・実行したとき、選択肢が表示されクリック後に対応シーンが実行されるラウンドトリップ動作が確認できる状態が完了の目安
  - _Requirements: 1.1, 1.2, 2.1, 2.2, 3.1_
  - _Depends: 4.3_
