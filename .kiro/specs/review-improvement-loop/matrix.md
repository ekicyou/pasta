# Review Matrix — 実行開始: 2026-06-10T20:55:19+09:00 / ベースライン: 4027097

| Cell | Area | Paths | Groups | Status | Commit | Notes |
|------|------|-------|--------|--------|--------|-------|
| 3.1 | pasta_core | crates/pasta_core/** | G1 | IMPROVED |  | テスト38件追加（67→105・プロダクションコード変更なし）・scene_registry.rs: register_global_raw 完全未テストを4件で網羅（__start__除外/属性伝播/カウンタ非干渉）＋merge_from 3件（ID再採番/カウンタmax統合）＋get_scene 不正ID境界・word_registry.rs: merge_from 2件・scene_table_tests.rs: resolve_scene_id 空キー InvalidScene/SceneNotFound/属性フィルタ一致・NoMatchingScene（unified含む）/find_scene レガシー/replace_selector キャッシュクリア/labels_iter/get_scene 範囲外/同名シーン順次消費 計11件・word_table_test.rs: new空テーブル/clear_cache再開/replace_selector（entries保持）/空キー全グローバル一致/空values WordNotFound ガード（global+local）計6件・random.rs: select_index 境界/with_seed 再現性/Mock 空シーケンス/modulo 折返し/shuffle_usize 不変条件 計7件・error.rs: Display 文言契約 3件（test-only mod追加）・不要テスト削除なし |
| 3.2 | pasta_core | crates/pasta_core/** | G2+G3+G4+G5 | IMPROVED |  | G2: clippy 警告1件（useless_vec・tests/word_table_test.rs:338）を --fix で解消→警告0。依存3件（thiserror/fast_radix_trie/rand）全て使用確認・未使用依存なし。workspace 外部未使用の pub（find_scene/collect_word_candidates/set_shuffle_enabled/into_entries/labels_iter/WordCacheKey）は crates.io 公開クレート（publish=true）の公開API面につき削除せず維持・G3: unwrap/expect は全て #[cfg(test)] 内でプロダクションコードに0件。scene_table.rs の labels[id.0]（filter_by_attributes 内）は from_scene_registry で labels と同時構築される prefix_index 由来 ID のみ・構築後 labels 不変（公開変更APIなし）＝到達不能不変条件と判断し維持。word_table.rs は全 .get() 防御済・scene_registry.rs get_scene は id<1 ガード済・random.rs は len==0/空シーケンスガード済。信頼境界（FFI/外部入力）なし・非有界アロケーションなし → ハードニング挙動変更0件（回帰テスト追加不要）・G4: karpathy 準拠。是正2件: resolve_scene_id/resolve_scene_id_unified の属性フィルタ重複ブロック（16行×2）を private helper filter_by_attributes へ抽出・fn_name_to_search_key の split+collect+join を replace("::",":") に簡素化。collect_scene_candidates/collect_word_candidates の構造類似は別ドメイン（型/エラー型相違）のため統合せず・G5: README.md ドリフト是正（存在しない tests/scene_table_test.rs 記載削除・旧API使用例 SceneTable::new()引数なし/insert/get_exact/get_prefix/SceneEntry.file_path を現行API例へ全面更新・依存表 fast_radix_trie 1.1.0→1.2.0/rand 0.9→0.10/未使用 tracing 行削除）。TEST_COVERAGE.md pasta_core 分同期（67→105・§2.2 に境界回帰38テスト行追加・§3 DoD 行更新）。検証: cargo test -p pasta_core 105 passed/0 failed・workspace 1548 passed/0 failed（ベースライン一致）。申し送り: TEST_COVERAGE.md の他クレート数値（pasta_dsl 153/pasta_lua 522/合計1050+ 等）も陳腐化 — 各領域 G5 セルおよび 3.64 の所掌 |
| 3.3 | pasta_dsl | crates/pasta_dsl/** | G1 | IMPROVED |  | テスト30件追加（206→236・プロダクションコード変更なし）・tests/error_api_test.rs（16件）: ParseError API 完全未テストを網羅 — syntax_error/pest_error/io_error コンストラクタ＋Display 形式契約・From<io::Error>・MultipleErrors 件数表示・ParseErrorInfo PartialEq・Clone。parse_file 完全未テスト→成功（path 設定・AST 構築）/存在しないパス IoError の2経路。parse_str エラー経路 — シーン外生テキスト 1:1 SyntaxError（file/line/column 検証）/2行目 ＄＝ の 2:4 位置精度/裸 ＞・裸 ＆ マーカー拒否。境界正常系 — コメントのみ/空行のみ（span 検証）/CRLF 改行 LF 等価・tests/expr_parse_test.rs（14件）: 二項演算式 AST 完全未テスト（Expr::Binary/Paren を検証する既存テスト0件）を網羅 — add/sub/div/mod・全角数字＋全角演算子正規化・左結合連鎖 1+2+3=(1+2)+3・優先順位なし文法仕様 1+2*3=(1+2)*3 の文書化・括弧単項 (5)*3・VarRef 混在式・float 全半角・key_arg（Arg::Keyword）/位置＋キーワード混在/引数内二項式・不要テスト削除なし・検証: cargo test -p pasta_dsl 236 passed/0 failed・workspace 1578 passed/0 failed（baseline 1548＋30）・既知バグ発見（挙動保存により未修正・開発者報告）: parse_action.rs:348 try_parse_expr の paren_expr 分岐が最初の term のみ包む — (1+2)*3 が Paren(1)*3 になり +2 が無音欠落（parse_expr_from_parts を使うべき箇所）。3.4/3.5 セルへ申し送り |
| 3.4 | pasta_dsl | crates/pasta_dsl/** | G2+G3 | IMPROVED |  | G2: clippy 指摘3件を解消→警告0（approx_constant×2: tests/expr_parse_test.rs の float リテラル 3.14→2.5 へ変更・全角入力 ３．１４→２．５ も整合更新／clone_on_copy×1: tests/ast_test.rs:98 span.clone()→コピー代入）。プロダクション src は clippy 警告0・デッドコードなし。依存3件（pest/pest_derive/thiserror）全て使用確認・未使用依存なし。pub API は crates.io 公開クレート（publish=true）につき保守的に全維持（parse_str_partial は pasta_lsp 使用・infer_rule_from_line 等も公開API面として維持）・G3: src の unwrap/expect 0件（doc コメントのみ）。到達可能パニック2件をハードニング — ①parse_scene.rs:171 parse_actors_item の n+1/+= 1 整数オーバーフロー（入力 ＝4294967295 で debug ビルド panic 実証）→ saturating_add 化＋回帰テスト2件（u32::MAX 明示番号・MAX 後の暗黙採番が MAX 飽和する境界を固定）②span.rs:101 extract_source が逆転 Span（start_byte>end_byte・pub フィールド経由で構築可能）でスライス panic 実証 → OutOfBounds エラー化＋回帰テスト1件。正当化して維持: action.rs:165 KeyWords::name() の names[0]（grammar key_list = id ~ (comma_sep ~ id)* が≥1保証・手動構築誤用は fail-fast）／parse_action.rs:281 build_left_assoc_expr の terms.remove(0)（全3呼出元が is_empty ガード済）／parse_scene.rs:357 スライスは find() 由来境界で安全。3.3 既知バグ（parse_action.rs:348 paren_expr 第1項のみ包む）は挙動保存により未修正・touched せず。検証: cargo test -p pasta_dsl 239 passed/0 failed（236＋回帰3）・workspace 1581 passed/0 failed（1578＋3）・申し送り: 深い括弧ネスト入力で pest 再帰によるスタックオーバーフロー（深さ100はOK・1000で STATUS_STACK_OVERFLOW プロセス異常終了を実証）— pest 2.x に深さ制限APIなし・事前走査ヒューリスティックは正常系誤検知リスクがあり本ループの挙動保存制約下では修正困難。攻撃面（悪意ある辞書ファイル）の DoS として開発者フォローアップ推奨 |
| 3.5 | pasta_dsl | crates/pasta_dsl/** | G4+G5 | IMPROVED |  | G4: 等価簡素化6件 — parse_action.rs: bin_op_from_rule ヘルパー新設（rule→BinOp 対応表）で parse_expr_from_parts/parse_key_arg/parse_elements.rs parse_var_set の5アーム×3箇所重複 match を一掃・build_left_assoc_expr を remove(0)+index+clone から into_iter+zip へ書換（clone 排除・panic 挙動同等・余剰 op/term 無視のセマンティクス維持をdocコメント化）・parse_scene.rs:172 parse_actors_item の採番 if-else 二重分岐を unwrap_or＋saturating_add 一本化（両分岐で代数的同値）・partial.rs: try_parse_chunk の未使用 _rule 引数削除＋呼出元2箇所の不要 let 束縛を is_some() 判定へ簡素化・mod.rs/ast/mod.rs: 完了済み parser2 移行期の陳腐化 doc（Migration Purpose 節・pasta2.pest 言及）を現行記述へ更新。3.3 既知バグ（parse_action.rs:355 paren_expr 第1項のみ包む）は挙動保存により未修正・維持確認済・G5: README.md ドリフト是正 — 使用例 word.name→word.name()（フィールド非公開化済）・parse_str_partial/PartialParseResult/PartialParseError/CueCommandNode/ChoiceNode を API 一覧へ追加・ファイル構成へ partial.rs/cue.rs 追加・tests/ 一覧を実在13ファイルへ同期。TEST_COVERAGE.md pasta_dsl 分同期（§2.1 に dynamic_call/var_set_none/error_api/expr_parse 4行追加・§3 DoD 62→239・§サマリ表 153→239）。検証: cargo test -p pasta_dsl 239 passed/0 failed・clippy -p pasta_dsl --all-targets 警告0・workspace 1581 passed/0 failed（ベースライン一致） |
| 3.6 | pasta_lua-root | crates/pasta_lua/src/*.rs（直下のみ）, crates/pasta_lua/build.rs, crates/pasta_lua/build_zip.rs, crates/pasta_lua/Cargo.toml, crates/pasta_lua/README.md, crates/pasta_lua/tests/*.rs, crates/pasta_lua/tests/{common,fixtures,transpiler}/** | G1 | IMPROVED |  | G1: ルート層の未テスト公開挙動へ 22 ユニットテスト追加（プロダクションコード変更0）。error.rs+7: UndefinedScene/UndefinedWord/TooManyLocalVariables/StringLiteralError/PropertyInExpression の Display と unsupported()/property_in_expression() コンストラクタ・ConfigError::UnknownLibrary メッセージ。string_literalizer.rs+4: 空文字列・MAX_EQUALS=10 境界成功・全フォーマット危険時の StringLiteralError（エラーパス初到達・ミューテーションで red 確認済）・literalize_with_span のスパン伝搬。config.rs+4: LineEnding::native()/Default 整合・new==default・without_comments の line_ending 維持。context.rs+3: merge_from のレジストリ統合と file_attrs 非統合・get_current_module 初期 None。transpiler.rs+4: with_defaults()・FileAttr 累積（シャドーイング）・writer 失敗→IoError 経路・CrLf 設定でも正規化後 LF のみ（挙動文書化）。sink/source-map seam・without_comments 出力・PropertyInExpression 実発生は tests/transpiler/** で既カバーのため重複追加せず。build.rs/build_zip.rs は tests/build_determinism_test.rs で既カバー。検証: cargo test -p pasta_lua 989 passed/0 failed・workspace 1603 passed/0 failed（ベースライン1581+22） |
| 3.7 | pasta_lua-root | crates/pasta_lua/src/*.rs（直下のみ）, crates/pasta_lua/build.rs, crates/pasta_lua/build_zip.rs, crates/pasta_lua/Cargo.toml, crates/pasta_lua/README.md, crates/pasta_lua/tests/*.rs, crates/pasta_lua/tests/{common,fixtures,transpiler}/** | G2+G3+G4+G5 | IMPROVED |  | G2: 境界内 clippy 21件→0（src/error.rs: io::Error::other 化1件／境界内テスト20件 — needless-borrow 5・mlua 非推奨 Value::as_str→as_string().to_string_lossy() 3・push_str("\n")→push('\n') 3・doc list 過剰インデント 5・不要 usize キャスト 2・drop_non_drop 1・collapsible_if→let チェーン 1）。境界外 55件を申し送り（src/debug 27〈うち deny レベル never_loop エラー2件: transport.rs:639/session.rs:1353 — clippy --all-targets が lib test でコンパイルエラー扱いになるため debug セルで要対応〉・src/sakura_script 14・tests/runtime 6・src/loader 5・src/code_gen 2・src/logging 1）。依存は本体17+build 2+dev 4 全て使用確認・未使用依存なし。workspace 外部未使用 pub（LuaTranspiler::with_defaults・TranspileContext::set_current_module/get_current_module/current_module フィールド）は crates.io 公開クレート（publish=true）の公開API面につき維持・将来メジャー時の整理候補として申し送り・G3: ルート層 src 直下の unwrap/expect/panic は全て #[cfg(test)] 内でプロダクションコード0件（transpiler.rs の from_utf8 は Result 化済）。build.rs/build_zip.rs の panic はビルド時エラー報告イディオム（cargo 環境変数由来パスのみ・symlink スキップ済・ランタイム攻撃面なし）として正当化維持。脆弱性スキャン: トランスパイラ入力はエラー型で防御済・パス処理に traversal なし → ハードニング挙動変更0件（回帰テスト追加不要）・G4: karpathy 準拠検証 — ルート層に過剰複雑化なし（transpile→with_sink→with_source_map の薄いラッパ階層は byte-invariance 契約として文書化済・意図的）→ 是正0件・G5: 3.6 申し送り（with_line_ending(CrLf) が最終出力に無効）へ保守的対応 — pub API 維持のまま config.rs の line_ending フィールド/with_line_ending へ実挙動（最終出力は normalize で常に LF）を正直に doc 化（既存テスト test_transpile_output_is_lf_only_even_with_crlf_config が挙動固定済）。README ソースモジュール構成ツリーの陳腐化是正（build.rs/build_zip.rs・config/context/error/normalize/string_literalizer・debug/encoding/logging/search/sakura_script を実構造へ同期）。TEST_COVERAGE.md pasta_lua 行同期（§3 DoD 233→989・§5 サマリ 522→989）。申し送り: TEST_COVERAGE.md §5 合計行（1050+）と pasta_shiori 等他クレート行は他セル所掌。検証: cargo test -p pasta_lua 989 passed/0 failed・clippy 境界内0・workspace 1603 passed/0 failed（ベースライン一致・insta スナップショット変更なし） |
| 3.8 | pasta_lua/code_gen | crates/pasta_lua/src/code_gen/**, crates/pasta_lua/tests/code_gen/**（新設可） | G1 | IMPROVED |  | テスト31件追加（989→1020・プロダクションコード変更なし・src/code_gen 内 #[cfg(test)] モジュール3件新設 — tests/*.rs が境界外のため tests/code_gen/ サブディレクトリは新設せず）。mod.rs+8: out_line 会計契約（writeln/write_blank_line/write_raw 埋め込み改行計数/write_line_terminator のチョークポイント別増分とバイト一致）・indent 4スペース段付け＋dedent 0 飽和・end_block 形状・CrLf 端末出力・write_header バイト厳密・record_span 無効スパン（end_byte==0）スキップ/有効スパン記録・record_block_line 0 スキップ・シンク未接続時不活性。element_gen.rs+15: 空エスケープ（2文字目なし）の無出力＋無記録・SakuraScript アクション・継続行 actor 未確立→InvalidContinuation/action_line からの話者継承・call_scene 5件（無引数/空 Args 同形/位置+キーワード引数/動的ターゲット tostring/末尾 return TCO）・式 3件（全5二項演算子+括弧+Float+BlankString の厳密表記・Args 0始まり→1始まり変換・expr_fn/GLOBAL 呼出形）・単語定義（空 words 早期 return・PASTA./SCENE: プレフィクス×複数キー行）・無効スパン code_block は出力のみで記録なし。scope_gen.rs+8: actor の空単語定義スキップ＋非 lua コードブロック除外・__start__ の clear_spot→set_spot 宣言順・名前付きシーン counter 接尾辞＋スポット初期化スキップ・actors 空でスポットなし・TCO 末尾 CallScene のみ return・choice ラベル/ターゲット フォールバック・!select 小数引数保持と非数値→nil・非 select キュー無出力。ミューテーション3件（write_raw 改行計数除去/Args index+1→index/TCO return 除去）で新テスト4件 red 確認後復元。検証: cargo test -p pasta_lua 1020 passed/0 failed・workspace 1634 passed/0 failed（ベースライン1603+31・insta スナップショット変更なし） |
| 3.9 | pasta_lua/code_gen | crates/pasta_lua/src/code_gen/**, crates/pasta_lua/tests/code_gen/**（新設可） | G2+G3+G4+G5 | IMPROVED |  | G2: 境界内 clippy 2件→0（3.7 申し送りどおり mod.rs record_span/record_block_line の collapsible_if 2件を let チェーン化）。境界外の残警告（src/debug・src/sakura_script・src/loader・src/logging・tests/runtime — 3.7 台帳の境界外分から code_gen 2件を除いた集合）は該当セルへ申し送り。デッドコード/未使用 pub なし — 全 pub 項目に消費者確認済（transpiler.rs: write_header/generate_global_word/generate_global_scene/generate_actor・debug/source_map.rs: PastaPos/SourceMapSink・tests/transpiler/**: 残り全 generate_* と set_source_map/out_line）・G3: プロダクションコードに unwrap/expect/panic/添字アクセス0件（grep 全数調査 — unwrap 系は全て #[cfg(test)] 内）。非パニック構文のみ: from_utf8().unwrap_or_default()・(*f as i64) 飽和キャスト・saturating_sub・chars().nth(1) は if let 防御済。u32 行カウンタ加算（out_line/content_start_line+offset）は >42億行入力のみで debug オーバーフロー＝パーサー防御済み不変条件と分類し維持 → ハードニング挙動変更0件（回帰テスト不要）・G4: 等価簡素化4件 — element_gen.rs: expr_to_string ヘルパー新設で「Vec::new+generate_expr_to_buffer+from_utf8」5箇所重複を一掃・generate_call_scene の引数ループ（generate_args_string と完全重複の20行）を同関数呼出へ置換・generate_args_string の Positional/Keyword 同形アーム統合・scope_gen.rs: 既 import 済 HashMap の完全修飾再記述を簡素化。元 1,304 行→diff -19 行・G5: doc 正直化1件（generate_call_scene doc の act:call("モジュール名",...) を実出力 SCENE.__global_name__ へ同期）。他 pub doc・依存方向（debug→code_gen シーム不変）は正確と確認。TEST_COVERAGE.md pasta_lua 行同期（§3 DoD 989→1020・§5 サマリ 989→1020、3.8 の +31 反映）。検証: cargo test -p pasta_lua 1020 passed/0 failed・clippy 境界内0・workspace 1634 passed/0 failed（ベースライン一致・insta スナップショット変更なし） |
| 3.10 | pasta_lua/enc-log-search | crates/pasta_lua/src/{encoding,logging,search}/**, crates/pasta_lua/tests/{log,search}/** | G1 | IMPROVED |  | テスト38件追加（1020→1058・プロダクションコード変更なし・全て境界内 #[cfg(test)] 追記＋tests/search/module_test.rs 追記）。encoding+10: mod.rs+2 — 完全未テストの pub path_from_lua（ASCII パススルー/空文字列）・windows.rs+8 — ロケール非依存の明示 CP932/CP_UTF8 指定で MultiByteToWideChar 正常系（SJIS 0x93FA0x967B→日本）/切詰めリードバイト 0x93 単独 Err/不正 UTF-8 Err・string_to_multibyte SJIS 厳密バイト列＋往復・CP932 非収録 €（U+20AC）の default_char なし Err(InvalidInput)/あり b"?" 置換・wide_char_to_multi_byte 空入力。logging+10: logger.rs+3 — カスタム file_path のディレクトリ自動作成/profile/../escape.log の traversal 分岐 PermissionDenied（既存テストは profile 接頭辞分岐のみ）/write→drop 後のファイル内容実在検証・registry.rs+6 — register/get/unregister・同一キー再登録の置換・make_writer コンテキストなし no-op/未登録 dir no-op/登録済みロガーへのルーティング E2E（ファイル内容検証）・LoadDirGuard 2 段ネスト復元・tracing_init.rs+1 — init 冪等/update 前後/不正フィルタ directive フォールバックの無 panic 契約（プロセスグローバルのため単一テストに順序固定）。search+18: error.rs+4 — Display 文言契約 2・From<SearchError> for LuaError の RuntimeError 包装/LuaError パススルー・context.rs+11 — Rust 直接層が完全未テスト→グローバル/ローカル発見・未発見 None・空キー Err(SceneTableError)・Mock セレクタで同接頭辞シーンの順次無重複消費（挨拶_1→挨拶_2→折返し）・単語の登録順決定的消費（東京→大阪）・親スコープ指定時はローカル限定で global フォールバックなし（実挙動の文書化テスト2件 — doc コメントの「local → global fallback」記載と相違）・セレクタ既定リセット・tests/search/module_test.rs+3 — Lua 経由の決定的単語順/親スコープでグローバル語不可視/空シーン名 pcall エラー。RED 確認: 順次消費テストの期待値を故意改変し fail 実証後復元。検証: cargo test -p pasta_lua 1058 passed/0 failed・workspace 1672 passed/0 failed（ベースライン1634+38）。申し送り: search/context.rs の search_scene/search_word doc コメント「fallback strategy (local → global)」が実装（local-only・フォールバックなし）と乖離 — 挙動保存制約により未修正・3.11 G5 で doc 正直化推奨 |
| 3.11 | pasta_lua/enc-log-search | crates/pasta_lua/src/{encoding,logging,search}/**, crates/pasta_lua/tests/{log,search}/** | G2+G3+G4+G5 | IMPROVED |  | G2: logging/registry.rs の死テストコード #[cfg(test)] len()（clippy 警告源）を削除・encoding/mod.rs の未使用 pub(crate) use 再エクスポート（windows/unix 両方・#[allow(unused_imports)] 付き）を削除し説明コメント化 → {encoding,logging,search} 配下 clippy 指摘0（debug/sakura_script の警告・never_loop エラーは境界外かつ stash 検証でベースライン時点から存在を確認）・G3: encoding/windows.rs の Win32 FFI 境界ハードニング — `len as _` キャスト4箇所（MultiByteToWideChar×2/WideCharToMultiByte×2）が i32::MAX 超で負値へラップ（u32::MAX→-1＝null終端扱いで借用スライス境界外読取）する欠陥を buffer_len_to_i32 ガード（i32::try_from→Err(InvalidInput)）へ置換・回帰テスト2件追加（0/1/i32::MAX 受理境界・i32::MAX+1/u32::MAX 拒否）・正常パス（<2GiB）挙動不変・G4: search/context.rs 等価保存リファクタ — SceneTableError 3アーム×2箇所を or-pattern へ統合・set_scene_selector/set_word_selector のセレクタ構築重複を build_selector へ抽出・Lua varargs 整数解析重複（set_scene_selector/set_word_selector メソッド登録内）を parse_selector_args へ抽出・G5: search/context.rs doc 正直化（3.10 申し送り対応）— search_scene/search_word の「fallback strategy (local → global)」記載を実装どおり「親スコープ指定時は local-only でフォールバックなし／None 時は global のみ」へ修正（3.10 の特性テストが実挙動を固定済・プロダクションロジック変更なし）。TEST_COVERAGE.md pasta_lua 行同期（1020→1060・FFI 長ガード回帰2件追記）。検証: cargo clippy -p pasta_lua --all-targets 境界内0件・cargo test -p pasta_lua 1060 passed/0 failed（ベースライン1058+2）・workspace 1674 passed/0 failed（1672+2）・スナップショット変更なし |
| 3.12 | pasta_lua/loader-core | crates/pasta_lua/src/loader/{mod,context,discovery,error}.rs, crates/pasta_lua/tests/loader/** | G1 | IMPROVED |  | テスト27件追加（1060→1087・プロダクションコード変更なし・src 内 #[cfg(test)] 追記＋tests/loader/{startup,lua_passthrough}_test.rs 追記）。discovery.rs+5: base_dir がファイル→DirectoryNotFound・不正 glob パターン（未閉 [）→GlobPattern エラー・絶対パスパターンの traversal ガード棄却・contains_traversal の RootDir/Windows Prefix 拒否・profile2 等の接頭辞類似ディレクトリが除外されない（コンポーネント境界 starts_with）の文書化。error.rs+14: 未テスト Display/コンストラクタ網羅 — ConfigNotFound/Config（実 toml::de::Error）/InvalidFileName/CacheDirectoryError/MetadataError/CacheWriteError/SceneDicGeneration（source Some/None で source() 有無）・parse vs parse_with_source の source チェーン差・From 変換3種（glob::PatternError→GlobPattern/mlua::Error→Runtime/TranspileError→Transpile）・PartialTranspile の空 failures "[]" とカンマ結合パス。context.rs+5: generate_package_path の ?.lua＋?/init.lua 両パターン宣言順厳密一致・バックスラッシュ→スラッシュ正規化・空 search_paths→空文字列・from_config 実在 dir の絶対化＋\\?\ 接頭辞除去・非実在 dir のフォールバック。startup_test.rs+2: pasta.toml 欠如→PastaLoader::load が ConfigNotFound 表面化・カスタム pasta_patterns（extra/*.pasta）と派生 .lua パターンの発見→scene_dic 登録 E2E。lua_passthrough_test.rs+1: ハイフン正規化衝突（my-mod.pasta vs my_mod.lua）で .pasta 優先・lua 無視。RED 確認: generate_package_path の init.lua パターン除去ミューテーションで新テスト2件のみ fail（既存テスト全 pass＝ギャップ実在の証明）後復元。build_source_map は tests/loader_source_map_build_test.rs（境界外）で既カバーのため重複追加せず。検証: cargo test -p pasta_lua 1087 passed/0 failed・境界内 clippy 新規警告0（loader 既存5件は 3.13 所掌・extract.rs 帰属）・workspace 1701 passed/0 failed（ベースライン1674+27） |
| 3.13 | pasta_lua/loader-core | crates/pasta_lua/src/loader/{mod,context,discovery,error}.rs, crates/pasta_lua/tests/loader/** | G2+G3+G4+G5 | IMPROVED |  | G2: 境界4ファイルの clippy 警告0を新鮮再確認（loader 帰属の既存5件は全て extract.rs＝3.15 所掌・debug/* の never_loop deny 2件含む 54 警告も境界外につき申し送り維持）。未使用 pub 3 件（LoaderError::Parse 系・absolute_search_paths）は crates.io v0.2.2 公開 API 面につき 3.2 と同方針で維持（対応テスト test_parse_error_display/test_parse_error_source_none_vs_some/test_absolute_search_paths も維持）。LoaderContext::new は境界外 tests/runtime/encoding_test.rs が消費のため維持。rustfmt ドリフトを境界内のみ是正（discovery.rs 関数シグネチャ・tests/loader 3ファイル）・G3: プロダクションコードの unwrap/expect/panic/添字 0件（全 unwrap_or* フォールバックまたは #[cfg(test)] 内）＝Result 化対象なし。traversal 防御点検: contains_traversal（../絶対/ドライブ接頭辞拒否）・is_within_base_dir・has_symlink_component の3層を確認。Windows ジャンクション懸念を RED 検証 → Rust std の is_symlink() は name-surrogate reparse tag（IO_REPARSE_TAG_MOUNT_POINT）でジャンクションも true を返し既に防御済と実証（mklink /J 実テストが修正なしで pass）→ 挙動固定の境界回帰テスト test_discover_skips_junction_directory を追加。TOML/glob エラーは全て Result 経路済を確認・G4: karpathy 準拠是正2件 — discover_all_files 内のモジュールキー導出インライン重複（20行×2・CacheManager と同一導出）を private free fn module_key へ抽出・process_incremental の未使用 _base_dir パラメータ除去。等価変換のみ・G5: build_source_map_inner の虚偽 doc（「integration tests が直接 assert 可能」— 実際は private）を是正・discover_files doc にシンボリックリンク/ジャンクション skip 挙動を追記・TEST_COVERAGE.md pasta_lua 行同期（1060→1088）。検証: cargo test -p pasta_lua 1088 passed/0 failed（1087＋junction回帰1）・境界 clippy 0・workspace 1702 passed/0 failed（1701＋1） |
| 3.14 | pasta_lua/loader-io | crates/pasta_lua/src/loader/{cache,config,extract}.rs, crates/pasta_lua/tests/loader/** | G1 | IMPROVED |  | テスト21件追加（1088→1109・プロダクションコード変更なし）。cache_test.rs+10: prepare_cache_dir バージョン trim 比較（空白/改行付きで温存）・needs_transpile ソース欠落→MetadataError（エラーパス初到達）・save_cache の base_dir 外絶対パス→CacheWriteError トラバーサル拒否＋境界外書込なし・save_cache 上書き（増分更新）・dic/ 接頭辞なしソースのモジュール名フォールバック・ハイフン→アンダースコアのキャッシュパス側・find_orphaned_caches scene dir 未生成→空（早期 return）・scene_dic.lua/非.lua スキップ＋ネスト orphan 検出（unix 限定内部テストの Windows 互換版）・generate_scene_dic 旧配置（cache_dir 直下）後方互換クリーンアップ＋新配置 pasta/ 検証・cache_dir() アクセサ。config_test.rs+10: 既存カバレッジ精査の結果、PastaConfig::load は startup_test.rs（test_config_load_with_file＝成功経路／test_config_load_not_found＝バリアント＋パス検証）で、TalkConfig は output_test.rs（test_talk_config_default＝禁則2フィールド以外の既定値／from_toml_full／partial_override）で既カバーと判明 → 真の新規分のみ追加: load 不正 TOML→LoaderError::Config（load 3経路中唯一の未カバー経路）・[loader] フィールド型不正→Err（厳格パース）・[loader] 未知キー無視（前方互換）・カスタムセクション型不一致→None 無 panic（.ok() フォールバック）・TalkConfig 禁則文字2フィールド既定値（test_talk_config_default_kinsoku_fields＝output_test 未検証分のみに縮約）・PastaConfig::talk() アクセサ経路（部分指定＋既定混在・None — 増分はアクセサ経由のセクション抽出、TalkConfig 単体の部分上書きは output_test 既カバーと doc 明記）・[debug] セクション欠落→None・port 上書き・effective_file_path 第3分岐（非json非dat→.dat付加）。重複処理決定（Req 2.3・remediation round 1）: 初稿 +12 のうち test_load_reads_pasta_toml_from_base_dir は startup_test 成功経路の純重複につき削除・test_load_missing_file_returns_config_not_found はバリアント＋パスが startup_test、Display メッセージが src/loader/error.rs::test_config_not_found_display（3.12 追加）で既カバー＝増分ゼロにつき削除・test_talk_config_default は test_talk_config_default_kinsoku_fields へリネームし禁則2フィールドへ縮約（増分を doc コメント化）。既カバー出典は config_test.rs 内 NOTE コメントに記録。extract.rs 内 #[cfg(test)]+1: .md5 マーカー前後空白/CRLF でも trim 一致→Skipped＋無書き込み（スナップショット比較・Req 1.5 の trim 挙動固定）。RED 確認: CacheWriteError→MetadataError／port 12345→9276 の故意改変2件で fail 実証後復元。clippy: 境界内テスト追加による新規警告0（digest never_read 2件は stash 検証でベースライン由来＝3.15 G2 所掌）。検証: cargo test -p pasta_lua 1109 passed/0 failed・workspace 1723 passed/0 failed（ベースライン1702+21）。申し送り（プロダクション未修正・挙動保存）: ①save_cache のトラバーサルガード Path::starts_with は字句比較のため `..` 含む相対ソース（dic/../../..）はガードを素通りし cache_dir 外へ書込可能（get_relative_path の doc「save_cache boundary check で捕捉」と乖離・実運用は discovery 由来パスのみで非到達）②source_to_module_name の str::strip_prefix("dic") は「dictionary.pasta」「dicx/」等 dic 始まり名でも誤って剥がす（パターン既定 dic/*/* では非到達）— 3.15 G3/G5 で要検討 |
| 3.15 | pasta_lua/loader-io | crates/pasta_lua/src/loader/{cache,config,extract}.rs, crates/pasta_lua/tests/loader/** | G2+G3+G4+G5 | IMPROVED |  | G2: loader 帰属 clippy 5件（全て extract.rs）を0化 — ①② SyncOutcome::Skipped/Deployed の digest フィールド never_read 2件: pub(crate) 内部 enum・消費側 mod.rs は Ok(_outcome) のみ・値は常にコンパイル時定数 EXPECTED_MD5 でログ記録済につきフィールド除去（ユニットバリアント化・enum doc に根拠記録）③④ モジュール doc list 非インデント2件（旧 L27-28・番号付きリスト直後の結語段落に空行挿入）⑤ collapsible_if（旧 L130 → if had_live && let Err チェーンへ統合）。境界3ファイルの rustfmt ドリフトも是正（3.13 と同方針）。境界外（debug/{session,transport}.rs の never_loop deny 2件・sakura_script 等の警告群）は申し送り維持・G3: 3.14 申し送り2件をハードニング（各 RED 実証→回帰テスト追加）— ①save_cache トラバーサルガード: Path::starts_with は字句比較のため dic/../../../evil.pasta が素通りし OS 解決で cache_dir 外へ書込成功（RED で Ok("pasta.scene..........evil")＋cache/ 直下へ evil.lua 書込を実証）→ strip_prefix 残部の ParentDir コンポーネント検査へ強化・回帰テスト test_save_cache_rejects_parent_traversal_in_relative_source（Err(CacheWriteError)＋cache_dir 外無書込）②dic 接頭辞剥離の非コンポーネント境界: str::strip_prefix("dic") が dictionary.pasta→pasta.scene.tionary・dicx/foo→pasta.scene.x.foo と誤剥離（RED 実証）→ strip_component_prefix ヘルパー（/・\ 境界または完全一致のみ剥離）へ修正・回帰テスト3件（module_name 側2＋cache_path 側1）。いずれも不正入力面のみの挙動変化＝許容ハードニング（dic/ 配下の正常系は既存テスト28件全パスで不変確認）。サーベイ: extract.rs zip 展開は enclosed_name で Zip Slip 防御済・EMBEDDED_ZIP はコンパイル時内蔵の信頼正本・3ファイルのプロダクション unwrap/expect/panic/添字 0件＝Result 化追加対象なし。generate_scene_dic の require("{module}") 補間はファイル名由来だがゴースト作者＝Lua 実行権限保持者の信頼域内につき非対象と判断・G4: source_to_module_name/source_to_cache_path の dic 剥離インライン重複（6行×2）を strip_component_prefix へ抽出（G3②と同一変更・等価＋境界修正のみ）。config.rs は get_custom_config 共通化済・簡素で是正なし・G5: get_relative_path doc「save_cache boundary check で捕捉」の乖離（3.14 指摘）は G3① 修正により実態と一致化（doc 文言変更不要）。TEST_COVERAGE.md pasta_lua 行同期（1088→1113・3.14 の +21 未同期分も含めて是正）。依存整合: zip/toml/serde/tracing 全て使用中・未使用なし。pub API（save_cache 等のシグネチャ）変更なし。検証: cargo test -p pasta_lua 1113 passed/0 failed（1109＋回帰4）・loader 帰属 clippy 0・workspace 1727 passed/0 failed（1723＋4）・スナップショット不変 |
| 3.16 | pasta_lua/runtime | crates/pasta_lua/src/runtime/**, crates/pasta_lua/tests/{runtime,shiori}/** | G1 | IMPROVED |  | テスト28件追加（pasta_lua 1113→1141・プロダクションコード変更なし）。事前 grep で全テスト所在（tests/**・src #[cfg(test)]・lua_specs）を照合し真の未到達のみ選定。新規 runtime_api_test.rs +20: exec_file 3（成功/欠損 Err/スクリプトエラー伝播 — 全テスト未到達だった公開 API）・exec_named 2（評価結果＋チャンク名:行番号がエラーへ反映される契約。初稿は error("msg") で検証し position 情報が付かず organic RED → nil 算術の実行時エラーへ是正＝アサーションが実挙動を噛む証拠）・register_module 2（require 連携・再登録 last-wins — 未到達公開 API）・with_config 不正ライブラリ名 ConfigError 伝播 1・config() ad-hoc None 1・lua() VM 状態共有 1・load_scene_dic 3（実行/チャンク名 pasta.scene_dic/欠損 Err — 未到達公開 API）・from_loader 4（entry.lua 読込＋実行時エラーでも警告継続の graceful degradation 2・transpiled コード宣言順実行＋エラー時モジュール名チャンク伝播 2 — 従来 &[] 空のみ）・@pasta_config 3（toml_to_lua の int/float/bool/datetime/array 1-based/nested 変換・actor name 注入＋既存 name 上書き・非テーブル actor no-op — 従来 string 1 型のみ。actor 注入は tests/loader/config_actors_initialization_test.rs の STORE 連携既カバーに対するモジュール登録境界の増分と明記）。persistence_integration_test.rs +5: load() の劣化動作（壊れた JSON→空テーブル・スカラー JSON→空テーブル・gzip マジック付き破損→空テーブル — src 単体テストは load_from_file の Err までで Lua 境界のフォールバック未到達）・モジュール経由 obfuscate=true 往復（gzip マジック確認込み）・save() 失敗時 (nil, "Failed to save...")（保存先に同名ディレクトリ）。encoding_test.rs +3: to_ansi 不正 UTF-8 入力 (nil,err)・[windows] 絵文字の ANSI 変換不能 (nil,err)・[windows] 不正 ANSI バイト 0x81 の to_utf8 (nil,err) — enc.rs のエラー3分岐を初到達。clippy: tests/runtime 既存6件（3.17 所掌）は位置照合で全て既存ファイル由来・新規ファイル/編集箇所の新規警告0。検証: cargo test -p pasta_lua 1141 passed/0 failed（1113＋28）・workspace 1755 passed/0 failed（1727＋28） |
| 3.17 | pasta_lua/runtime | crates/pasta_lua/src/runtime/**, crates/pasta_lua/tests/{runtime,shiori}/** | G2+G3 | IMPROVED |  | G2: 申し送りの tests/runtime clippy 6件を解消（runtime_toggle_e2e_test.rs collapsible_if 1＋doc list 3・debug_integration_test.rs collapsible_if 1・finalize_scene_test.rs needless borrow 1）→ 境界内警告0。src/runtime・tests/shiori は新鮮確認で警告0。境界外警告（src/debug・src/sakura_script）は申し送り維持。デッドコード/未使用 pub なし（pub API は公開面につき維持）。finalize.rs の TODO コメントを将来拡張ノートへ言い換え。3.16 提案採用: runtime_api_test のチャンク名検証を完全形 [string "pasta.scene_dic"]:2: へ厳密化・G3: src/runtime 全7ファイル（2,484行）panic 調査 — プロダクション unwrap/expect/panic 0件（全unwrapは #[cfg(test)] 内）・persistence.rs の data[0]/[1] は len>=2 ガード済・path traversal 検証/atomic write/破損フォールバック既存。到達可能 abort 1件を発見・修正: log.rs table_to_string が深い入れ子テーブルを再帰 serde 変換しており Lua 入力（`for _=1,200000 do t={t} end`＋log.info）でホストスタックオーバーフロー（STATUS_STACK_OVERFLOW 0xc00000fd を RED で実証）— SHIORI 境界越し abort（R3.7 安全性修正）。反復走査の深さ事前ゲート table_depth_exceeds（再帰なし・循環でも停止）を変換前に挿入し tostring() フォールバックへ退避（深さ>10 と循環の可観測挙動は旧 json_depth 事後チェック/deny_recursive_tables と同一・json_depth は冗長化につき除去）。境界回帰テスト2件追加（200k 深入れ子・自己参照循環、tests/runtime/runtime_api_test.rs）。脆弱性評価: gzip 展開は無制限だがセーブファイルはローカル書込権限前提のため変更なし・toml_to_lua 再帰は信頼済 pasta.toml 入力で低リスク維持。pasta_lua 1143/0（基線1141＋2）・workspace 1757/0（基線1755＋2） |
| 3.18 | pasta_lua/runtime | crates/pasta_lua/src/runtime/**, crates/pasta_lua/tests/{runtime,shiori}/** | G4+G5 | IMPROVED |  | G4: karpathy 準拠検証 — runtime 全7ファイル（2,484行）精査。是正1件: log.rs の5レベルログ関数（trace/debug/info/warn/error・本体17行×5の機械的重複）を log_fn! 宣言マクロへ統合（69行→29行・tracing マクロはレベルがコンパイル時必須のためマクロ生成が唯一の重複排除手段・既存5レベルテスト＋caller info テストで等価性確認）。他は是正なしと判断: finalize.rs/module_registry.rs は 3.x 系で抽出済（collect_word_entries/set_loaded_module）・enc.rs の2関数はエラーメッセージ可観測につき統合せず・mod.rs の source_map 保持 if は doc 密度優先で維持・runtime_config.rs to_stdlib の XOR は注釈済イディオムで維持＝mlua グルーの不可約ボイラープレートと分類。3.17 申し送り採用: log.rs 深さゲートの境界ペアテスト2件追加（runtime_api_test.rs — 深さ10=JSON 変換でリーフ値露出/深さ11=tostring フォールバックでリーフ値非露出・MAX_NESTING_DEPTH 10→11 ミューテーションで深さ11テスト RED 実証後復元＝閾値ドリフト防止を固定）・G5: doc 同期3件 — mod.rs new() doc へ常時登録の @pasta_log を追記（実装 register_log_module と一致化）・README 組み込みモジュール表へ @pasta_persistence 行追加（loader 経路で常時登録済も表に欠落）・README std ライブラリ表の std_utf8 行（parse_std_lib 非対応＝指定すると UnknownLibrary エラーになる虚偽記載）を削除し実対応の std_jit/std_ffi/std_bit 3行追加＋std_all 例コメントの utf8 言及除去（LuaJIT に utf8 stdlib なし）。TEST_COVERAGE.md pasta_lua 行同期（1113→1145・§2.4 へ runtime_api_test 行追加・§3 DoD 更新）。依存整合: runtime 使用の mlua/serde_json/flate2/toml/thiserror/tracing/pasta_core 全て使用中・未使用なし。検証: cargo test -p pasta_lua 1145 passed/0 failed（1143＋境界ペア2）・境界内 clippy 0（残警告は src/debug・src/sakura_script の境界外申し送り分）・workspace 1759 passed/0 failed（1757＋2）・スナップショット不変 |
| 3.19 | pasta_lua/sakura_script | crates/pasta_lua/src/sakura_script/**, crates/pasta_lua/tests/sakura_script/** | G1 | IMPROVED |  | テスト21件追加（pasta_lua 1145→1166・プロダクションコード変更なし・新規 tests/sakura_script/edge_case_test.rs＋main.rs へ mod 登録のみ）。事前 grep で全テスト所在（src #[cfg(test)] 44件・tests/sakura_script 30件・lua_specs の sakura_builder は境界外別層）を照合し真の未到達のみ選定。tokenizer エッジ6: 末尾孤立 `\`→General・`\あ`（タグ外文字直前の `\`）→General・`\\h`→General `\`＋タグ `\h`・未閉ブラケット `\s[0`→タグ `\s`＋`[` は LineEndProhibited（既定 chars_line_end_prohibited に ASCII `[` が含まれる実挙動を organic RED で発見し文書化）＋General `0`・空パラメータ `\s[]`→単一タグ・`\![open,inputbox]` カンマ含みパラメータ→単一タグ。classify 優先順位2: `･`（U+FF65）は既定 leader と line_start_prohibited の両方に重複収録→Leader 優先勝ち・`､`（U+FF64）は comma 非収録→LineStartProhibited。wait_inserter 5: LineStartProhibited のみの連続列→ウェイトなし（unwrap_or(0) 経路初到達）・読点＋」で max 非上昇・」→！で strong が max 採用・SakuraScript タグによる pending 句読点 flush・LineEndProhibited による flush。mod.rs/Lua API 8: register(lua, None) ハードコード既定値経路（初到達）・actor 文字列（非テーブル非nil）→config フォールバック・actor ウェイト 30（<50 床）でタグ抑止・budoux={} 空配列→改行なし・budoux=6 非テーブル→無視・budoux={true} 非数値要素→Lua エラー伝播の文書化・break_lines widths 文字列→入力素通し・break_lines タグのみ入力→無変更。RED 確認: 未閉ブラケットテストが初稿期待値 General で organic fail→実挙動（LineEndProhibited）へ是正＝アサーションが実挙動を噛む証拠。clippy: 新規ファイル由来の警告0（sakura_script src の既存14件＋never_loop は 3.20 G2 所掌・不変）。検証: cargo test -p pasta_lua 1166 passed/0 failed（1145＋21）・workspace 1780 passed/0 failed（1759＋21） |
| 3.20 | pasta_lua/sakura_script | crates/pasta_lua/src/sakura_script/**, crates/pasta_lua/tests/sakura_script/** | G2+G3+G4+G5 | IMPROVED |  | G2: 申し送りの clippy 14件は全て line_breaker.rs テスト内 needless_borrow（`&m`→`m`、model() が既に `&'static Model` を返すため）→機械修正でゼロ化・境界内 clippy 指摘 0 達成（never_loop deny 2件は src/debug 帰属＝3.21/3.22 所掌で不変・本セル対象外を確認）。デッドコード/未使用 pub なし（pub API は lib.rs `pub mod sakura_script` 経由の公開面で除去禁止・全て統合テストから使用確認）。G3: パニック調査 1,412 行 — tokenizer.rs `bytes[pos]` は `pos < input.len()` ガード済・全 `&input[pos..]` スライスは char 境界不変条件維持（タグ正規表現マッチ長 or len_utf8 で前進）＝到達不能、line_breaker.rs tag_ranges は find_iter 由来で境界整列・`widths[min(len-1)]` は空チェック後＝到達不能、wait_inserter.rs `unreachable!()` は外側 match アームで変種制約済＝証明可能な到達不能（SAFETY 文書化済・Result 化不要）、mod.rs は全可謬経路 LuaResult 済・不正 UTF-8 は mlua String 変換で Lua エラー化（パニックなし）・巨大文字列は全アルゴリズム線形＝再帰なし。到達可能パニック経路ゼロ→ハードニング変更なし・回帰テスト不要。G4: mod.rs の Lua テーブル→Vec<usize> 変換重複（apply_budoux_if_configured / break_lines_lua_impl で逐語一致 8 行×2）を table_to_widths ヘルパーへ抽出・resolve_wait_values の未使用 `_lua` パラメータ除去（連鎖で talk_to_script_impl の lua パラメータも除去・全て private fn で公開面不変）。G5: register() doc の戻り値記述に break_lines 欠落→補記・TEST_COVERAGE.md pasta_lua 行 1145→1166 同期（3.19 の 21 件反映）。申し送り: crates/pasta_lua/README.md の @pasta_sakura_script 例が実装と不一致（`actor={talk={script_wait_default=50}}` だが実装は actor テーブル直下の script_wait_normal 等を参照・出力例 `\_w[100]` も不正確）— README は境界外。検証: cargo test -p pasta_lua 1166/0・境界内 clippy 0・workspace 1780/0・スナップショット不変 |
| 3.21 | pasta_lua/debug-core | crates/pasta_lua/src/debug/{mod,types,transport}.rs, crates/pasta_lua/tests/debug/**（新設可） | G1 | IMPROVED |  | テスト14件追加（pasta_lua 1166→1180・プロダクションコード変更なし・src 内 #[cfg(test)] 追記のみ。tests/debug/ は root tests/*.rs（境界外）への mod 登録なしでは自動検出されないため新設せず src 内一択と判断）。事前 grep で全テスト所在（src #[cfg(test)] 46件・tests/runtime/debug_integration_test.rs・runtime_toggle_e2e_test.rs・tests/loader/config_test.rs の from_file 系・wiring/session の SharedSourceMode 消費テスト）を照合し真の未到達のみ選定。transport.rs+7: read_frame エラー4分岐を初到達 — 非数値 Content-Length→InvalidData・ヘッダブロック途中 EOF→UnexpectedEof（フレーム間クリーン EOF Ok(None) との対比固定）・非 UTF-8 ボディ→InvalidData・不正 JSON ボディ→InvalidData（Content-Length: 0 の空ボディ拒否も同梱）・bare-LF ヘッダ行終端の許容・enabled transport の shutdown 後 send→Disconnected（冪等 shutdown 込み・accept 解放は connect-and-drop）・disabled の join/shutdown no-op 冪等。mod.rs+4: enable() bind 失敗→DebugError::Bind 初到達（ループバック :0 を先占有・パニックなし／サイレント無効化なしを固定）・SharedSourceMode の clone 共有 get/set 往復（Lua⇔Pasta 双方向）・SourceMode as_u8/from_u8 往復＋未知バイト→Pasta 防御既定・from_file の不正 present_as→Pasta フォールバック（file_source_mode 単位）。types.rs+3: ペイロードなしコマンド8種（RefreshPresentation/Disconnect 含む）の pairwise distinct＋Clone 同一性・結果系イベント5種（Breakpoints[verified=false]/Stack/Scopes/Variables/Threads）のペイロード往復＋Terminated との distinct・StopReason 4種 pairwise distinct。固定ポート不使用（bind は全て 127.0.0.1:0）。RED 確認: 1ファイル1件ずつ期待値ミューテーション3件（EOF kind 差替/SharedSourceMode 伝播否定/イベント同一性差替）で全 fail 実証後復元。never_loop deny（transport.rs テスト内）は 3.22 G2 所掌につき不変。検証: cargo test -p pasta_lua 1180 passed/0 failed（1166＋14）・workspace 1794 passed/0 failed（1780＋14） |
| 3.22 | pasta_lua/debug-core | crates/pasta_lua/src/debug/{mod,types,transport}.rs, crates/pasta_lua/tests/debug/**（新設可） | G2+G3+G4+G5 | IMPROVED |  | G2: transport.rs テスト内 clippy deny never_loop（一周で必ず break/panic するループ）を単一 match へ置換し --all-targets fail 要因を半減（残る deny は session.rs:1353＝境界外・該当セル所掌）・read_frame ヘッダ解析の collapsible_if を let チェーンへ機械修正・mod.rs doc_lazy_continuation 7件（行頭 `+` の Markdown リスト誤認）を `+` 行末移動で解消・ブランケット #![allow(dead_code)]（transport.rs 全体）を除去し実際に masked だった shutdown/join のみメソッド単位 #[allow(dead_code)]＋理由コメントへ縮小（テスト専用 teardown ヘルパ・pub(crate) 設計シームとして維持）→境界内 clippy 指摘 0 達成。G3: パニック調査 — mod/types/transport の本番経路に unwrap/expect/インデックス無し・types は純 DTO・境界内 Mutex 不使用（poisoning 非該当・SharedSourceMode は AtomicU8）。信頼境界（TCP デバッガクライアント）の read_frame に Content-Length 無制限アロケーション欠陥（攻撃者制御長で vec![0;len] 事前確保 → OOM/DoS）→ 16MiB 上限ガード追加（許容ハードニング・挙動変化は >16MiB フレームの InvalidData 化のみ・実 DAP メッセージは遥かに小さい）。回帰テスト1件: 上限+1→InvalidData（cap 名指し）＋上限ちょうど→ガード非発火（UnexpectedEof）を固定・RED 実証（ガード無効化で UnexpectedEof fail → 復元で pass）。G4: karpathy 準拠確認 — 境界内に投機的抽象・単一用途の過剰柔軟性なし、never_loop 解消が等価簡素化を兼ねる・追加是正なし。G5: mod.rs モジュールヘッダの陳腐化記述（「enabled パスは skeleton DebugHandle を返す」＝task 4.1 完了前の記述）を full wiring 実装へ同期・re-export 位置コメント誤記（above→below）修正・TEST_COVERAGE.md pasta_lua 行 1166→1181 同期（3.21 の 14 件未反映分＋本セル回帰 1 件）。申し送り: inspect.rs collapsible_if×8/type_complexity×1・wiring.rs needless_borrows×3/doc_lazy×1/collapsible_if×1・source_map.rs nonminimal_bool×1/doc_overindented×2・session.rs never_loop deny は全て境界外＝該当セルへ。検証: cargo test -p pasta_lua 1181/0（1180＋1）・workspace 66スイート全 pass/0 fail・境界内 clippy 0 |
| 3.23 | pasta_lua/debug-dap | crates/pasta_lua/src/debug/{dap,breakpoints}.rs, crates/pasta_lua/tests/debug/**（新設可） | G1 | IMPROVED |  | テスト10件追加（pasta_lua 1181→1191・プロダクションコード変更なし・src 内 #[cfg(test)] 追記のみ＝3.21 と同判断で tests/debug/ 新設せず）。事前 grep で全テスト所在（dap.rs 34件・breakpoints.rs 10件・tests/runtime/debug_integration_test.rs・runtime_toggle_e2e_test.rs）を照合し真の未到達のみ選定。dap.rs+8: 文書化済み劣化経路を初到達 — setBreakpoints 不正引数（source.path 欠落→空パス・line 欠落/非数値エントリのスキップと正常隣接行の温存）・seq 欠落/非数値→request_seq 0 フォールバック・scopes frameId 欠落→frame 0/varRef 1・variables variablesReference 欠落→var_ref 0・attach の非文字列 sourcePresentation→None（不正文字列の Pasta フォールバックとの型ゲート差を固定）・未対応イベント2件 — pending なし deferred イベント→request_seq 0 で正常エンコード・SessionEvent::Scopes のワイヤ no-op＋seq 非消費・PendingTable のキンド別 FIFO 独立性（threads/stackTrace 逆順到着で横取りなし）。breakpoints.rs+2: poisoned lock 劣化（doc 契約の初到達 — should_pause fail-safe false・register 無パニック no-op・set_breakpoints は resolved 返却継続）・重複要求行（resolved は要求順を重複込みでミラー・ストアは HashSet dedup で発火）。RED 確認: spurious イベントの request_seq 期待値ミューテーション（0→99）で fail 実証後復元。境界内 clippy 新規警告0（session.rs:1353 never_loop deny は境界外・3.27/3.28 所掌で不変）。検証: cargo test -p pasta_lua 1191 passed/0 failed（1181＋10）・workspace 1805 passed/0 failed（1795＋10） |
| 3.24 | pasta_lua/debug-dap | crates/pasta_lua/src/debug/{dap,breakpoints}.rs, crates/pasta_lua/tests/debug/**（新設可） | G2+G3+G4+G5 | IMPROVED |  | G2: dap.rs/breakpoints.rs 帰属 clippy 0 を新鮮確認（残存 17 警告は inspect9/wiring5/source_map3＋session.rs:1353 never_loop deny＝全て境界外申し送り維持）。ブランケット #![allow(dead_code)] を両ファイルから除去 — breakpoints.rs は全項目使用中で allow 自体が不要、dap.rs で唯一 masked だった私的デッド関数 encode_scope（即時 scopes 応答方式により未使用の投機保持）を除去（pub API 除去なし・Scope import はテスト側へ移動・モジュール doc の [`Scope`] リンクはフルパス化で温存）。G3: パニック調査 2,179 行 — 本番経路に unwrap/expect/インデックス 0 件（DAP JSON アクセスは全て get/and_then/unwrap_or 劣化・json! オブジェクトへの IndexMut のみ・breakpoints は poisoned lock fail-safe/no-op 済を確認）。到達可能パニック 1 件: scopes ハンドラの var_ref = frame_id + 1 が信頼境界外 frameId=u32::MAX でデバッグビルド加算オーバーフローパニック → saturating_add(1) 化（DAP 予約 0 回避の非ゼロ性維持）＋境界回帰テスト 1 件（RED 実証: attempt to add with overflow で fail → 修正後 pass）。G4: decode_request 全 12 アームの Decoded 5 フィールド逐語繰返しを struct update 構文 ..Decoded::default() へ集約（等価・全アーム既存テストでカバー・フィールド追加時の将来チャーン削減・約 35 行削減）。rustfmt 一括適用はワークスペース非 fmt-clean（全体 350 diff）のため見送り＝外科的変更維持。G5: TEST_COVERAGE.md pasta_lua 行 1181→1192 同期（3.23 の 10 件未反映分＋本セル回帰 1 件）。モジュール doc は実装と整合（3.23 が劣化経路を検証済み）・追加是正なし。検証: cargo test -p pasta_lua 1192/0（1191＋1）・workspace 1806/0（1805＋1）・境界内 clippy 0 |
| 3.25 | pasta_lua/debug-hook-inspect | crates/pasta_lua/src/debug/{hook,inspect}.rs, crates/pasta_lua/tests/debug/**（新設可） | G1 | IMPROVED |  | テスト10件追加（pasta_lua 1192→1202・プロダクションコード変更なし・src 内 #[cfg(test)] 追記のみ＝3.21/3.23 と同判断で tests/debug/ 新設せず）。事前 grep で全テスト所在（hook.rs 6件・inspect.rs 9件・session/wiring の E2E・tests/runtime/debug_integration_test.rs）を照合し真の未到達のみ選定。hook.rs+6: ハンドラ Err swallow 分岐を初到達（Err 連発下でチャンク完走＋正値返却＋panic 側チャネル None 維持＝Err≠panic の弁別）・record_panic_cause 優先順位3段の直接固定 — 既存パニックテストは pre-record とペイロードが同文言で順位非弁別だったため (1)pre-record 優先（ペイロードで上書きしない）/(2a)&'static str ペイロード回収/(2b)String ペイロード回収/(3)非文字列→汎用マーカー "hook handler panicked" を各個固定＋poisoned lock 無パニック no-op。inspect.rs+4: FrameInfo::func_name 全テスト未検証→ネスト呼出で複数 Lua フレーム callee→caller 順＋callee func_name="inner" 解決＋トップレベルチャンク None・frame_level=1 の呼出元フレーム変数捕捉（outer_var=99 取得かつ callee ローカル不可視で対象フレームを弁別）・範囲外 frame_level=200→空 Vec＋VM スタック健全（R2.5 劣化経路）・number repr 未到達分岐（小数 2.5 保持/負整数 -3 小数点なし/math.huge→inf 非有限経路）。RED 確認: record_panic_cause の pre-record ガード除去ミューテーションで prefers テスト fail 実証後復元。新テストの collapsible_if 4件は let チェーンで回避し境界内新規 clippy 警告0（inspect.rs 既存9警告は 3.26 所掌で不変・session.rs:1353 never_loop deny は境界外申し送り維持）。検証: cargo test -p pasta_lua 1202 passed/0 failed（1192＋10）・workspace 1816 passed/0 failed（1806＋10） |
| 3.26 | pasta_lua/debug-hook-inspect | crates/pasta_lua/src/debug/{hook,inspect}.rs, crates/pasta_lua/tests/debug/**（新設可） | G2+G3+G4+G5 | IMPROVED |  | G2: inspect.rs 帰属 clippy 9件をゼロ化（collapsible_if 8件＝lib 1＋tests 7 を let チェーン化・type_complexity 1件をテスト型エイリアス CoroutineCapture 抽出で解消）・hook.rs 0件を新鮮確認。staged-wiring 期のブランケット #![allow(dead_code)] を両ファイルから除去 — capture_stack/capture_variables/LineHook/install は session.rs・mod.rs 消費済みで allow 不要化、唯一 masked だった HookHandle::panic_cause（フィールド＋メソッド）は本番配線（mod.rs:632）が handle を破棄しテストのみが読む設計シームのため cfg_attr(not(test), allow(dead_code))＋理由コメントへ精密化（pub(crate) API 除去なし）。G3: パニック/脆弱性調査 — 非テスト経路に unwrap/expect/インデックス 0件・hook コールバックは catch_unwind 全包囲＋全アーム Ok(Continue) で FFI 境界越えパニックなし（R3.7 充足を実装確認）・record_panic_cause は poisoned lock を let-else no-op 処理済・inspect は null ガード/CString フォールバック/MAX_STACK_LEVELS・c_int::MAX ループ上限完備で再帰皆無（テーブルはアドレスプレースホルダ表示＝3.17 log.rs 同型の深ネスト再帰問題は構造的に不存在）。是正 1 件: capture_variables の debug_assert_eq! が lua_settop 復元前に発火し得る順序欠陥（デバッグビルドで不変条件違反時に VM スタック未復元のまま unwind）→復元先行へ並べ替え（release 挙動同一・既存スタック健全性テスト網が平衡経路を被覆）。G4: karpathy 準拠検証 — 投機的抽象・単一用途過剰柔軟性なし（LineHook trait＋blanket impl は session.rs 消費・FFI ガードは全て要件起因）・追加是正なし。G5: hook.rs LineHook doc の実装矛盾「returned Err propagates as a Lua error」（実装は swallow＝handler_error_is_swallowed テストが固定済み）を是正・「future DebugSession (task 2.2)/full wiring (task 4.1)」時制 4 箇所を現実装（session.rs/debug::enable）へ同期・inspect.rs frame_level doc の「For THIS task」を session 経由の実態へ同期・TEST_COVERAGE.md pasta_lua 行 1192→1202 同期（3.25 の 10 件反映・DoD 行＋サマリー行）。申し送り: wiring.rs:364 collapsible_if・session.rs:1353 never_loop deny（--all-targets abort 要因）は境界外＝該当セル所掌。検証: cargo test -p pasta_lua 1202/0・workspace 1816/0・境界内 clippy 0 |
| 3.27 | pasta_lua/debug-session | crates/pasta_lua/src/debug/session.rs, crates/pasta_lua/tests/debug/**（新設可） | G1 | IMPROVED |  | テスト12件追加（pasta_lua 1202→1214・プロダクションコード変更なし・src 内 #[cfg(test)] 追記のみ＝3.21/3.23/3.25 と同判断で tests/debug/ 新設せず）。事前 grep で全テスト所在（session.rs 既存40件・inspect.rs の capture 関数テスト・tests/runtime/debug_integration_test.rs＝SessionCommand 直接使用 0 件）を照合し真の未到達のみ選定。stop_loop ルーティング+5: StackTrace→Stack イベント（停止行・ソース名検証＋progress 凍結で keep-blocking 固定。既存テストは非再開のみ検証でイベント未検証だった）・Variables→Variables（frame-0 ローカル a/b を名前で検証＋var_ref=0 の saturating_sub 下限＝アンダーフローなしで frame 0 へ縮退）・Scopes→単一 Locals スコープ＋variables_reference=frame_id+1 番号付け・Threads→単一 main スレッド（id=MAIN_THREAD_ID/name=main）・SetBreakpoints ライブ適用（停止中 {2}→{5} 置換が無イベント・無再開で受理され Continue 後に新行 5 で停止＝置換セマンティクスと Arc<Mutex> 共有を E2E 固定）。チャネル切断+1: 全 cmd_tx drop→recv Err 分岐初到達（VM 解放・ホスト完走・Terminated 非送出＝明示 Disconnect 経路との弁別）。step_should_stop 純粋判定+4: thread 不一致スキップ全 kind・Over/In/Out の depth×line 判定行列（E2E のみだった式を変異耐性化）。ユニット+2: source_mode() の shared cell フリップ即時観測（baked Lua×shared Pasta→双方向）・resolve_current_pasta の 4 象限ゲーティング（Pasta+map+対応行のみ Some・未対応行/Lua モード/map なし→None）。RED 確認: Scopes 番号付け frame_id+1→+2 ミューテーションで fail（left:6 right:5）実証後復元。固定ポート不使用・新規 clippy 警告 0（session.rs:1353 never_loop deny は 3.28 所掌で不変）。検証: cargo test -p pasta_lua 1214 passed/0 failed（1202＋12）・workspace 1828 passed/0 failed（1816＋12） |
| 3.28 | pasta_lua/debug-session | crates/pasta_lua/src/debug/session.rs, crates/pasta_lua/tests/debug/**（新設可） | G2+G3 | IMPROVED |  | G2: session.rs:1353 の clippy deny never_loop（テスト StepHost::recv_stop 内・全アーム発散ループ）を 3.22 transport.rs と同型の単一 match へ等価再構成 — pasta_lua --all-targets を abort させる最後の deny を解消しフル clippy 実行可能化・session.rs 帰属 clippy 0 を新鮮確認（残存 9 警告は wiring.rs 5/source_map.rs 3＋lib1＝境界外申し送り維持）。staged-wiring 期のブランケット #![allow(dead_code)] を除去 — 完全デッドだった mode()（lib/テスト双方で呼出 0 件）を削除（pub(crate)＝非 pub 除去のみ）、テスト専用観測ヘルパ source_map()/source_mode()/report_error は #[cfg(test)]＋理由コメントへ精密化（on_line_impl doc の Self::report_error intra-doc リンクは非テストビルドの rustdoc 破損を避けるため文言参照化）。G3: パニック/脆弱性調査（本番部 ~810 行）— unwrap/expect/インデックス 0 件（source_and_line は unwrap_or 劣化・Variables decode は saturating_sub 済・チャネル send/recv は全アーム let _ =／Err→VM 解放設計・RefCell は単一 VM スレッド規律＋mode 事前 clone で重複 borrow 経路なし・境界内 Mutex 不使用＝BreakpointSet 側 poisoned fail-safe 済を確認）。到達可能パニック 1 件: stop_loop Scopes ハンドラの variables_reference = frame_id + 1 が信頼境界外 frameId=u32::MAX でデバッグビルド加算オーバーフローパニック（VM スレッド死）→ saturating_add(1) 化（3.24 dap.rs と同型・通常域挙動同一）＋境界回帰テスト 1 件（RED 実証: "attempt to add with overflow" panicked at session.rs:638 で fail → 修正後 pass・saturate 値 u32::MAX 返却＋keep-blocking＋Continue 正常再開を固定）。検証: cargo test -p pasta_lua 1215/0（1214＋1）・workspace 1829/0（1828＋1）・境界内 clippy 0 |
| 3.29 | pasta_lua/debug-session | crates/pasta_lua/src/debug/session.rs, crates/pasta_lua/tests/debug/**（新設可） | G4+G5 | IMPROVED |  | G4: karpathy 準拠検証（本番部 ~810 行）— 是正1件: on_line_impl のステップ精密化が pasta ゲート（effective_mode==Pasta && map 有）と resolve_current_pasta(source,line) を行頭計算済みの `pasta`/`cur` と逐語重複再計算 → 再利用へ等価集約（同一行内の単一ゲート/単一解決化・純粋等価変換・公開面不変）。是正なし判断（clean）: stop_loop の9アーム match は各アーム3〜8行＋個別 doc 付きで抽出は間接化のみ＝不可約 channel オーケストレーション・Stopped イベント構築2箇所は1行構造体リテラルでヘルパ化は過剰抽象・stop_loop 内 source_and_line/current_thread_and_depth の停止点再取得はシグネチャ結合回避で維持。G5: 陳腐化 doc 4箇所同期 — source_map フィールド doc「CONSUMER lands in task 5.4」未来時制→実装済み consumer（resolve_current_pasta/on_line_impl）へ・with_source_map「SKELETON plumbing only」→実態へ・モジュール doc Implemented scope 見出し 2.2+2.5→2.2/2.5/4.1/5.4/5.5 拡張＋.pasta 粒度精密化/break anchor/attach フリップ段落追記・テスト節ヘッダ「No stepping behavior is implemented here」→ステップ挙動テスト所在へ更新。README は境界外・確認のみ: debug/ 言及はディレクトリ構成1行のみで drift なし。依存整合: session.rs は mlua＋crate 内部のみ・未使用依存なし。TEST_COVERAGE.md pasta_lua 行 1202→1215 同期（DoD 行＋サマリー行・3.27 の12＋3.28 の1を反映）。検証: cargo test -p pasta_lua 1215/0・session.rs 帰属 clippy 0（--all-targets フル実行・残8警告は wiring5/source_map3＝境界外申し送り維持）・workspace 1829/0 |
| 3.30 | pasta_lua/debug-source_map | crates/pasta_lua/src/debug/source_map.rs, crates/pasta_lua/tests/debug/**（新設可） | G1 | IMPROVED |  | テスト11件追加（pasta_lua 1215→1226・source_map.rs 内 21→32・プロダクションコード変更なし・src 内 #[cfg(test)] 追記のみ＝3.21/3.23/3.25/3.27 と同判断で tests/debug/ 新設せず）。事前 grep で全テスト所在（src 内 21件・tests/loader_source_map_build_test.rs＝loader 集約/サイドカー往復/無効時非出力・tests/transpiler/source_map_seam_test.rs・record_wiring_test.rs・chunk_name_validation_test.rs・tests/runtime/source_map_handoff_test.rs）を照合し真の未到達のみ選定。空状態系+4: 空 ChunkSourceMap（new/Default の len/is_empty/両方向 None・非空 len=5 対比）・記録ゼロ件 MapBuilderSink::finish→空マップ・空 SourceMap の resolve_* 三種全て対応なし・空チャンク insert_chunk 後も解決なし（ファイルキーのみ登録で range 系安全）。canonicalize+1: 冪等性（正規形再適用不変）・`@` 無し素通り・先頭 `@` 1個のみ除去（`@@x`→`@x`・途中 `@` 保持）・空文字列/`@` 単独の無パニック。STORE 側正規化+1: 非正規形（backslash）pasta_file で insert→正規形 query の逆引き/最近接一致＋前方解決の PastaPos::file は格納時生文字列保持を固定。サイドカー+5: read_sidecar 不存在→NotFound/JSON 破損→InvalidData の無パニック Err 経路初到達・to_chunk 重複 lua_line ペアの last-write-wins（8.1 不変条件が読込側でも成立）・version 未検証の前方互換読みを文書化テストで固定（将来 version ゲート導入時に本テスト更新と明記）・write_sidecar 親ディレクトリ自動作成（create_dir_all 分岐の正例初到達）＋空マップ往復同一・sidecar_path_for_lua の拡張子なし/多重拡張子でも付加（置換しない）。RED 確認: write_sidecar の create_dir_all を一時除去するミューテーションで creates_parents テストのみ fail（既存31件 pass＝既存網の盲点を実証）後復元。insert_chunk 同一チャンク名再投入で reverse 索引に旧エントリ残留（本番経路は loader が 1 chunk 1 回投入のため非到達・防御課題として申し送り）。clippy: source_map.rs 帰属は既存3件（745 nonminimal_bool/844・846 doc_overindented＝3.31 所掌）のみで新規 0。検証: cargo test -p pasta_lua 1226 passed/0 failed（1215＋11）・workspace 1840 passed/0 failed（1829＋11） |
| 3.31 | pasta_lua/debug-source_map | crates/pasta_lua/src/debug/source_map.rs, crates/pasta_lua/tests/debug/**（新設可） | G2+G3+G4+G5 | IMPROVED |  | G2: source_map.rs 帰属 clippy 3 件ゼロ化 — 745 nonminimal_bool（テスト内 `!is_some()`→`is_none()`）・844/846 doc_overindented（sample_source_map doc のリスト継続行 4→2 スペース）。--all-targets 再実行で source_map.rs 帰属 0 を新鮮確認（残 5 警告は全て wiring.rs＝3.33 所掌・境界外申し送り維持）。デッドコード: 全 pub 項目は pub mod debug→pub mod source_map 経由の公開 API 面（published crate）— lib 外部使用 0 件の lua_lines_for_pasta/chunk_name()/SIDECAR_VERSION/SidecarFile も公開 API 面につき維持。私的項目 canonicalize_pasta_file/map_forward_iter はモジュール内使用ありで真のデッドコードなし（コンパイラ dead_code 警告 0）。G3: パニック/脆弱性調査（本番部 ~580 行）— unwrap/expect/インデックス/加減算 0 件（262 strip_prefix の unwrap_or・419 unwrap_or_default の劣化系のみ・サイドカー I/O は全 Result 経路で panic/abort なし・FFI 境界なし・Mutex/RefCell 不使用）。3.30 申し送りの insert_chunk 同一チャンク名再投入 stale 逆引き残留を防御修正 — chunks.contains_key ゲートで再投入時のみ旧チャンクの逆引きエントリを全ファイルキーから retain 掃除（forward の HashMap 上書きと整合化。初回投入はゲート false の no-op＝loader は 1 chunk 1 回投入のため正常系挙動厳密保存・公開 API として外部再投入に備える）＋境界回帰テスト 1 件（RED 実証: 修正前 assert fail「left: [(other,50),(x,10)] right: [(other,50)]」で stale (x,10) 残留を実証 → 修正後 pass。同一/別 .pasta ファイルへの再投入・他チャンクエントリ温存・最近接調整の stale 行非返却を固定）。G4: karpathy 準拠検証 — 是正 1 件: insert_chunk の for ループ冗長再束縛（`for lua_line in ..` 直後の `let (lua_line, pasta_line) = lua_line`）をパターン直接分解 `for (lua_line, pasta_line) in ..` へ純粋等価変換（テスト安全網: insert_chunk 経由テスト多数）。是正なし判断（clean）: canonicalize_pasta_file 薄ラッパは STORE/QUERY 同一規則の意図表明 doc 付きで適正・map_forward_iter は 2 呼び出し箇所（insert_chunk/SidecarFile::from_chunk）共有で適正・insert_chunk の per_file 全値 sort は冪等でタッチ行限定の簿記追加の方が複雑化・サイドカー I/O は最小 Result 構成で過剰抽象なし。G5: 陳腐化 doc 6 箇所同期 — ChunkName「後続タスク（3.4）が用いる・本タスク（3.2）では型エイリアスのみ」・ChunkSourceMap「生成は後続タスク（3.3）の担当」・from_forward「本番パイプラインは後続タスク（3.3）」・MapBuilderSink.chunk_name フィールド「本タスク（3.3）では…登録（task 3.4）で」・SourceMap「Arc 化は後続タスク 4.x の担当」・insert_chunk「loader（task 4.3）が投入する想定」の未来時制を実装済み実態（同モジュール MapBuilderSink::finish／loader build_source_map の Arc<SourceMap> 共有）へ更新＋insert_chunk doc へ再投入置換セマンティクス節を追記（G3 修正の文書化）。README は境界外・確認のみ: debug/ 言及はディレクトリ構成 1 行（「DAP デバッグサポート・ソースマップ」）で drift なし。TEST_COVERAGE.md は境界外につき申し送り: pasta_lua 行 1215 のまま（3.30 の＋11・本セル＋1 が未反映 → 1227 への同期を後続セルへ）。依存整合: source_map.rs の use は serde/serde_json＋crate 内部（code_gen::source_map/normalize）＋dev の tempfile/pasta_dsl のみ・未使用依存なし。検証: cargo test -p pasta_lua 1227 passed/0 failed（1226＋1）・workspace 1841 passed/0 failed（1840＋1）・source_map.rs 帰属 clippy 0 |
| 3.32 | pasta_lua/debug-wiring | crates/pasta_lua/src/debug/wiring.rs, crates/pasta_lua/tests/debug/**（新設可） | G1 | IMPROVED |  | テスト13件追加（pasta_lua 1227→1240・wiring.rs 内 38→51・プロダクションコード変更なし・src 内 #[cfg(test)] 追記のみ＝3.21/3.23/3.25/3.27/3.30 と同判断で tests/debug/ 新設せず）。事前 grep で全テスト所在（wiring.rs 内 9 モジュール 38 件＝full E2E 2・source_map_wiring 3・resolver_attach 4・attach_source_presentation 3・bp_translator 7・pasta_bp/step/mode_edge/coalesce E2E 12・presentation_toggle 5、tests/runtime/runtime_toggle_e2e_test.rs 6・debug_integration_test.rs）を照合し真の未到達のみ選定。bp_translator_tests+2: 空行リスト再設定（DAP の全消去）が当該 present source の座標を権威的に全除去し空 resolved を返す（register 置換セマンティクスの空 entries 成立を翻訳経路越しに初固定）・translate_pasta_breakpoints 冒頭の map None 防御分岐初到達（`.lua` 直接登録へ安全劣化 — 要求順 verified ミラー＋present source=chunk 直接登録・無パニック）。bridge_lifecycle_tests 新設+11: run_event_encoder の 3 終端条件を初到達（イベントチャネル閉→stopped frame 転送後 clean 終了・out_rx 受信側喪失→ループ/パニックなしで終了・poisoned adapter→無パニック終了＋フレーム非送出）・drain_outbound の「closed out_rx は停止条件でない」文書化契約（true 返却＋待機フレームの順序保存 flush）・run_socket_bridge の preset shutdown 即時退出（先着 initialize 要求を未応答のまま EOF＝ループ先頭チェックの弁別）・クライアント切断時 pending outbound flush（Disconnected アーム初到達 — 半閉→inbound チャネル閉を bounded 待機で決定化し pending-1/2 受信→EOF を固定）・toggle の seq 欠落→request_seq 0 劣化（トグル自体は完全適用）・session 喪失×toggle→ack/event 送出後 false・session 喪失×stop-context（continue）→ack 送出後 false・unknown/missing command の沈黙無視（フレーム/コマンド非発行＋同一接続で後続 initialize 継続サービス）・poisoned adapter で attach_pasta_resolver 両ゲート（active/inactive）無パニック＋handle_inbound false。RED 確認: run_socket_bridge Disconnected アームの drain_outbound 呼出除去ミューテーションで socket_bridge_flushes_pending_outbound_on_client_disconnect のみ fail（50 passed/1 failed＝既存 38 件網の盲点を実証）後 cmp バイト同一復元。固定ポート不使用（全 bind 127.0.0.1:0）・sleep 同期なし（全待機はチャネル/ソケットの bounded blocking）・wiring スコープ 5 回連続実行 flake なし。clippy: 新テスト起因 single_match 2 件は if let 化で自己解消し新規警告 0（wiring.rs 既存 5 警告＝collapsible_if 364/needless_borrows 2261・4310・4570/doc_lazy 4479 は 3.33 所掌で不変・行番号は挿入でシフト）。検証: cargo test -p pasta_lua 1240 passed/0 failed（1227＋13）・workspace 1854 passed/0 failed（1841＋13） |
| 3.33 | pasta_lua/debug-wiring | crates/pasta_lua/src/debug/wiring.rs, crates/pasta_lua/tests/debug/**（新設可） | G2+G3 | IMPROVED |  | G2: wiring.rs 帰属 clippy 5 件ゼロ化 — 364 collapsible_if（本番 handle_inbound の応答送出 if 入れ子 → let チェーンへ機械等価変換・clippy --fix）・2261/4310/4570 needless_borrows_for_generic_args（テストの set_name(&format!(..)) → format! 直渡し・--fix）・4479 doc_lazy_continuation（テスト doc の lazy 継続行 → 空行挿入で独立段落化＝clippy help 第一案。「となり…」は両リスト項目に掛かる総括文のため項目内インデントより段落分離が意味的に正）。3.28/3.31 から申し送られた最後の保持者が解消し pasta_lua クレート全体 clippy --all-targets 警告 0 を新鮮確認。デッドコード: staged-wiring 期のブランケット #![allow(dead_code)]（行78）を除去 — 顕在化 2 件を処理: ①SourceMapWiring::disabled() は lib 呼出 0 件のテスト専用（本番 enable は struct literal 構築）→ pub(crate)＝非公開 API につき #[cfg(test)]＋理由コメントへ精密化（3.28 session.rs と同判断）②pasta_step_e2e の Expected struct 未読 6 フィールド（origin_lua/callee_first_lua/next_caller_lua/step_out_lua/recur_call_lua/after_recur_lua — derive() 内ローカルとして表明アサートに使用済みで struct 格納のみが冗長）を削除し doc へアンカー行番号を残置。公開 API 面: wiring.rs は全項目 pub(crate) 以下＝published API なし・公開 API 変更ゼロ。G3: パニック/脆弱性調査（本番部 行 1–600・全 10 テストモジュールの #[cfg(test)] ゲートを grep で確認）— unwrap/expect/panic!/unreachable!/インデックス/スライス/整数加減乗算/as 変換 0 件（unwrap_or 2 箇所＝command ""/seq 0 の非パニック防御既定のみ）。信頼境界（TCP→req: Value）: 全フィールドアクセスが get().and_then()＋既定値の防御形・SetBreakpoints の lines（Vec u32）へ演算なし（entries 蓄積はリクエストサイズ線形・フレーミング上限は transport 所掌）。ロック毒化: adapter.lock() 全 7 箇所処理済（attach_pasta_resolver の if let Ok 2＝毒化時は安全側既定 .lua リゾルバ維持・handle_inbound 4＝return false でブリッジ正常終了・run_event_encoder 1＝return。毒化経路の無パニックは 3.32 bridge_lifecycle_tests が実証済）。チャネル: transport.send/cmd_tx.send/out_tx.send 全 Result 処理・recv_timeout 全アーム処理（Disconnected→drain_outbound flush 後終了）。越境呼出先の panic-free 確認: SharedSourceMode（AtomicU8・from_u8 未知値→Pasta 既定で panic 不能）・Breakpoint::new（move のみ）・resolve_pasta_to_lua/nearest_pasta_line_with_mapping（3.31 確認済）・BreakpointSet（3.28 で poisoned fail-safe 確認済）・dap decode/encode（3.24 所掌）。FFI/SHIORI: wiring のスレッドはブリッジ専用（FFI 境界を直接越えない）＋到達可能パニック 0 → ハードニング対象なし（挙動変更ゼロのため境界回帰テスト不要・R3.3 非該当）。env 解析（PASTA_DEBUG/PASTA_DEBUG_PORT）は mod.rs 所掌＝境界外。申し送り: クレート広域の rustfmt 乖離（cargo fmt --check で pasta_lua 多数ファイル・既存・プロジェクトに fmt ゲートなし）は境界外。検証: cargo test -p pasta_lua 1240 passed/0 failed（ベースライン一致・テスト数不変）・workspace 1854 passed/0 failed（一致）・cargo clippy -p pasta_lua --all-targets 警告 0（クレート全体） |
| 3.34 | pasta_lua/debug-wiring | crates/pasta_lua/src/debug/wiring.rs, crates/pasta_lua/tests/debug/**（新設可） | G4+G5 | IMPROVED |  | G4: karpathy 準拠検証（本番部 行 1–599・安全網=in-module 51 テスト＋runtime_toggle_e2e/debug_integration）— 是正 1 件: attach_pasta_resolver の lock＋set_source_resolver 重複 2 サイト（Pasta 装着枝／Lua リセット枝）を resolver 値の先行構築＋単一 lock サイトへ純粋等価集約（resolver は副作用なしの Box クロージャ構築のみ・poisoned 時 no-op 含む全 4 分岐で可観測挙動同一・private fn＝公開面不変。安全網: resolver_attach 4＋attach_source_presentation 3＋presentation_toggle 5＋E2E 群が全分岐被覆）。是正なし判断（clean）: translate_pasta_breakpoints の (1)直接対応/(2)最近接調整の push ループ類似（2 行×2）は doc の 3 ケース番号構造と 1:1 対応・統合は resolve_pasta_to_lua 再計算か「nearest あり coords 空」縮退エッジの挙動変化リスクを伴い維持／handle_inbound の adapter.lock() match 4 箇所はブロック式でロックスコープを送信前に限定する意図的パターン（let-else 平坦化は guard 生存期間が延び encoder スレッドとのロック競合面＝重複でなくイディオム）／pasta/sourcePresentation 分岐のインライン処理は apply→ack→event→Refresh の順序厳守を関数抽出で崩さない設計判断（コメント文書化済）／run_event_encoder・drain_outbound・is_pasta_source は最小構成。テストコード簡素化は保守的に見送り: 6 E2E モジュールの DapClient/send_request/is_event/top_frame 等の自己完結重複（~4,900 行）は各モジュールがシナリオ固有 fixture を持つ独立読解設計で統合チャーン＞利益・G5: staged-wiring 期の陳腐化 doc 4 箇所同期 — ①SourceMapWiring struct doc「source_map は enable がマップを渡され**かつ** Pasta モードの時のみ Some（ゲート判断は enable 在住）」が mod.rs 実装（マップは供給時に常時スレッディング・モード半分のゲートは pasta_active() の消費時判断＝attach Lua→Pasta フリップ対応）と矛盾→実態へ更新＋「This task only DELIVERS…the resolver/translation LOGIC is 5.x」を実装済み consumer（attach_pasta_resolver/translate_pasta_breakpoints が同モジュール在住）へ②handle_inbound の source_map param コメント「will consult…no translation yet, the `.lua` path is unchanged」の虚偽（翻訳は同関数 setBreakpoints 分岐で実装済・行 417）→実態＋toggle/attach 分岐の読取も追記③run_socket_bridge param コメントの disabled() 言及（3.33 で #[cfg(test)] 化済みの test-only コンストラクタを本番入力のように記載）→「No map / Lua mode」へ④モジュール doc が実装済み 5.x consumer 群を一切記載せず→「.pasta presentation seam」節を新設（resolver/BP 翻訳/toggle の所在と pasta_active 消費時ゲートを集約・3.29 session.rs の Implemented scope 拡張と同型）。他 doc（pasta_active/source_mode field/translate_pasta_breakpoints/drain_outbound/run_event_encoder/topology 節）は実装と整合確認済。README は境界外・確認のみ: debug/ 言及はディレクトリ構成 1 行で drift なし。依存整合: wiring.rs の use は std/serde_json＋crate 内部のみ・未使用依存なし・公開 API なし（全項目 pub(crate) 以下）。申し送り: ①TEST_COVERAGE.md pasta_lua 行 1215 のまま陳腐化（実測 1240・3.30 の＋11/3.31 の＋1/3.32 の＋13 未反映 — 3.30–3.32 から継続）→後続セルまたは 3.64 で同期②debug/mod.rs enable() doc「This task wires the SKELETON only — the consumer LOGIC is tasks 5.x」（557 行付近）が実装済み実態と乖離 — mod.rs は境界外につき記録のみ。検証: cargo test -p pasta_lua 1240 passed/0 failed（ベースライン一致・テスト数不変）・cargo clippy -p pasta_lua --all-targets 警告 0（3.33 達成のクレート全体 0 を維持）・workspace 1854 passed/0 failed（一致）・スナップショット不変 |
| 3.35 | pasta_shiori | crates/pasta_shiori/** | G1 | IMPROVED |  | テスト28件追加（pasta_shiori 120→148・プロダクションコード変更なし・初の pasta_shiori セルにつきスコープベースライン 120 passed/0 failed を本セルで確立 — 内訳 lib 63/async_callback 12/lua_request 23/ontalk_probe 1/lifecycle 5/response 9/sample_ghost 2/test_env 5）。事前 grep で全テスト所在（src #[cfg(test)]＝shiori_tests.rs 18・error.rs 4・hglobal 1＋enc 4＋windows_api 13・req.rs 3・req_parser.rs 16、tests/*.rs 8 ファイル、他クレートからの被依存なし＝Cargo.toml 逆依存 0 件を確認）を照合し真の未到達のみ選定。windows.rs+10: モジュール全体が完全未テスト（テスト0件）→ RawShiori<T> が Shiori トレイトジェネリックである性質を利用し MockShiori で SSP ホスト不要のインプロセス検証 — load→request ラウンドトリップ（ANSI dir 復号パススルー/UTF-8 req 復号/出力 len 更新）・load 前 request→error_response 500 の厳密文字列契約（==NotInitialized.to_shiori_response()）・unload 冪等＋状態リセット（未 load でも true/unload 後 request 500）・Shiori Err→X-ERROR-REASON: Script error 伝搬・load Err→false＋スロット None 維持（後続 request 500）・load Ok(false) でもインスタンス格納＝後続 request 到達の特性化（PastaShiori 側の劣化応答へ委譲する実セマンティクスを文書化）・extern エントリの null/zero-len ガード（load null/len0 拒否・request null→null＋len0）・OnceLock 未初期化時の extern 3 関数安全劣化・DllMain DETACH→false/未知 reason→true（ATTACH は process-global OnceLock を汚染し並列テストの順序依存を生むため不実行とコメントで禁止明記・全テスト 127.0.0.1 等の固定資源なし）。shiori_tests.rs+5: call_lua_load の Ok(false)/Err 分岐初到達（SHIORI.load false→Ok(false)＋runtime 維持の特性化・error()→Ok(false)）・call_lua_request Err 分岐初到達（Lua error→Err(Script{msg 検証})・nil 返却→mlua 変換 Err(Script)）・runtime() None。error.rs+6: Display 未テスト変種（Load/Poison/EncodeAnsi/Script — X-ERROR-REASON に {self} 直埋めの可観測面）・From 変換 5 種初到達（Utf8Error→EncodeUtf8/PoisonError→Poison/mlua RuntimeError→Script/LoaderError ConfigNotFound→Load メッセージ厳密/pest ParseError→ParseRequest＋Display 接頭辞）。hglobal/mod.rs+2: to_utf8_str 不正 UTF-8（過長エンコード C0 AF）→Err(EncodeUtf8) エラーパス初到達・空ペイロード境界（len0/is_empty/空文字列往復 — Lua が "" を返す本番到達経路）。lua_request_test.rs+5: Reference 番号 i32 オーバーフロー（Reference99999999999）→Err(Script "Invalid reference number") エラーパス初到達・重複ヘッダキー last-wins（top-level/dic 両方 — src 内テスト専用 ShioriRequest の or_insert=first-wins と本番が相違する点を doc 明記）・bare-LF 行末許容（既存テストは全 CRLF）・key_other はトップレベル非汚染で dic のみ・空ヘッダ値（remain 零文字）許容。RED 確認: windows.rs error_response の to_shiori_response→to_shiori_400_response ミューテーションで新テスト4件のみ fail（82 passed/4 failed＝既存網の盲点実証）後 cmp バイト同一復元。意図的 from_utf8 不正リテラルの invalid_from_utf8 新規警告 1 件はランタイム構築 vec へ書換で自己解消 → 新規 clippy 警告 0（既存 12 件＝lua_request_test manual_range_contains 11＋async_callback manual char comparison 1 は 3.36 所掌として記録）。未テスト判断の記録: shiori2_ver の <0/>9 クランプ分岐は grammar ver=単一 ASCII_DIGIT につき公開 API 経由で到達不能・From<IndeterminateOffset> は外部構築不能・Encoding::ANSI(CP_ACP) の不正バイトテストはロケール依存につき不追加（既存 shiori_string_test の SJIS len 18 アサートも CP932 ロケール前提＝3.39 G5 へ申し送り）。申し送り（G1 範囲外・プロダクション未修正）: ①windows_api.rs の `len as _` i32 キャスト 4 箇所が 2GiB 超入力で負値ラップ（pasta_lua 3.11 encoding/windows.rs と同型欠陥）→3.37 G3 所掌②extern load の len==0 ガードと SHIORI 未初期化 request/load パスは HGLOBAL を解放せずリーク（ownership transfer 契約の例外系）→3.37 検討③Cargo.toml に publish キーなし＝既定 publishable・pub API（error/lua_request/PastaShiori/Shiori）は公開面として全維持。検証: cargo test -p pasta_shiori 148 passed/0 failed（120＋28）・cargo clippy -p pasta_shiori --all-targets 新規警告 0・workspace 1882 passed/0 failed（1854＋28） |
| 3.36 | pasta_shiori | crates/pasta_shiori/** | G2 | IMPROVED |  | G2-D4: クレート帰属 clippy 12 件ゼロ化（3.35 記録の既知状態と完全一致を着手時再確認）— manual_range_contains 11 件＝tests/lua_request_test.rs（年/月/日/yday/SHIORI ver の範囲アサート: 行 139–141・174・307–309・378・661–663）を `(a..=b).contains(&x)` へ・manual_pattern_char_comparison 1 件＝tests/async_callback_integration_test.rs:448 の `find(\|c\| c==','\|\|c==']')` を `find([',', ']'])` へ、全 12 件 `cargo clippy --fix` 機械適用（一次手段・手書きゼロ）。等価性: RangeInclusive::contains は同一比較の定義どおりの糖衣・char 配列パターンは同一 2 文字との一致判定で純粋等価、かつ全てテストコード内のアサート式＝プロダクションコード変更ゼロにつき外部観測挙動の変更余地なし（R3.1 厳密保存）。D5 デッドコード/未使用 pub 監査: Cargo.toml に publish キーなし＝既定 publishable（crates.io 公開面）→ 公開 API（pub mod error/lua_request・pub use PastaShiori/Shiori・MyError/MyResult/lua_date_from/lua_date/parse_request/to_shiori_response/to_shiori_400_response/runtime()）は公開 API 面につき維持 — ただし全項目とも src/tests 内に実使用ありを grep 実証（未使用 pub 自体が 0 件）。DLL ABI（#[unsafe(no_mangle)] extern "C" load/unload/request＋extern "system" DllMain＝SHIORI ホストの動的解決面）は不可侵・全維持。逆依存: workspace Cargo.toml の宣言のみで他クレートからの use pasta:: 0 件（3.35 記録と一致）。コンパイラ dead_code 警告 0（clippy --all-targets が rustc lint を包含）。#[allow(dead_code)] 全 2 箇所を個別監査: ①util/hglobal/mod.rs is_empty() の allow は 3.35 追加の empty_payload_roundtrip テストが使用するようになり陳腐化 → 除去（除去後 clippy 0 で stale を実証）②util/parsers/req_parser.rs ShioriRequestParser の allow は pest_derive 生成 Rule 変種（パーサー内部のみ使用）への正当な抑制で理由コメント付き → 維持。未使用依存: 全 8 依存（pasta_lua/time/tracing/thiserror/pest/pest_derive/windows-sys/tempfile）の使用箇所を grep 確認・未使用なし。申し送り: なし（12 警告は全て crates/pasta_shiori/** 帰属＝境界外指摘ゼロ）。検証: cargo test -p pasta_shiori 148 passed/0 failed（ベースライン一致・テスト数不変）・workspace 1882 passed/0 failed（一致）・cargo clippy -p pasta_shiori --all-targets 警告 0（12→0） |
| 3.37 | pasta_shiori | crates/pasta_shiori/** | G3 | IMPROVED |  | ハードニング 6 件（FFI/SHIORI 境界最優先・全件 RED 証拠付き）＋テスト 9 件追加（148→157）。①lua_request.rs parse1 の再帰を反復へ（D6）: ヘッダ 1 組ごとに 1 スタックフレームを消費する再帰で、ホスト制御のヘッダ数によりスタック枯渇→STATUS_STACK_OVERFLOW で SSP プロセスごと abort（catch 不能・SHIORI 境界越えクラッシュ）。RED: 256KiB スレッド＋Reference 50,000 件で 0xc00000fd を実証（tests/lua_request_test.rs stack_safety_tests）→反復化後 GREEN。②windows.rs RawShiori::{load,request,unload} へ catch_unwind(AssertUnwindSafe)＋panic_to_error 導入（R3.7）: FFI 境界を越えるパニックは歴史的 UB・Rust1.81 以降は即 abort（ホスト巻き添え）。SHIORI エラー契約へ縮退（load→false/request→500 X-ERROR-REASON: panic at SHIORI boundary/unload→true 維持）。パニックによる Mutex 毒化→以後 Poison 500 への縮退もテストで文書化。RED: mock panic 3 種（request 内/load 内/unload 時の Drop 内）が catch なしで全 panic 失敗→導入後 GREEN。release は workspace panic=abort につき catch 不到達（dev/test ビルドと rlib 利用者の保護）。AssertUnwindSafe の健全性根拠（状態は全て Mutex 背後・毒化で隔離）をコメント記録。③extern ガードパスの HGLOBAL 解放（3.35 申し送り②）: load len==0・load SHIORI 未初期化・request SHIORI 未初期化の 3 経路が ownership transfer 契約（callee が入力 HGLOBAL を解放）に反しリーク。戻り値挙動は不変＝解放のみ追加。RED: GlobalFlags==GMEM_INVALID_HANDLE プローブで must free アサート 2 件失敗を実証→修正後 GREEN、最終テスト形でも mutation（drop 除去）RED 再確認。テスト技法の知見: 解放済み GMEM_FIXED ポインタへの GlobalFlags は heap walk で STATUS_HEAP_CORRUPTION を誘発→プローブを GMEM_MOVEABLE ハンドル（ハンドルテーブル経由で安全に検証可能・ガードパスはデータ非参照のため忠実）へ変更。GMEM_INVALID_HANDLE は windows-sys 0.61 未収載のため SDK 値 0x8000 をローカル定義。④hglobal clone_from_slice_impl の GlobalAlloc null 未チェック（本セル新規発見・D3）: 旧 SAFETY コメントは「失敗時プロセス終了」と誤認していたが GlobalAlloc は失敗時 null を返し、null が from_raw_parts_mut へ流入する UB だった。alloc_global ヘルパー抽出＋null→Err(MyError::Script{message:"Global memory allocation error"})（公開 API 面につき変種追加不可・既存変種相乗り＝panic_to_error と同技法・診断価値はメッセージ文字列で保持）＋コンストラクタ群 MyResult 化（request_impl は ? 伝播・error_response はエラー応答すら確保不能時 (null,0)=応答なしへ縮退、null＋len>0 は決して返さない）。RED: mutation（null チェックバイパス）で Expected Err, got Ok(0x0) を実証＝巨大要求で GlobalAlloc が実際に null を返すことも同時確認。⑤windows_api.rs buffer_len_to_i32 ガード（3.35 申し送り①・pasta_lua 3.11 と同型）: `.len() as _` 4 箇所が 2GiB 超入力で負値ラップ（u32::MAX→-1=null-terminated 指定＝スライス外 OOB read）。production 経路 multi_byte_to_wide_char（load の ANSI 復号で host 制御 len に到達）＋cfg(test) の wide_char_to_multi_byte 両方をガード。RED: mutation（Ok(len as i32) へ巻き戻し）で rejects テスト失敗（i32::MAX+1→-2147483648 観測）。>2GiB 実バッファはテストで確保不能のためヘルパー単位でピン留め（手法も pasta_lua 3.11 踏襲）。⑥R2（再審是正）: ③と同欠陥クラスの残存 HGLOBAL リーク 3 経路を capture-before-lock 再順序化で一括解消 — request_impl の未初期化（load 前 request）early return・request_impl の毒化 Mutex early return（②の catch_unwind 導入が到達可能化した経路）・load_impl の毒化 Mutex early return（capture を T::default() より先行させたため default() 内パニック時の hdir リークも構造的に解消）。capture は入力 HGLOBAL の所有権取得のみで lock との順序入替にプロトコル可視の影響なし（正常系は既存 155 テストで保存確認）。RED: GMEM_MOVEABLE プローブの must free アサートで 3 テスト失敗（raw_request_not_initialized_frees_input・raw_load_poisoned_lock_returns_false_and_frees_input・raw_request_catches_panic_as_500_then_poison_500 の毒化経路プローブ追加分）を実証→capture 先行化後 GREEN。変更なし分類の記録: Mutex 毒化=From<PoisonError>→Poison→500/false で処理済（②で到達可能化しテスト固定）・OnceLock=DllMain get_or_init/extern get の正用法・GlobalLock=N/A（GMEM_FIXED 運用で不使用）・req.rs の unwrap/panic! 群=#[cfg(test)] 専用 ShioriRequest のみでプロダクション到達不能・request の len: &mut usize=ABI 凍結につき null 検査不能（参照型）でホスト契約として Safety doc 記録済み（host が len を偽る OOB は accepted risk）・MultiByteToWideChar 2 回目戻り値の &wstr[0..len]=API 契約で len≤バッファ・Rust グローバルアロケータ失敗=abort（unwind しない）につき FFI UB なし・DllMain ATTACH=RawShiori::new にパニック経路なし。挙動境界（R3.2/R3.3/R3.6）: 正常系厳密保存（既存 148 テスト全 GREEN 維持・変更なし）。変化は全て異常系: パニック時 abort→500/false/true・不正ガード経路のリーク→解放（戻り値不変）・>2GiB→Err・アロケ失敗 UB→Err/null 応答・大量ヘッダ abort→正常解析。公開面: DLL ABI（no_mangle load/unload/request/DllMain）凍結維持・pub fn シグネチャ変更なし・MyError 変種追加なし（R2 是正: 初版が追加した GlobalAlloc 変種は、pasta_shiori が crates.io 公開済み（26 版・既定 0.2.2=ローカル版）かつ MyError が non_exhaustive なし pub enum のため、変種追加＝下流の網羅 match 破壊＝semver-major で R3.5 違反と判定し削除。初版の「逆依存 0 なので変種追加可」根拠は公開クレートには不成立で誤り。non_exhaustive 付与もそれ自体破壊的変更のため不採用＝既存 Script 変種相乗りを採択）。検証: cargo test -p pasta_shiori 157 passed/0 failed（148+9: lib 86→94=panic 3/alloc_global 1/buffer_len 2/R2 リーク回帰 2・lua_request_test 28→29=stack 1）・cargo clippy -p pasta_shiori --all-targets 0 警告維持（3.36 達成の継続）・workspace 1891 passed/0 failed（1882+9）。申し送り: ①shiori.rs→pasta_lua の呼出網は catch_unwind で覆ったが pasta_lua 側のパニック自由証明は未達（pasta_lua 各セル所掌）②ガードパスの drop(ShioriString::capture(..)) 3 連は 3.38 G4 簡素化で集約検討可③テスト専用 req.rs ShioriRequest の parse1 も再帰だが test-only・有界入力につき未変更（G4 で本番 parse1 との重複自体の解消を検討可） |
| 3.38 | pasta_shiori | crates/pasta_shiori/** | G4 | IMPROVED |  | G4: karpathy 準拠検証（本番部 src/** 約1,200行・安全網=3.35〜3.37 で確立した 157 テスト）— 是正4件＋fmt 正規化（全て純粋等価変換・RED 不要＝既存網で等価検証・公開 API/DLL ABI 不変）。①shiori.rs cache_lua_functions の 3 連同型 match（load/request/unload×各11行・差分は関数名と miss 時ログレベルのみ）を private ヘルパー lookup_lua_fn(table,name,optional) へ等価集約（-26行）: ログ文言・レベルは逐語保存（trace「SHIORI.{name} function cached」/warn「not found」/unload のみ debug「(optional)」を optional フラグで再現・format 引数化はレンダリング同一）。Err アームの 3 フィールド None 代入は既存 clear_cached_lua_functions() 再利用へ。安全網: shiori_tests が load_fn/request_fn/unload_fn の Some/None 状態を直接ピン（test_cached_functions_cleared_on_reload・test_multiple_instances_independent_caches・SHIORI テーブル欠如時 None 群）。②lua_request.rs parse1 の loop{match it.next(){Some=>束縛,None=>return Ok(())}} を while let Some(pair)=it.next() 慣用形へ（末尾 Ok(()) へ等価移動・3.37 の再帰→反復変換時の残骸形を是正。it は本体で parse_key_value(&mut it) に使用するため for 化は不可＝while-let が終端形・clippy while_let_on_iterator も非発火を確認）。③lua_request.rs 行1 の #![allow(clippy::trivially_copy_pass_by_ref)] 除去: 当該 lint は pedantic 群で workspace/クレートに [lints] 設定なし＝allow-by-default につき発火不能の死んだ抑制（除去後 clippy 0 維持で stale 実証・3.36 G2 の残漏れ）。④windows.rs unload() の match{Ok(_)=>(),Err(e)=>log}; を if let Err(e) へ（パターン網羅同一・-3行）。⑤cargo fmt 正規化: pre-existing drift 6 箇所（shiori.rs 余分空行/windows_api.rs multi_byte_to_wide_char シグネチャ折返し/req.rs import 順/shiori_tests.rs assert 折返し×2/tests の test_env.rs・lua_request_test.rs 折返し各1）— 機械整形のみ・トークン列不変。是正なし判断（clean）: 3.37 申し送り②の drop(ShioriString::capture(..)) 3 サイト集約は評価の結果見送り — 各サイト 1 行＋所有権コメント付きで、ヘルパー化は行数削減ゼロ（4行ヘルパー＋3呼出 vs 現3行）の純間接化＝karpathy「単一用途の抽象禁止」逸脱・イディオムとして一貫使用につき維持／3.37 申し送り③の req.rs テスト専用 ShioriRequest parse1 と本番 parse1 の重複解消も見送り — 別目的（文法レベル特性化を Lua 非依存で検証する独立 fixture）で、本番 parse_request への統合は文法層カバレッジの独立性喪失＋テストチャーンのみ・再帰は有界テスト入力（固定3ファイル）につき安全＝テストコード保守的方針／windows.rs RawShiori の load/request/unload 3 ディスパッチの catch_unwind 構造類似は戻り値型とエラー縮退（false/500/true）が各々異なり統合はクロージャ間接化のみ＝維持／windows_api.rs の uninit Vec+set_len 2 連は片方 #[cfg(test)] 専用で統合は cfg 複雑化＋unsafe 不変条件が API ごとに相違（exactly len vs up to len）＝維持／shiori.rs load() の reload 分岐・2 段ロガー初期化・request() の last_load_error 縮退・Drop 順序は全行意図コメント付き最小構成／error.rs（thiserror 慣用形・From 群最小）・hglobal/mod.rs（has_free 設計最小・alloc_global は 3.37 抽出済）・enc.rs・req_parser.rs（本番部11行）・lib.rs/mod 群＝過剰抽象なし。公開面: pub API（error/lua_request/PastaShiori/Shiori）シグネチャ不変・DLL ABI（no_mangle 4 関数）凍結維持・変更は全て private fn/モジュール内部。検証: cargo test -p pasta_shiori 157 passed/0 failed（ベースライン一致・テスト数不変）・cargo clippy -p pasta_shiori --all-targets 警告0（維持）・cargo test --workspace 1891 passed/0 failed（一致）・cargo fmt --check -p pasta_shiori クリーン化。申し送り: fmt drift が pasta_shiori に 6 箇所蓄積していた — 他領域も cargo fmt --check 不通過の可能性があり後続セルまたは 3.64 で確認推奨 |
| 3.39 | pasta_shiori | crates/pasta_shiori/** | G5 | IMPROVED |  | G5: 領域ドキュメント同期＋依存整合＋テスト衛生（プロダクションコード変更ゼロ）。①README.md 依存表 drift 是正: pasta_core 行を削除（Cargo.toml に不存在・src/tests 内 use 0 件を grep 実証＝実体なき記載）・pest 2.8/pest_derive 2.8 行を追加（req_parser.pest 文法の実依存が未記載だった）・windows-sys 0.59→0.61（workspace 実バージョンへ）・dev-dependencies 節新設（tempfile 3）・「バージョンは workspace 一元管理」注記追加。②README.md アーキ図/構成同期: 存在しない「shiori32 エントリポイント」→実エクスポート load/unload/request＋DllMain（windows.rs）へ・「LuaRequest」→実モジュール名 lua_request（SHIORI リクエスト→Lua テーブル変換）へ・util/ 内訳（hglobal/=HGLOBAL 文字列・エンコーディング、parsers/=pest パーサー）を追記。③README.md 公開 API 同期: Shiori トレイト snippet の戻り値 Result→MyResult（実シグネチャ）＋MyResult 注記・PastaShiori メソッド表へ runtime() 追加。④README.md「FFI 境界の安全性」節新設: 3.37 で実装済みの catch_unwind パニック縮退契約（load→false/request→500+X-ERROR-REASON/unload→true）・入力 HGLOBAL のガードパス含む全経路解放・GlobalAlloc 失敗の明示エラー化・ヘッダパース反復化（スタック枯渇なし）を文書化（実装済み挙動の記載のみ＝仕様変更なし）。⑤クレート内 doc 監査: lib.rs/util/mod.rs/hglobal/parsers のモジュール doc に陳腐化なし・util/shiori.md は外部 SHIORI 仕様の転記資料で drift 対象外と判断。⑥TEST_COVERAGE.md pasta_shiori 行同期: サマリー行 47→157（3.35/3.37 の追加 120→157 を注記）・DoD 行「全パス」→157 テスト・§2.6 lua_request_test 行 23+→29。⑦TEST_COVERAGE.md pasta_lua 行 1215→1240 同期（3.30〜3.34 申し送りの消化: 3.30 source_map+11・3.31 stale 残留是正回帰+1・3.32 wiring+13、DoD 行＋サマリー行・注記へ追記）。⑧依存整合: 全 8 依存（pasta_lua/time/tracing/thiserror/pest/pest_derive/windows-sys/tempfile）の使用を grep 再実証（3.36 と一致・未使用 0）・全依存 workspace 継承でバージョンドリフトなし。cargo-audit/cargo-deny はリポジトリ横断 D7 セル（TEST_COVERAGE.md/deny.toml 境界セル）の所掌につき本セルでは領域スコープ分のみ実施。⑨3.35 申し送り消化（テスト衛生）: hglobal/mod.rs shiori_string_test の SJIS ブロックが CP932 ACP 前提（非 932 環境では Encoding::ANSI.to_bytes が変換不能 Err→unwrap panic でテスト失敗）→ GetACP()==932 ゲートで環境非依存化（932 環境では従来アサート全件を逐語維持・スキップ時は eprintln 記録・テスト数不変 157）。⑩3.36 台帳補正（自行記録・3.36 行は不変更）: 3.36 Notes の「#[allow(dead_code)] 全 2 箇所を個別監査」は src 帰属 2 箇所（hglobal is_empty・req_parser.rs）の監査としては正確だが、クレート全体では tests/common/mod.rs:3 の inner #![allow(dead_code)] を含む計 3 箇所が存在した。後者は共有テストヘルパーの標準イディオムにつき維持が正当（本セルで監査・維持確認）。現存 dead_code allow は src 1（req_parser.rs 理由コメント付き・正当）＋tests 1 の計 2。申し送り: TEST_COVERAGE.md ヘッダー（最終更新 2026-05-29・総テスト数 1050+）と §5 合計行は領域横断につき本セル境界外＝D7 横断セルで同期推奨。検証: cargo test -p pasta_shiori 157/0（94 lib＋12 async_callback＋29 lua_request＋1 ontalk_probe＋5 lifecycle＋9 response＋2 sample_ghost＋5 test_env）・cargo test -p pasta_lua 1240/0（台帳同期の新鮮根拠）・workspace 1891/0・cargo clippy -p pasta_shiori --all-targets 0 警告・cargo fmt -p pasta_shiori --check 差分なし |
| 3.40 | pasta_lsp | crates/pasta_lsp/** | G1 | IMPROVED |  | テスト18件追加（pasta_lsp 92→110・プロダクションコード変更なし・初の pasta_lsp セルにつきスコープベースライン 92 passed/0 failed を本セルで確立 — 内訳 lib 16〔document 8/transport 8〕＋tests/ 11 ファイル 76〔analysis 12/crash_recovery 4/cue_command 10/diagnostics 6/document_sync 4/fullwidth_halfwidth 5/japanese_identifier 5/lsp_lifecycle 4/partial_token 5/semantic_token 9/utf16_conversion 12〕）。事前 grep で全テスト所在（src #[cfg(test)]＝document.rs・transport.rs のみ、tests/*.rs 11 ファイル、他クレートからの被依存 0 件＝workspace Cargo.toml 逆依存 grep で確認・Cargo.toml publish=true＝crates.io 公開クレート・crate-type cdylib+rlib/wasm32 向け）を照合し真の未到達のみ選定。tests/var_set_token_test.rs+10: 変数代入行（var_set）のトークン生成が完全未テスト（tests/・src 内に ＄/$ を含むテスト 0 件＝visitors.rs visit_var_set〜tokenize_expr_recursive 約340行と text_utils 式スキャンヘルパー5関数が未到達）→ ローカル代入 ＄好感度＝１００ のマーカー/変数名(definition mod=2)/＝ OPERATOR/数値 NUMBER の位置・長さ厳密固定・グローバル ＄＊ マーカー len2＋変数名 definition|global mods=6・var_set_none（＄＝１）は名前トークン非生成・二項式 RHS（NUMBER OPERATOR NUMBER）・単語参照 RHS ＠挨拶→WORD・変数参照 RHS ＄ｘ→VARIABLE mods0・文字列 RHS は中身のみ TALK・関数呼出 RHS ＠乱数（１、２）の WORD＋括弧 OPERATOR×2＋引数 NUMBER×2・括弧式 RHS の特性化（後述上流バグ込みの現挙動固定・上流修正時の期待値更新手順をテスト内 doc 化）・アクター辞書スコープ経由（visit_actor_scope→visit_var_set 経路）。tests/analysis_test.rs+3: scan_comment_lines が無アサート（既存 CRLF テストは no-panic のみ）→ 全角＃行の COMMENT トークン（行0 col0 len5）・半角#＋先行空白（行全長 len11・col0 開始）・末尾改行なし入力（VSCode 経路・Cow::Owned 分岐）が改行あり入力とトークン列完全一致。src/error.rs+2: LangServerError 全 3 変種の Display 文言契約（lib.rs 再公開の公開 API・テスト 0 件だった）＋std::error::Error 実装と source None。src/transport.rs+1: WasmDiagnostic severity 変換の未到達 3 分岐（INFORMATION→3/HINT→4/None→1 既定・既存は ERROR/WARNING のみ）。src/document.rs+2: didChange 複数 contentChanges の逐次適用（2 番目の range が 1 番目適用後テキスト基準で解釈される LSP セマンティクス — 元テキスト基準だと結果が異なる入力で固定）・line_col_to_offset の UTF-16 col がサロゲートペア内部を指す場合 None（補間しない）。RED 確認: visitors.rs:225 tokenize_var_set_text の変数名 modifiers (1<<1)|global_mod→0 ミューテーションで新テスト 3 件のみ fail（test_var_set_local_number_tokens/global_marker_and_modifiers/in_actor_scope・既存 13 スイート全 green＝既存網の盲点実証）後 cmp バイト同一復元。上流バグ再確認（プロダクション未修正・挙動保存）: pasta_dsl parse_action.rs:354 paren_expr 分岐が括弧内の最初の term のみ AST 化 — ＄ｗ＝（１＋２）＊３ の AST が Paren(Integer(1))*3 となり ＋２ が無音欠落（3.3 既知バグと同一・3.4/3.5 で挙動保存維持済を LSP 側トークン出力からも実証）→ LSP 側は AST へ忠実のため括弧内 ＋/２ トークン非生成が現仕様・特性化テストで固定し上流修正時に期待値更新。未テスト判断の記録: server.rs の LanguageServer trait ハンドラ（initialize/did_open/did_change/did_close/semantic_tokens_full）は tower-lsp Client 実体が必要（トランスポート起動は非決定性）・コアロジック（DocumentManager＋AnalysisEngine 連携・キャッシュ・全文置換）は既存 lsp_lifecycle/document_sync 統合テストで被覆済につき不追加。transport.rs wasm モジュールは cfg(target_arch=wasm32) 限定で native テスト不能（変換ロジック WasmAnalysisResult は既存＋本セルで被覆）。検証: cargo test -p pasta_lsp 110 passed/0 failed（92＋18）・cargo clippy -p pasta_lsp --all-targets 警告 0（既存 0・新規 0）・workspace 1909 passed/0 failed（1891＋18）・本セル追加分は rustfmt クリーン。申し送り（3.41 G2-G5 所掌）: ①既存 rustfmt ドリフト 10 hunks（src/server.rs:98・src/transport.rs:91・tests/analysis_test.rs:3・crash_recovery_test.rs:24,65・japanese_identifier_test.rs:8,16,24・partial_token_test.rs:22,31 — HEAD 時点比較で本セル起因でないことを実証済）②transport.rs native モジュールは空スタブ（Reserved コメントのみ）③error.rs LangServerError はクレート内で未使用（doc コメントに downstream 向け公開 API と明記済・publish=true につき維持が妥当か 3.41 G2 で再点検） |
| 3.41 | pasta_lsp | crates/pasta_lsp/** | G2+G3+G4+G5 | IMPROVED |  | G2: clippy --all-targets 警告 0 を新鮮再確認（3.40 達成の継続）。既知 rustfmt ドリフト 10 hunks（server.rs:98・transport.rs:91・analysis_test.rs:3・crash_recovery 24/65・japanese_identifier 8/16/24・partial_token 22/31）を cargo fmt -p pasta_lsp で正規化 → fmt --check 差分 0。デッドコード/未使用 pub: #[allow] 0 件・workspace 逆依存 0（3.40 確認と一致）。workspace 内未消費の pub（LangServerError・WasmAnalysisResult::from_analysis・analysis 再公開群）は crates.io 公開クレート（publish=true）の公開 API 面につき維持。3.40 申し送り③へ回答: error.rs LangServerError は downstream 向け doc 注記済み＋Display 契約テスト固定済みにつき維持が妥当と判断。申し送り②の transport.rs native 空スタブは将来 stdio 拡張の明示予約コメント付きにつき維持・G3: 信頼できない入力面 2 系統を監査（LSP はエディタ由来テキスト/位置を受信）— ①DocumentManager::change のハードニング: LSP 契約違反の逆転 range（start>end）で String::replace_range がスライス境界パニック（RED: 一時 probe テスト probe_document_change_inverted_range が panicked『slice index starts at 9 but ends at 2』で fail を実証）→ start<=end ガードで no-op 化（既存の範囲外 no-op セマンティクスと整合・正常系不変）＋境界回帰テスト test_incremental_change_inverted_range_is_noop（document.rs）追加（R3.2/R3.3/R3.6）。②AnalysisEngine::analyze: 敵対的入力コーパス 40 種（裸マーカー 8 種・var_set_none・閉じない括弧/文字列・末尾演算子・全角空白/タブ字下げ・$＊全半角混在マーカー・絵文字識別子/アクター・プロパティ/グローバル/単語参照 RHS・CRLF/孤立 CR/BOM/NUL/改行なし末尾・深さ 50 括弧・全角二項演算子・コードブロック・1 万字行）で panic 0 を実証 → tests/analyze_robustness_test.rs として恒久化（no-panic 契約の固定）。正当化して維持: utf8_offset_to_utf16 のスライスはパーサー由来 char 境界オフセットのみ到達・encode_tokens の減算は sort 後単調性で安全・visitors の (line-1) は pest 1-based 行＋is_valid ガードで 0 非到達・mod.rs の e.line-1 は >0 ガード済・server.rs は RwLock 毒化 let-else 縮退＋catch_unwind 済・wasm_analyze は catch_unwind＋unwrap_or(NULL) 済・行カウンタ u32 cast は >42 億行のみ＝パーサー防御済み不変条件。unwrap/expect/panic! はプロダクション src 0 件（全て #[cfg(test)] 内）・G4: 等価簡素化 3 件（安全網=112 テスト・トークン出力は semantic/var_set/cue_command 各テストが位置/長さ/型を厳密固定）— ①visit_global_scene/visit_local_scene/visit_actor_scope のマーカー行トークン生成 3 連重複（約 12 行×3）を marker_line_token ヘルパーへ集約（global/actor の line_len>start_char ガードは length>0 判定へ代数的等価変換・local の無条件 push は逐語維持）②visit_var_set/visit_cue_command の span 行ウィンドウ抽出重複を span_line_window へ集約（start 非クランプ・end クランプの現挙動を逐語保存）③括弧 OPERATOR トークン push の 6 箇所重複（tokenize_expr_recursive Paren 開/閉・tokenize_args_text 開/閉・visit_cue_command 開/閉）を push_char_op_token へ集約。visitors.rs 922→908 行・G5: ①Cargo.toml 依存衛生: serde_json が [dependencies] 宣言だが使用は #[cfg(test)] のみ（transport.rs テスト 2 箇所を grep 実証）→ [dev-dependencies] へ移動（downstream 依存フットプリント削減・公開 API 非影響・build GREEN 確認）。残依存（pasta_dsl/thiserror/serde/tower-lsp＋wasm32: wasm-bindgen/serde-wasm-bindgen）は全て使用確認・全 workspace 継承でドリフトなし。②README ドリフト是正: テスト節 88→112 全面再集計（var_set/analysis 15/robustness/document 11/error 2 行を実数同期）・実体のない wasm-bindgen-futures 記載を削除・トークン表 string→talk（実トークン名 SemanticTokenType::new("talk") へ）・operator 説明へ代入＝/括弧/二項演算子を追記（3.40 var_set トークン実装の反映）・TransportBridge（実体なし）→Transport (transport.rs) 実名化・serde_json を dev 専用と明記・workspace 一元管理注記追加。③lib.rs/モジュール doc 監査: 陳腐化なし。④TEST_COVERAGE.md pasta_lsp 行同期: §2.5 analysis_test 12→15・var_set 10/robustness 1/document インライン 11/error インライン 2 行追加・§2.7 transport 7→9・§5 サマリ 79→112（3.40+18/3.41+2 注記）。検証: cargo test -p pasta_lsp 112 passed/0 failed（ベースライン 110＋回帰 2）・cargo clippy -p pasta_lsp --all-targets 0 警告・cargo fmt -p pasta_lsp --check 差分 0・cargo test --workspace 1911 passed/0 failed（1909＋2）。申し送り: TEST_COVERAGE.md §5 合計行（1050+）とヘッダー総数は領域横断につき D7 横断セル（3.64）所掌・pasta-vscode 行 79 は本セル境界外 |
| 3.42 | pasta_check | crates/pasta_check/** | G1 | IMPROVED |  | テスト32件追加（31→63・プロダクションコード変更なし）。tests/cli_test.rs 新設+6: main() の終了コード/stdout/stderr が完全未テスト → --version/--help の exit 0 と出力契約・引数なし exit 1＋Usage・未知サブコマンド exit 1・release I/O エラー exit 1（Usage 非表示）・バイナリ経由 release E2E（オーバーレイ込み成果物実在＋target 無変更）。main.rs+7: 短縮フラグ -h/-V・release 指定中の --help 優先（早期 return）・余剰位置引数拒否・未知オプション拒否・--target 値欠落・オプション先行順序許容の文書化。copy.rs+3: 存在しない src のエラー伝播・空 src（count 0＋dst 作成）・release パスが既存ファイル時の remove_dir_all エラー＋入力非破壊。nar.rs+4: 空ディレクトリ→0エントリ正当 ZIP・存在しない release dir エラー・ZIP エントリ内容のバイト忠実性（往復一致）・「profile」名の通常ファイルは除外されない境界文書化（除外はディレクトリ限定）。update_files.rs+8: days_to_ymd うるう年分岐（2024-02-29/2000-02-29 400年規則/2100-03-01 100年規則非うるう/2023-12-31T23:59:59 年末境界）・対象0件の早期 return（updates.txt 非生成）・存在しない root→Ok(0) フォールバック・var 除外・developer_options.txt 除外・エントリ辞書順ソート（pkg.txt と pkg/inner.txt の '.'<'/' 境界で走査順と区別可能化＝sort 除去ミューテーションで fail することを実証）・空ファイル MD5 既知ベクトル・8192 バイト境界マルチチャンク MD5 の一括計算一致。release.rs+4: 複数オーバーレイ後勝ち・stale release dir 掃除・NAR への updates.txt 封入（生成→NAR の順序契約）・存在しない target エラー伝播。ミューテーション2件で red 確認後復元（var 除外解除→excludes_var_dir FAILED／sort 除去→sorted_by_path FAILED）。不要テスト削除なし。clippy -p pasta_check --all-targets 警告0・rustfmt 適用済。検証: cargo test -p pasta_check 63 passed/0 failed・workspace 1943 passed/0 failed |
| 3.43 | pasta_check | crates/pasta_check/** | G2+G3+G4+G5 | IMPROVED |  | G2: clippy -p pasta_check --all-targets 警告0（変更前後とも新鮮実行で確認）・rustfmt 適用済。デッドコード/未使用 pub なし（lib なしの bin クレートのため pub API 面の制約は実質適用外・rustc dead_code 警告0）。依存3件（lexopt/md5/zip）＋dev 1件（tempfile）全て使用確認・未使用依存なし・G3: 3.42 申し送り①を許容ハードニング — nar.rs の ZIP エントリ「..」検査が debug_assert! のみで release ビルド無効（cargo test --release で "../evil.txt" が Ok 素通りすることを RED 実証）→ ensure_no_parent_component ヘルパー（実行時 InvalidInput エラー・debug/release 常時有効）へ置換・境界回帰テスト2件追加（test_zip_entry_name_rejects_parent_component: ".."コンポーネント4種拒否／test_zip_entry_name_accepts_normal_paths: "..foo"・"a..b" 等の部分文字列一致は受理＝拒否は厳密にコンポーネント単位）。正常系不変（read_dir 由来パスに ".." は構造的に非発生＝防御層のみの強化）。3.42 申し送り②（target 直下の除外ファイルが NAR に封入される）を再評価 → updates.txt は Step 4 で常に再生成され封入は仕様（test_execute_release_nar_contains_updates_txt が順序契約を固定済）・updates2.dau/developer_options.txt の NAR 封入は正常入力に対する観測挙動につき変更は R3.1 違反＝挙動保存により維持（実害なしの 3.42 判断を追認）。その他棚卸し: プロダクションコードに unwrap/expect/panic/添字アクセス0件（grep 全数調査 — unwrap 系は全て #[cfg(test)] 内・unwrap_or_default/unwrap_or(UNIX_EPOCH) フォールバック2件は安全）・copy.rs は ensure_within＋シンボリックリンクスキップ防御済・CLI 終了コード契約（3.42 追加の cli_test.rs 6件）不変・G4: karpathy 準拠検証 — 5ファイル・線形5ステップパイプライン・重複ブロックなし・過剰抽象なし → 是正0件。days_to_ymd 手書き暦変換は依存追加回避の意図的選択かつ 3.42 でうるう年分岐テスト済につき維持。nar.rs（profile ディレクトリのみ除外）と update_files.rs（profile+var＋ファイル3種除外）の除外規則非対称は仕様（NAR は var/ を封入・updates.txt は除外）につき統合せず・G5: README.md 新設 — 7クレート中唯一 README 不在（publish=true・crates.io 公開済）→ CLI 使用法/5ステップフロー/updates.txt Version 3 形式/NAR 封入規則（profile 除外・symlink スキップ・トラバーサル検査）/ソース構成/依存表を実装と整合する内容で作成。Cargo.toml description は実態と一致確認。申し送り: TEST_COVERAGE.md に pasta_check 行が完全不在（65テストが台帳未記載）— TEST_COVERAGE.md は境界外につき横断 D7 セル（3.64）所掌へ。検証: cargo test -p pasta_check 65 passed/0 failed（63＋回帰2・--release でも 65/0）・clippy --all-targets 警告0・workspace 1945 passed/0 failed（ベースライン1943＋2） |
| 3.44 | pasta_sample_ghost | crates/pasta_sample_ghost/** | G1 | IMPROVED |  | テスト13件追加（スコープ 27→40・プロダクションコード変更なし）。初の pasta_sample_ghost セルにつきスコープベースライン 27 passed/0 failed（lib 13＋bin 0※unix限定1件はWindowsで0＋dist 1＋integration 10＋self_deploy 3）を本セルで確立。事前点検: src 4 ファイル＋tests 3 ファイルの公開挙動を既存テストと突合し未到達を選定。main.rs+4: バイナリ CLI 契約が完全未テスト — get_output_dir の最初の非フラグ引数採用/フラグスキップ/デフォルト ghosts/hello-pasta フォールバック（2件）・walkdir の基本カウント（既存テストは #[cfg(unix)] symlink のみ＝Windows 開発環境でゼロカバレッジだった）ネスト3ファイル計数＋不在/空ディレクトリ0（2件）。config_templates.rs+2: charset ヘッダ先頭固定・サーフェス定義ブロックの完全検証（0-8/10-18 の各IDが同一ID png を参照する逐語ブロック・ブロック数ちょうど18・欠番 surface9/範囲外 surface19 非定義 — 画像IDとの整合保証というモジュール存在目的の固定）。image_generator.rs+4: Expression オフセットの 0..=8 全単射（重複オフセット＝サーフェス相互上書き退行の検知・既存は3値のみ）・9表情の描画相互相違（draw_expression の match 腕が潰れる退行検知 — 既存テストでは腕削除が無検出だった）・四隅透明＋頭部キャラクター色（sakura赤/kero青・背景非塗装契約）・generate_surfaces の保存18 PNG デコード可能性＋128x256 寸法＋欠番 surface9.png 非生成（統合テストの存在チェックより踏込み）。lib.rs+3: GhostConfig::default の配布メタデータ残フィールド（version/craftman/craftman_w/homeurl — descript.txt と整合すべき値）・generate_ghost の surfaces.txt 内容がテンプレート出力と完全一致・既存出力への再実行冪等性（リリース再生成シナリオ・19ファイル不変）。ミューテーション3件で RED 実証後復元: ①Angry offset 8→7（全単射テスト FAILED）②kero 範囲 10..=18→9..=18（ブロック数テスト FAILED）③get_output_dir フラグスキップ除去（CLI テスト2件 FAILED）。不要テスト削除なし。申し送り（3.45 G2-G5 所掌）: ①generate_ghost の _config 引数は完全未使用（API互換性コメント付き）・GhostError::ConfigError 変種はプロダクションで未構築 — publish=false クレートにつき G2/G4 で除去検討可②release.ps1/RELEASE.md/build.rs の文書整合は G5 で点検。検証: cargo test -p pasta_sample_ghost 40 passed/0 failed（27＋13）・workspace 1958 passed/0 failed（ベースライン1945＋13） |
| 3.45 | pasta_sample_ghost | crates/pasta_sample_ghost/** | G2+G3+G4+G5 | IMPROVED |  | G2(D4): clippy 2件（collapsible_if×2・scripts.rs テスト fn）を cargo clippy --fix 一次手段で解消→ clippy --all-targets 警告0。G2(D5): publish=false を Cargo.toml で確認の上、リポジトリ全域で未構築の GhostError::ConfigError 変種と generate_ghost の完全未使用 _config 引数を除去。引数除去で全プロダクション消費者を失った GhostConfig 構造体（フィールドは自テストのみ参照の定数台帳）も未使用 pub として除去 — 随伴テスト2件削除（test_default_config / default_config_release_metadata。R2.3 根拠: 被テスト対象の構造体削除に伴う随伴削除であり、descript.txt の実配布メタデータは integration_test::test_ukadoc_files が craftman/craftmanw/homeurl/sakura.name 等を独立検証済みのため網羅低下なし）。呼出側更新は main.rs/lib テスト/integration_test.rs/README 例と境界内で完結。G3(D3+D6): NO_CHANGE — 棚卸し証拠: src 全 unwrap/expect/panic は #[cfg(test)] 内のみ・プロダクション0件（main.rs:76 は unwrap_or_else フォールバックで非パニック・put_pixel_checked は境界ガード済）。build.rs の expect 2件は cargo 保証の CARGO_MANIFEST_DIR 由来でビルド時専用（FFI境界なし・実行時到達不能）につき妥当と判断し維持。攻撃面: ローカル生成 CLI・argv の出力先パスは意図された入力・walkdir symlink スキップ済（既存ハードニング）・ネットワーク/デシリアライズ/unsafe なし → 許容ハードニング適用0件（挙動変化なしのため回帰テスト不要）。G4(D2): karpathy 準拠是正4件 — main.rs: count_files（walkdir への単純委譲ラッパー）インライン化・image_generator.rs: generate_surfaces の sakura/kero 同型2ループを (character, base) ペアの1ループへ統合・draw_body の match 2アーム（ratio 方向のみ相違の重複描画ループ 38→22行）を係数選択 match へ統合・config_templates.rs: generate_surfaces_txt の同型2ループを (0..=8).chain(10..=18) へ統合。等価性証明: 簡素化後の再生成出力（surface*.png 18＋surfaces.txt）が git 管理フィクスチャ ghosts/hello-pasta/shell/master と cmp 全19ファイルバイト一致＝画像出力不変。G5(D7): 実在しない release.bat への参照を是正（README ディレクトリツリー行・「ダブルクリック」記述・RELEASE.md 前提/Step1・build.rs 案内 eprintln）。RELEASE.md の gh release アセットパス誤り crates/pasta_sample_ghost/hello-pasta.nar → release/hello-pasta.nar（release.ps1 の $NarFilePath 実出力と同期）。build.rs の存在しない templates/ への rerun-if-changed を削除（不在パスは毎ビルドの build script 再実行を誘発）。README ツリーへ main.rs 追記・generate_ghost API 例を新シグネチャへ同期。依存整合: image/imageproc/thiserror/tempfile/pasta_lua/tracing-test 全使用確認・tracing は除去を実試行したが tracing-test の #[traced_test] マクロ展開が直接参照し E0433 でコンパイル不能＝必須と実証し Cargo.toml に理由コメント追記。付随: tests/common/mod.rs・self_deploy_integration_test.rs は cargo fmt 由来の整形のみ。検証: cargo test -p pasta_sample_ghost 38 passed/0 failed（前セル40−削除2）・clippy --all-targets 警告0・workspace 1956 passed/0 failed（ベースライン1958−2 と完全一致） |
| 3.46 | lua-pasta_scripts-core | crates/pasta_lua/pasta_scripts/{ct.lua,main.lua,README.md}, crates/pasta_lua/pasta_scripts/pasta/*.lua, crates/pasta_lua/pasta_scripts/pasta/areka/**, crates/pasta_lua/.luacheckrc, crates/pasta_lua/tests/lua_specs/**（テスト追加のみ） | G1 | IMPROVED |  | G1: lua_specs へテスト81件追加（スイート36→44・Lua ソース本体変更0・新規8ファイル＋init.lua 登録）。事前照合で境界12 Lua ファイル（1,532行）と既存37スイートを突き合わせ真の未到達のみ選定。ct_test+10: 完全未テスト（リポジトリ内参照0）だった ct.lua を網羅 — __close の LIFO 実行順/エラー引数伝搬/pcall 隔離（クリーンアップ中エラーでも残り実行）/close 後クリア（2回目無実行）/cancel 不可逆性＋IMPL.__index 未設定でインスタンスメソッド呼出不能の現行挙動を文書化テストで固定。config_get_test+7: PASTA_CONFIG.get 全分岐初到達（値返却/false 非置換/キー・セクション欠落 default/section nil error/@pasta_config 不在フォールバック）— package.loaded 退避→差替→再ロード→復元パターン。scene_registry_test+20: SCENE レジストリ直接テストの欠落を補完（カウンタ採番独立性/register 再利用/create_global_table 冪等/scene_table:create_word→local_words/get_start・get_global_name・get_all_scenes/create_scene カウンタ付与採番/search 非文字列型ガード・ヒット __call・miss・Rust ヒット Lua 未登録 nil/co_exec の nil・非関数・コルーチン実行＋build 結果伝搬・build nil）。word_builder_test+15: entry 空無登録/可変長1リスト/複数 entry 追記/チェーン self/再ビルダー既存保持/create_local シーン独立/create_word エイリアス/get_all_words 同一参照/resolve_value 全6分岐（nil/関数＋act 伝搬/テーブル先頭/空テーブル nil/数値 tostring/false→"false"）。actor_module_test+10: get_or_create キャッシュ同一性/ActorWordBuilder 登録・空 entry 無効/proxy:word ポストプロセス（nil・空文字/関数に proxy 自身伝搬/非関数 tostring）/proxy:expr_fn（可変引数伝搬/非関数 nil）。config_actor_init_test+4: ロード時1回の CONFIG.actor 初期化パス初到達（store: actors 採用＋数値 spot のみ転送・非数値/非テーブル除外、actor: name 補完＋メタテーブル付与・name 既設保持・非テーブルスキップ）。act_word_expr_test+10: act:word ポストプロセス（nil・空/関数に act 伝搬/数値 tostring/false→"false"/全ミス nil）/act:expr_fn（可変引数/非関数/未発見）/act:call nil キーガード（無エラー・無副作用）。pasta_init_test+5: PASTA 公開 API リダイレクト同一性3件＋finalize_scene スタブ契約＋GLOBAL.チェイントーク==yield 同一性。RED 実証: 実装ミューテーション2件（ct.lua LIFO→FIFO で2件 fail／word.lua entry 空ガード除去で1件 fail）→ともにバイト同一復元（git diff 空を確認）。初回実行の organic RED 1件: 先行スイートの pasta.store 個別リロードによるモジュール参照分裂を検出→自スイート冒頭の整合一括リロード（既存スイート規約）で解消。発見バグ（挙動保存により未修正・申し送り）: ct.lua の IMPL に __index 未設定のため obj:defer/cancel のインスタンスメソッド呼出が不能（<close> 構文も LuaJIT 5.1 系で非対応＝モジュール全体が事実上利用不能）— 3.47 G2/G5 で要検討。areka/init.lua の TODO コメント残置も 3.47 G5 所掌。検証: lua_unittest_runner 44スイート全 pass・workspace 1956 passed/0 failed（ベースライン一致） |
| 3.47 | lua-pasta_scripts-core | crates/pasta_lua/pasta_scripts/{ct.lua,main.lua,README.md}, crates/pasta_lua/pasta_scripts/pasta/*.lua, crates/pasta_lua/pasta_scripts/pasta/areka/**, crates/pasta_lua/.luacheckrc, crates/pasta_lua/tests/lua_specs/**（テスト追加のみ） | G2+G3+G4+G5 | IMPROVED |  | G2: luacheck CLI を D-6 ポリシーで dev 環境へ導入（リポジトリ非変更）— スタンドアロン exe 直接DLはサンドボックス拒否→公式チャネル経由で winget DEVCOM.Lua 5.4.6（lua.exe＋luarocks）→ MSVC vcvars64 下で `luarocks install luacheck`（1.2.0・argparse 0.7.2・luafilesystem 1.9.0・%APPDATA%\luarocks 配下）。境界14ファイルの luacheck 指摘4件→0: ①actor.lua:9 未使用 require GLOBAL 除去 ②global.lua:25 パースエラー（GLOBAL.チェイントーク の非ASCII識別子は LuaJIT 拡張・標準 Lua パーサー非対応）→等価ブラケット記法 GLOBAL["チェイントーク"] へ（pasta_init_test の同一性テストが等価性固定）③scene.lua:204 co_exec 内 act シャドーイング→resumed_act へリネーム ④act.lua:286 複雑度19>15→G4 の helper 抽出で解消。ActorWordBuilder の死蔵 private フィールド _actor/_key（格納のみ・リポジトリ全域で読取0・lua_specs 非参照を grep 確認）を除去。ct.lua はリポジトリ内参照0（ct_test.lua のみ）の死蔵モジュールだが zip 出荷物＝外部ゴーストの require 公開面につき保守的維持 — IMPL.__index 未設定（obj:defer/cancel 不能）＋<close> LuaJIT 非対応の既知負債として記録（挙動追加も外部仕様変更リスクのため不実施・ct_test.lua が現行挙動文書化済）。lua_specs 側 luacheck 54警告/3エラー（非ASCII識別子パースエラー3件＝LuaJIT 拡張の false positive 含む）は「テスト追加のみ」境界につき不改変・記録のみ・G3: NO_CHANGE — 棚卸し証拠: 境界14ファイルに FFI/unsafe/再帰/非有界アロケーションなし（group_by_actor/merge_consecutive_talks は線形走査）・全エラー経路は Lua error として mlua 保護実行で Rust 側 Result 化（abort 経路0）。設定値検証は store.lua spot 数値型ガード・actor.lua テーブル型ガード済。非テーブル actor 要素の温存挙動は 3.46 文書化テスト（config_actor_init_test.lua:93）が契約固定済→フィルタ強化は既存テスト改変（原則禁止）を要し不実施・既知挙動と記録。config.lua get の section nil error は文書化＋テスト済 fail-fast。SCENE.search の @pasta_search ハード require は呼出元 act.lua が pcall ガード済→ハードニング挙動変更0件（回帰テスト不要）・G4: act.lua find_act_handler の L2/L5 重複（word/scene 分岐×2箇所・逐語同型）を private helper search_dictionary へ抽出（SCENE.search のモジュールテーブル経由呼出維持＝テスト monkey-patch 互換・scene 分岐の result.func 返却は SCENE.search が func 非nil 保証につき等価）・複雑度19→15以下（luacheck 0で機械確認）。ACT.new の app_ctx = require("pasta.store").app_ctx をトップレベル STORE 束縛へ簡素化（require キャッシュで同一テーブル＝等価）。save = require("pasta.save") は遅延ロードタイミングが意味を持つため維持。等価性証拠: lua_unittest_runner 44スイート全 GREEN（act_find_act_handler_test の L1-L5 全段×word/scene/expr 両モード・モック差替含む）・G5: areka/init.lua 既存 TODO を仕様プレースホルダとして維持判断の上 TODO マーカーを外し「将来の拡張ポイント（仕様プレースホルダ・未実装）」平文 doc へ言い換え（情報量不変）。word.lua resolve_value の虚偽 doc（「ACT_IMPL.word と PROXY_IMPL.word から共通利用」— handler-resolution-fallback で呼出元廃止済・現在 pasta_scripts 内呼出0）を正直化（出荷物公開 API として維持根拠明記）。act.lua pcall コメント（「1回チェック・値保持」→実態は呼出毎チェック）正直化。pasta_scripts/README.md は実態（zip 同梱・scripts/ 上書き優先）と一致・ドリフトなし確認。.luacheckrc は CLI 実走で実効性を初検証（lua51 std・複雑度15が機能）→変更不要。検証: luacheck 境界14ファイル 0警告/0エラー・lua_unittest_runner 44スイート全 pass・workspace 1956 passed/0 failed（ベースライン完全一致） |
| 3.48 | lua-pasta_scripts-shiori | crates/pasta_lua/pasta_scripts/pasta/shiori/**, crates/pasta_lua/tests/lua_specs/**（テスト追加のみ） | G1 | IMPROVED |  | G1: lua_specs へテスト15件追加（スイート44→45・新規 shiori_entry_test.lua＋init.lua 登録・Lua ソース本体変更0）。事前点検で境界12ファイル（1,492行）を既存 lua_specs 20スイート＋Rust 側 tests/shiori/{res_test,event_dispatch_test,event_handler_test,virtual_event_config_test,virtual_event_dispatch_test}.rs と突合し真の未到達のみ選定（res.lua の build/err/warn/not_enough/advice/bad_request、virtual_dispatcher の config 解決クランプ/is_blocked/フォールバックチェーン、callback の try_route/sweep/staging、choice_select、act.lua の get_property/set_property/transfer 系は既存被覆を確認し不追加）。SHIORI.entry+3: リポジトリ内テスト0だった SHIORI.load の true 返却・SHIORI.unload の無エラー無返却・require==グローバル SHIORI テーブル同一性。SHIORI.request xpcall 境界+4: 登録ハンドラ文字列→200 OK+Value／未登録イベントシーン不在→204／ハンドラエラー→500＋X-Error-Reason 先頭行のみ（2行目がレスポンス全体に非出現）／非文字列エラーオブジェクト（table）→error_handler nil 経由 RES.err(nil)＝Unknown error。GLOBAL.close_ghost+5: 完全未テスト（参照0）— ms>=1 で wait(ms)→\- の順序・ms 省略/0/非数値文字列は wait なし \- のみ・GLOBAL.ゴースト終了==close_ghost エイリアス同一性。EVENT.fire×CALLBACK 統合+2: fire レベルのコールバックルーティングが未到達（callback_module_test は try_route/consume_staged 直接呼びのみ）— stage_pending→yield コルーチンが pending 登録され co_scene/co_callback に残留しない（set_co_scene の co_callback 分岐初到達）・到着イベントが fire 先頭の try_route 経由で待機コルーチンに 1-based refs で届き継続 yield 値が 200 OK で返る。OnSecondChange sweep 分岐+1: second_change.lua のタイムアウト早期 return が未到達（second_change_thread_test は passthrough のみ）— タイムアウト済み pending で dispatcher.dispatch 不呼出＋500＋reason＋pending 除去。RED 実証: ミューテーション2件 — ①entry.lua error_handler 先頭行抽出除去→「先頭行のみ」テスト fail ②second_change.lua sweep 早期 return 除去→sweep 分岐テスト fail。両者 git restore 後 git hash-object 一致（0408a7d/5d088d3）でバイト同一復元を確認。初回実行は全15件一発 GREEN。スイート冒頭で entry 依存閉包19モジュールの整合一括リロード（3.46 確立規約）。不要テスト削除なし。発見バグなし。検証: lua_unittest_runner 45スイート全 pass・workspace 1956 passed/0 failed（ベースライン完全一致） |
| 3.49 | lua-pasta_scripts-shiori | crates/pasta_lua/pasta_scripts/pasta/shiori/**, crates/pasta_lua/tests/lua_specs/**（テスト追加のみ） | G2+G3+G4+G5 | IMPROVED |  | G2: luacheck（lua 5.4.6＋luacheck 1.2.0・--config .luacheckrc）境界12ファイル 5警告/1エラー→0/0 — ①entry.lua:23 パースエラー（GLOBAL.ゴースト終了 の非ASCII識別子ドット記法＝LuaJIT 拡張）→等価ブラケット記法へ（3.47 global.lua 同方針・shiori_entry_test のエイリアス同一性テストが等価固定）②sakura_builder.lua:26 未使用 local spot_to_id（リポジトリ内呼出0の死蔵正規化関数・local につき外部参照不能）削除 ③同:131 シャドーイング（inner の t が外側 token.type の t を遮蔽）→helper 移設で解消 ④複雑度超過3件（transfer_date_to_var 18/get_property 19/BUILDER.build 33・閾値15）を等価抽出で解消・G3: ハードニング2件（テスト先行で RED 実証→修正→GREEN）①callback.lua try_route: reference 欠落 req で pending 削除後に nil 添字エラー（RED: callback.lua:83 attempt to index field 'reference'）→待機コルーチンが resume 不能のまま孤児化していた — Rust 境界（lua_request.rs:51）は reference を常時生成するため Lua 直接呼出経路のみの攻撃面 → `req.reference or {}` ガード＋境界回帰テスト1件（空 refs で resume 継続・200 OK）②sweep タイムアウト500 のインライン RES.build を RES.err 経路へ統一 — 申し送りの X-ERROR-REASON（callback.lua）/X-Error-Reason（res.lua）大文字小文字不一致を res.lua 正準へ解消・空文字列 on_timeout は RES.err 規約で Unknown error へ正規化（不正系経路の整理）・境界回帰テスト2件（plain find による厳密ケーシング固定・RED 実証済み）・既存テスト1件（callback_module_test の X%-ERROR%-REASON 固定アサート）をソース変更随伴の意図的更新（根拠コメント付与）。X-*-Reason はベースウェア非消費の診断ヘッダーで正常系外部観測影響なし（3.48 新テストは綴り非固定設計済み）・G4: 等価簡素化 — act.lua get_property の refs==nil 分岐（空テーブルへ nil 代入する no-op ループ）を refs or {} へ統合（n 値 nil 返却の同値性は経路解析で確認）・transfer_date_to_var をデータ駆動2ループ化（DATE_NUM_FIELDS/DATE_JA_FIELDS・string.format("%d%s") は "%d年" 等と出力同値）・get_property 引数検証を normalize_property_names へ抽出・BUILDER.build を clear_spots/emit_actor_switch/emit_inner_token の3 helper へ分解（sakura_builder 52・get_property 18・shiori_act 43 テストの安全網で等価確認）。注記: get_property 二重不正呼出（main スレッド＋不正名）時のエラー選択順のみ変化（名前検証が先行）— 単一不正系のメッセージ・正常系は不変・G5: doc 同期6件 — entry.lua req.version doc（string "3.0"→integer 30・lua_request.rs 実装と一致化）・register.lua ハンドラシグネチャ虚偽記載（function(req)→function(act)・EVENT.fire は act を渡す）＋使用例3件・@type 注釈更新・event/init.lua の陳腐化 alpha01/alpha03 注記（「戻り値無視、204 返却」「統合予定」）を現行動作（コルーチン resume→200 OK）へ更新・choice_select.lua create_scene_coroutine の実在しない @param act 削除・virtual_dispatcher.lua create_scene_thread の「act 未使用」虚偽 doc 是正（実際は SCENE.co_exec へ渡す）・shiori/init.lua TODO コメントを将来拡張ノートへ言い換え＋死蔵モジュール（リポジトリ内 require 0 件）の維持根拠（zip 出荷物の外部公開面・3.47 ct.lua 同方針の既知負債）をファイル内へ明文化。既知負債記録（不変更）: callback.sweep の on_timeout=nil 経路で resume 先が再 stage_pending すると _staged が未消費残留（現行コンシューマ get_property は常に timeout_message 非nil のため非到達・pairs 走査中の pending 挿入制約により修正は構造変更を要す）／res.lua RES.build の pairs 順序非決定（3.48 所見・プロトコル無害）。検証: luacheck 0警告/0エラー（12ファイル）・lua_unittest_runner 45スイート全 pass・workspace 1956 passed/0 failed（ベースライン一致・lua_specs +3 テストは runner 1件に内包） |
| 3.50 | lua_specs-act | crates/pasta_lua/tests/lua_specs/act_*.lua | G2+G4+G5 | IMPROVED |  | G2: luacheck（lua 5.4.6＋luacheck 1.2.0・--config crates/pasta_lua/.luacheckrc）act 系 10 ファイル（2,581 行 — 台帳記載 9 ファイル/2,475 行は 3.46 追加の act_word_expr_test.lua 106 行を未算入）2 警告/1 エラー→0/0: ①act_find_scene_test.lua:10 未使用 require GLOBAL 除去（L3 系テストは再読込ヘルパ経由で fresh_GLOBAL を取得・pasta.act が内部 require 済でロード順不変）②act_impl_call_test.lua:8 未使用 require SCENE 除去（同根拠）③act_word_expr_test.lua:22 パースエラー（非 ASCII テーブルキー 挨拶 のドット記法＝LuaJIT 拡張・luacheck 標準パーサー非対応）→ 等価ブラケット記法 ["挨拶"] へ（＋回数/フラグ/加算/定数 計5キー・キー文字列同一でテスト意図＝日本語単語名検索を保存・3.47/3.49 同方針・パースエラー解消後の残警告 0 確認）。act_find_act_handler_test.lua の write-only 変数 mock_search_called_with（table.insert 格納のみ・読取 0）を除去・G4: ファイル内重複セットアップの等価 helper 統合 3 ファイル — act_find_scene_test.lua「pasta.act 再読込＋GLOBAL 取得」3行×4箇所→reload_fresh_modules・act_find_act_handler_test.lua「@pasta_search 差替→pasta.act 再読込」×8箇所→reload_fresh_act/reload_act_with_search＋同型ダミー @pasta_search テーブル×3箇所→dummy_search_module（差替→再読込の実行順を helper 内で厳密保存）・act_word_expr_test.lua「@pasta_search 退避→nil 固定→pcall→復元」×2→with_search_absent。テスト 151 件（10 ファイル）不変・アサーション無変更。lua_test は describe 完了時即時実行（test.lua:194 performTest・失敗時 os.exit(1)）のため helper 化はモジュール捕捉タイミング等価。per-test require("pasta.act") イディオム（act_test/act_choice/act_grouping/act_build_early_return）はスイート分離規約上の防御として維持。クロスファイル重複（create_mock_actors×4/create_mock_ctx×2）は共有 helper ファイル新設が境界外（act_*.lua のみ）につき不実施・G1/G3: N/A — テスト資産自体（網羅性の所掌は pasta.act 系ソースの G1 セル＝3.46 で実施済・FFI/攻撃面なし）・G5: 境界内 doc 整合のみ（helper へ既存コメントの根拠を移設・正確化）。境界外ドリフト検出の申し送り2件: ①lua_specs/README.md が「*_spec.lua」「transpiler_spec.lua」の陳腐化記載（transpiler_spec は実在せず・現行は *_test.lua 主体）②TEST_COVERAGE.md の件数ドリフト — act_test.lua 32→実 36・act_grouping_test.lua 23→実 25（act_find_scene_test 13 は一致）— 3.51〜3.54 / 3.64 所掌。検証: luacheck act 10 ファイル 0 警告/0 エラー・lua_unittest_runner 45 スイート全 pass・workspace 1956 passed/0 failed（ベースライン完全一致） |
| 3.51 | lua_specs-runtime | crates/pasta_lua/tests/lua_specs/{actor_word_test,buf_test,callback_module_test,choice_select_test,event_coroutine_test,event_no_entry_test,init,integration_coroutine_test,lua_version_test,mocks_test}.lua | G2+G4+G5 | IMPROVED |  | G2: luacheck（lua 5.4.6＋luacheck 1.2.0・--config crates/pasta_lua/.luacheckrc）境界 10 ファイル（実測 2,150 行 — 台帳記載 2,062 行は軽微ドリフト）26 警告/0 エラー→0/0 — ①event_coroutine_test.lua(15件): 未使用 RES ローカル×2（res は依存なし純モジュールで EVENT リロード時の内部 require が追従＝明示 require 省略は等価）・未使用 call_count 除去・内側コルーチン引数 act の上位引数シャドーイング×12（コルーチン本体で act 未参照につきパラメータ除去＝resume 引数破棄で等価）②integration_coroutine_test.lua(6件): 未使用 RES（EVENT ロード後のキャッシュヒット束縛につき省略等価）・write-only 変数 old_co_closed（格納のみ・読取0）除去・シャドーイング×4 同方針 ③callback_module_test.lua:204 未使用 refs（yield 戻り値の破棄へ・3.49 更新部は不変更）④init.lua: 行長超過4件（47/49/51/53 行目）をコメント短縮のみで解消 — スイート登録リストの登録順・モジュール名は完全不変。actor_word/buf/choice_select/event_no_entry/lua_version/mocks は当初から 0 警告・G1/G3: N/A — テスト資産自体（網羅性の所掌は対応ソースの G1 セル＝3.46/3.48 で実施済・FFI/攻撃面なし）・G4: ファイル内重複セットアップの等価 helper 統合 3 ファイル — event_coroutine_test.lua: 5 describe の同型 setup（package.loaded 6 モジュール nil→再 require→STORE.reset→REG クリア）を reload_event_modules(reset_res) へ統合（「既存コルーチン置換」describe のみ res 非リロードだった従来差異を reset_res=false で厳密保存・register/res は依存なし純モジュール＝require 順序差は等価と読解確認）・integration_coroutine_test.lua: 2 describe の同型 setup（5 モジュール nil→再 require→STORE.actors 設定→co_scene クリア）を reload_modules へ統合・callback_module_test.lua: 3 describe の同型リロード部を reload_callback_modules(reset_res) へ統合（describe 1 の res 非リロード差異を保存）＋「1-based refs」テスト内の放棄済み設計試行ブロック（使われない初期 co 生成＋resume・「no, we need to design correctly」コメント付き死コード）を除去。テスト 91 件（10 ファイル中 9 テストファイル）不変・アサーション無変更 — runner 出力で per-describe 件数一致を確認（callback 7+6+8=21・event_coroutine 7+6+2+5+2=22・integration 4+3=7）。choice_select/event_no_entry 等は既に helper 化済みで追加統合なし・G5: 境界外ドリフト申し送り2件 — ①TEST_COVERAGE.md: callback_module_test.lua「18テスト」→実 21（3.49 の境界回帰3件追加が未反映。event_coroutine 16+6=22・mocks 8 は実態一致）②lua_specs/README.md の陳腐化（*_spec.lua/transpiler_spec 記載・実在せず）は 3.50 申し送りの通り残存 — いずれも境界外につき 3.64 所掌。検証: luacheck 10 ファイル 0 警告/0 エラー・lua_unittest_runner 45 スイート全 pass・workspace 1956 passed/0 failed（ベースライン完全一致） |
| 3.52 | lua_specs-property-global | crates/pasta_lua/tests/lua_specs/{get_property_test,set_property_test,global_chaintalk_call_test,global_chaintalk_integration_test,global_fallback_integration_test,transfer_req_to_var_test,proxy_find_handler_test}.lua | G2+G4+G5 | IMPROVED |  | G2: luacheck（lua 5.4.6＋luacheck 1.2.0・--config crates/pasta_lua/.luacheckrc）境界 7 ファイル（1,591 行）5 警告/0 エラー→0/0 — ①get_property_test.lua:144-145 未使用 CALLBACK 再束縛（setup 済みのキャッシュヒット束縛）と write-only yielded_value を除去 ②global_chaintalk_integration_test.lua:14 未使用 RES 束縛を除去（pasta.shiori.event が L60 で内部 require 済み＝キャッシュヒット束縛・package.loaded["pasta.shiori.res"] の nil 化は維持＝リロード順序不変・3.51 同方針）③同:63/72 未使用 response1/response2 束縛を式文呼び出しへ。set_property/global_chaintalk_call/global_fallback/transfer_req/proxy_find は当初から 0 警告・G1/G3: N/A — テスト資産自体（網羅性の所掌は対応ソースの G1 セルで実施済・FFI/攻撃面なし）・G4: ファイル内重複セットアップの等価 helper 統合 5 ファイル — get_property_test:「コルーチン生成→SHIORI_ACT 再 require→act 生成→get_property→resume」5 行×17 箇所を resume_get_property(...) へ（select("#",...)+unpack で引数個数を厳密保存＝引数なし/nil の区別維持・require はコルーチン内のまま＝setup() リロード後の捕捉タイミング等価・co も返却し consume_staged 系 4 箇所を統合）・set_property_test:「require＋new」2 行×20 箇所を new_act() へ（per-test require イディオム＝スイート分離防御を helper 内で保存・actors 併用テストは第2戻り値で対応）・transfer_req_to_var_test: 同 2 行×9 箇所を new_act(req) へ・global_chaintalk_integration_test: 5 テストの同型 wrapped_fn ボイラープレート（act:build() 自動呼出ラッパ 7 行×5）を make_scene_handler(scene_fn) へ＋同型継続ハンドラ×4 を continue_handler へ（STORE は describe ローカル upvalue 参照で setup 毎の再代入へ追従＝呼出時評価等価）・global_fallback_integration_test: 4 describe の同一 package.loaded nil 化 8 行×4 を reset_modules() へ（describe 2 の @pasta_search スタブ挿入順＝nil 化後・require 前を厳密保存）。proxy_find_handler_test は make_proxy 済みで変更なし・global_chaintalk_call_test は重複僅少（2 行×4）につき変更なし。テスト 74 件（7 ファイル）不変・アサーション無変更 — runner per-describe 件数一致（get_property 7+1+3+2+2+3=18・set_property 6+2+8+2=18・call 2+4=6・integration 5・fallback 2+2+1+2=7・transfer_req 3+3+2=8・proxy 2+3+2+5=12）・diff -152 行・G5: 境界外ドリフト申し送り3件 — ①TEST_COVERAGE.md に set_property_test.lua（実 18）・transfer_req_to_var_test.lua（実 8）・proxy_find_handler_test.lua（実 12）の行が不存在 ②TEST_COVERAGE.md「GLOBAL チェイントーク関数登録 6テスト」は registration describe 実 2 件（ファイル計 6・「L3解決 4テスト」行は一致）＝行粒度の曖昧ドリフト ③lua_specs/README.md の陳腐化（*_spec.lua/transpiler_spec 記載）は 3.50/3.51 申し送りの通り残存 — いずれも境界外につき 3.64 所掌。検証: luacheck 7 ファイル 0 警告/0 エラー・lua_unittest_runner 45 スイート全 pass・workspace 1956 passed/0 failed（ベースライン完全一致） |
| 3.53 | lua_specs-persist-dispatch | crates/pasta_lua/tests/lua_specs/{persist_spot_position_test,persistence_spec,store_coroutine_test,store_last_global_scene_test,store_save_test,second_change_thread_test,res_ok_test,virtual_dispatcher_spec,virtual_dispatcher_thread_test}.lua | G2+G4+G5 | IMPROVED |  | G2: luacheck（lua 5.4.6＋luacheck 1.2.0・--config crates/pasta_lua/.luacheckrc）境界 9 ファイル（1,888 行）22 警告/0 エラー→0/0 — ①virtual_dispatcher_thread_test.lua(5件): 内側コルーチン引数 act の上位 executor 引数シャドーイング×5（L36/61/88/184/222・コルーチン本体で act 未参照につきパラメータ除去＝resume 引数破棄で等価・3.51 同方針。L152/215 の同形 function(act) はトップレベル局所でシャドーイング非該当につき不変更）②virtual_dispatcher_spec.lua(17件): 未使用 result 束縛（L381・check_talk 戻り値は意図的に非検証＝コメントで文書化済）を式文呼び出しへ・行長 121〜138>120 ×16 — うち L115/168/206 の 3 件は create_mock_act 引数テーブルの折返しのみ、残り 13 件はブロック条件 describe 内で G4 ヘルパ統合により解消。persist_spot_position/persistence_spec/store_coroutine/store_last_global_scene/store_save/second_change_thread/res_ok の 7 ファイルは当初から 0 警告・G1/G3: N/A — テスト資産自体（網羅性の所掌は対応ソースの G1 セルで実施済・FFI/攻撃面なし）・G4: virtual_dispatcher_spec.lua のファイル内重複の等価 helper 統合 2 describe — 「dispatch - status block conditions」の同型 4 行ボイラープレート（init dispatch→時報境界 dispatch）×15 テストを dispatch_at_next_hour(init_status, fire_status) へ（ブロック 9＋複合 2 は init="idle"・非ブロック 4 は init=fire の従来差異を引数で厳密保存・status=nil はフィールド不在で原文と同形・アサーション行 expect(result):toBe(nil)/expect(result ~= nil):toBe(true) は 1 行も不変更）・「check_hour - HH format」の同型 13 行ボディ×4 テストを first_candidate_for_hour(hour) へ（executor 差替→初期化→発火の実行順を helper 内で厳密保存・アサーション行不変・executor の未使用第 2 引数 act は統合時に除去）。diff -50 行（2 ファイル +57/-107）。早期打ち切り/フォールバック順序系は executor が個別につき統合せず・persist_spot_position_test の actors 生成 2 箇所は per-test require イディオム（スイート分離防御）保存を優先し不変更・3.49 変更分（second_change_thread_test 等）は touched せず。テスト 101 件（9 ファイル）不変 — runner per-describe 件数一致（vd_spec 3+2+3+3+2+2+2+1+1+2+1+4+18=44・vd_thread 4+3=7・persist_spot 8・persistence 12・store_coroutine 7・store_last_global 3・store_save 10・second_change 3・res_ok 7）・G5: 境界外ドリフト申し送り3件 — ①TEST_COVERAGE.md: persist_spot_position_test.lua「5テスト」→実 8・境界 9 ファイル中 6 ファイル（persistence_spec 12/store_coroutine_test 7/store_last_global_scene_test 3/second_change_thread_test 3/res_ok_test 7/virtual_dispatcher_thread_test 7）の行が不存在 ②TEST_COVERAGE.md「Virtual Event Dispatcher 16+15テスト」は Rust 2 ファイル分のみで併記の virtual_dispatcher_spec.lua（実 44）の件数不明記・「STORE.save/CTX注入 store_save_test.lua」行は件数表記なし（実 10）＝粒度曖昧 ③lua_specs/README.md の陳腐化（*_spec.lua/transpiler_spec 記載・実在せず）は 3.50〜3.52 申し送りの通り残存 — いずれも境界外につき 3.64 所掌。検証: luacheck 9 ファイル 0 警告/0 エラー・lua_unittest_runner 45 スイート全 pass・workspace 1956 passed/0 failed（ベースライン完全一致） |
| 3.54 | lua_specs-sakura-shiori | crates/pasta_lua/tests/lua_specs/{sakura_builder_test,shiori_act_test}.lua | G2+G4+G5 | IMPROVED |  | G2: luacheck（lua 5.4.6＋luacheck 1.2.0・--config crates/pasta_lua/.luacheckrc）境界 2 ファイル（1,993 行）着手時から 0 警告/0 エラー・未使用ヘルパ/require なしを目視でも確認（変更後 0/0 を新鮮再実行）。G1/G3 はテスト資産につき N/A（テスト追加は本セル所掌外・FFI/入力境界なしで攻撃面なし）。G4: 同型プレリュードを等価 helper へ統合 — sakura_builder_test: 52 テスト全件の per-test require＋create_mock_actors を setup() へ統合（2 行型 45・BUILDER 単独 3・buf 併用 4。require はテスト実行時のまま＝リロード規約保存）。shiori_act_test: 3 行プレリュード（require→actors→new）を new_act(req)（SHIORI_ACT.new(actors, req) 位置引数等価）へ統合 — 無 req 30 件（うち actors 未使用 7 件は act のみ受領）＋req 系 10 件（req field 3・transfer_date_to_var 6・req=nil 1）。IMPL 直接参照 1＋create_mock_ctx プロキシ 2 は現状維持。expect 行は両ファイル HEAD と全件バイト一致・テスト名/件数不変（52＋43）・1,993→1,878 行（−115）。G5: lua_specs/README.md は *_spec.lua 命名と transpiler_spec.lua のみ記載で実態（*_test.lua 中心 45 スイート・本セル 2 ファイル含む）と乖離、TEST_COVERAGE.md は shiori_act_test 47 テスト記載（実測 43・行 88/89 で transfer 7 件二重計上疑い）・sakura_builder_test 24+3+3+choice 記載（実 52・string-buffer 5 件等未算入）— いずれも境界外につき申し送り（3.64 所掌）。検証: lua_unittest_runner 45 スイート 0 失敗・workspace 1956 passed/0 failed・luacheck 0/0 |
| 3.55 | vscode-extension | editors/vscode/**（node_modules/・out/ を除く） | G1 | IMPROVED |  | G1: 実モジュール直接テスト 38 件を新設（vscodeModules.test.ts・esbuild --alias:vscode で未使用だった mock/vscode.ts を初配線・プロダクションコード変更なし）: semanticTokensProvider（delta→絶対変換/キャッシュ/エラー時 null）・diagnosticsManager（LSP→VSCode severity 1..4+既定/range 保存/clear）・documentSync（open/close ライフサイクル・200ms デバウンス収束・解析失敗フォールバック・dispose）・PastaDebugAdapterFactory/同 ConfigurationProvider 実クラス・wordRefDecorator（装飾範囲/非 pasta 除外）・extension.activate（実 WASM ロード成功検証・提示モードトグル配線: push イベント駆動表示/不正 body 拒否/セッション終了リセット/警告・エラー面）。TS↔Rust↔package.json トークン 17 型の3者照合テスト追加。既存テストの錆び修復: tmGrammar 2 件は commit 0ccf429 の意図的スコープ変更（string.key.word→markup.inline.raw.string）に未追従でベースライン RED→期待値を現行文法へ同期（+inline-word-ref 1 件追加）・integration.test.ts ミラー定数 15→17 型・e2e.test.ts 10→12 カスタム型/15→17 スコープ同期。ミューテーション実証 3 件（delta 変換/デバウンス clearTimeout/parseMode 厳格性）で fail 確認・sha1 同一復元。検証: npm run test 全緑 152 件（exit 0）・cargo workspace 1956 passed/0 failed。除外テスト 0 件。テスト配線のため package.json scripts.test:unit のみ更新（動作定義部 contributes/main/activationEvents は不変） | TS 2,498 行（src＋src/test） |
| 3.56 | vscode-extension | editors/vscode/**（node_modules/・out/ を除く） | G2+G3 | IMPROVED |  | G2: 3.55 申し送り①（lint スクリプト実行不能）を D-6 ポリシーで解消 — eslint@10.4.1＋typescript-eslint@8.61.0 を devDependencies へ導入・eslint.config.mjs 新設（tseslint recommended・no-unused-vars は ^_ 接頭辞無視・src/test/** のみ no-explicit-any off＝モック配管の any は規約として許容）。初回 lint 37 エラー→0: プロダクション是正3件 — wasmBridge.ts の `wasmModule: any` を WasmBindings インターフェース型化・documentSync.ts activate の未使用 context 引数を _context へ（不使用根拠を doc 化・シグネチャ/呼出元不変）・integration.test.ts の write-only analyzeCount 除去。未使用エクスポート: debugAdapterFactory.ts の便宜再エクスポート（DEFAULT_DEBUG_PORT/HOST・resolveAttachTarget・resolveSourcePresentation・SOURCE_PRESENTATION_KEY＋型3種 — テストは debugAttachTarget を直接 import・消費者0を grep 全数確認）を削除。devDependencies は全消費確認・未使用なし。tsc --noEmit 0 エラー・npm run compile 成功。package.json は devDependencies のみ変更（contributes/main/activationEvents 等の動作定義不変）・G3: 入力検証ハードニング2件（各 RED 実証→境界回帰テスト追加）— ①debugAttachTarget.ts resolveAttachTarget: `Number(config.port)` 素通しで 'abc'→NaN・0/-1/65536/12.5・true→1 等の不正値がそのまま DebugAdapterServer 記述子化していた → resolvePort ガード（number/string 型のみ受理・整数 1..65535 検証・違反は既定 9276 へフォールバック）。回帰5件（debugAdapterFactory.test.ts）— RED で 4 件 fail 実証（有効境界 1/65535 受理テスト含む）②wasmBridge.ts analyzeDocument: wasm 境界の `as WasmAnalysisResult` キャスト盲信 → isWasmAnalysisResult 型ガード（tokens/diagnostics 配列必須）で不正形を既存「WASM analysis failed」例外経路（documentSync は log＋診断クリア・provider は null 返却で防御済）へ即時収束（従来は spread/iterate 時の遠隔 TypeError）。回帰3件（vscodeModules.test.ts・実 WasmBridge モジュール）— RED で 2 件 fail 実証＋整形結果パススルー等価1件。正常系の外部観測挙動不変＝既存 152 件全緑維持・ついで（3.55 申し送り④）: semanticTokensProvider.ts ヘッダ「14 semantic token types」→実 17 へ是正。検証: npm run lint 0 警告/0 エラー・npm run test 160 件全緑（152＋回帰8・exit 0）・cargo test --workspace 1956 passed/0 failed（ベースライン完全一致）。申し送り: npm audit 15 件（high 7/moderate 8）— 全て dev 専用推移依存（esbuild<=0.24.2 の serve 系・vsce 配下 lodash/markdown-it/ajv/minimatch ReDoS 系）で .vsix 出荷物へは非混入（バンドルはランタイム依存なし）。esbuild の修正は ^0.25 メジャー昇格要につき本セルでは未実施 — ツールチェーン更新として別途検討推奨 | TS 2,498 行（src＋src/test） |
| 3.57 | vscode-extension | editors/vscode/**（node_modules/・out/ を除く） | G4+G5 | IMPROVED |  | G4: karpathy 準拠検証 — プロダクション 9 ファイル（src 直下 TS）精査で過剰抽象・不要複雑度なし（debugAttachTarget/sourcePresentationToggle の vscode-free 分離は node テスト可能性のための意図的設計・doc 化済み）→ プロダクション是正 0 件。是正は 3.55/3.56 申し送り①のインラインコピー型テスト 2 ファイル削除（wasmBridge.test.ts 10 件・integration.test.ts 19 件）— 根拠 (R2.3): ①wasmBridge.test.ts の TestableWasmBridge は 3.56 で入った isWasmAnalysisResult ガードを含まず既ドリフト実証＝プロダクションを検証しないコピー ②integration.test.ts は全 19 件がローカルミラー/Map/severityMap 等のトートロジー（builder シミュレーションは delta をそのまま push しており実装の delta→絶対変換と不一致） ③全アサーションを vscodeModules.test.ts の実モジュールテストへ対応付け（17 型定数→実 PASTA_TOKEN_TYPES 全順序＋package.json 照合＋Rust 正本照合・severity/range→実 DiagnosticsManager・デバウンス/close/キャッシュ→実 DocumentSync/Provider）。残余ギャップ 4 件を実 WasmBridge テストで補完（未初期化 isReady/throw・dispose 後 not-ready/throw・wasm_analyze 例外の WASM analysis failed ラップ・空 well-formed 結果パススルー）＋well-formed パススルーのフィクスチャへ diagnostics 追加（全フィールド保存検証を強化）。dispose ミューテーション（ready リセット除去）で新テスト RED 実証後 sha1 同一復元。package.json scripts.test:unit から削除 2 バンドル除去（動作定義 contributes/main/activationEvents 不変）。テスト 160→135（−29＋4・実モジュールカバレッジは純増）・G5: README.md ドリフト是正 — セマンティックトークン「15種」→17種・一覧表へ cueMarker(15)/cueCommand(16) 行追加・operator 行を現行実装（代入/括弧/二項演算子含む）へ同期・機能節へデバッガ接続（DAP attach＋.pasta⇔.lua 提示モード切替）を追記（contributes 済み機能の文書欠落）。package.json メタデータ（description/keywords/engines ^1.85.0=@types/vscode 整合/repository/license/icon）正確・是正なし。依存整合: devDependencies 9 件全消費確認（vscode-oniguruma+vscode-textmate=tmGrammar.test・typescript=typescript-eslint peer・vsce=package スクリプト等）・未使用なし。wasm/README.md（pasta_lsp README の wasm-pack コピー）は git 非追跡（.gitignore wasm/）のビルド成果物で乖離はローカルのみ — 正本コピーで同期（次回 build:wasm でも同一再生成・コミット対象外）。申し送り: ①CHANGELOG.md 最新 0.1.5 で package.json 0.2.2 と乖離（0.2.x 系リリースノート欠落 — 遡及記載は履歴捏造リスクにつき未実施・3.64/release-workflow で要検討）②TEST_COVERAGE.md §2.7 の vscode 行陳腐化（wasmBridge/integration 行は本セルで削除済・debugAdapterFactory 16/sourcePresentationToggle 17/vscodeModules 45 行不存在・合計 79 乖離）— 境界外につき 3.64 所掌 ③npm audit 15 件（全 dev ツリー）は 3.64 所掌維持。検証: npm run lint 0 警告/0 エラー・npm run test 135 件全緑（grammar 27＋debugAdapterFactory 16＋toggle 17＋vscodeModules 45＋e2e 30・exit 0）・cargo test --workspace 1956 passed/0 failed（ベースライン完全一致） | TS 2,498 行（src＋src/test） |
| 3.58 | book-tools-core | book/tools/*.mjs | G1 | IMPROVED |  | JS 2,562 行。テスト38件追加（49→87・ツール本体変更なし）・drift-check-test.mjs +23: detectDrift 異常系（incomplete-mapping×2/missing-source＋端到端 failed=true）/sha256File 改行正規化（CRLF・単独CR・内容差）/TOML パーサ端ケース（文字列内#・エスケープ引用符・バックスラッシュ・複数mapping）/detectUnmapped doc/spec 不在/リンクのフラグメント・クエリ許容/reportDriftCheck 分岐（FAIL・recorded/current 表示・未マップ警告OK）/CLI 結線（exit0・RESULT出力・DRIFT_STRICT）・tutorial-check-test.mjs +15: fatal 経路（チュートリアル不在→ok=false・FATAL・RESULT:FAIL）/レポート分岐（MISMATCH 表示・対処ガイダンス・OK/MATCH）/extractPastaBlocks 端ケース（CRLF・行内空白・pastalang 除外・未閉鎖）/CLI 結線・verify-scripts-test.mjs 新設(+10): verify-drift-gate/verify-content の回帰スモーク（exit0・チェック実数下限・失敗0・RESULT:OK。両者は import 即実行型で本体改変なしには単体テスト不可のため子プロセス検証）。verify-static/verify-search は mdbook ビルド前提のためスモーク対象外（verify-static は --self-test 内蔵）。ミューテーション実証3件（sha256File 正規化除去→7FAIL/fatal 分岐 ok:true→4FAIL/verify-content FACTS 改変→3FAIL・全て復元済）・不要テスト除外なし。実行: node book/tools/drift-check-test.mjs && node book/tools/tutorial-check-test.mjs && node book/tools/verify-scripts-test.mjs |
| 3.59 | book-tools-core | book/tools/*.mjs | G2+G3 | IMPROVED |  | G2: D4 lint = N/A（lint 基盤なし — book/package.json に lint スクリプト・eslint 設定とも不存在を新鮮確認。正準コマンド表どおり N/A 記録・導入せず）。D5 デッドコード: フラット 9 ファイルの全 export/関数×消費者の手動照合 — verify-drift-gate.mjs の未呼出 sha256() ヘルパー＋それのみが使う crypto import を削除・verify-search.mjs の未使用 import 2 名（rebuildBigramIndex/serializeSearchIndex — 本体は build-index.mjs を execFileSync 子プロセス実行しており直接呼出なし・モジュール評価は他 3 名の import で不変）を削除。外部 import のない export（drift-check.mjs UNMAPPED_EXCLUDE/listMarkdownFiles・verify-content.mjs REPO_ROOT 等）はモジュール内で消費される生きたシンボル（export キーワードのみ未消費）＝デッドコードでないと判定し維持・G3: 外部入力面点検（CLI 引数なし・env DRIFT_STRICT は '1' 厳密比較で安全・読込 TOML/MD/HTML）→ ハードニング 3 件（全て RED 実証→修正→回帰テスト）: ①② drift-check.mjs detectBrokenLinks のパストラバーサル — `..` を含む自リポ blob URL／book 内相対 .md リンクが repoRoot 外の実在ファイルへ解決されると素通り（RED: broken=0/failed=false をサンドボックスで実証 — リポジトリ外の存在プローブが可能）→ isWithinRoot ガード新設で実在有無にかかわらず broken 報告へ（repoRoot 内に留まる `..` 相対参照は従来どおり実在チェックのみ＝正常系不変）・回帰テスト B-14 5 件（blob 脱出/相対 .md 脱出/境界内 .. 非誤検出/件数/failed=true）③ tutorial-check.mjs normalizeForCompare の単独 CR 非対称（3.58 申し送り①）— /\r\n/g のみで旧 Mac 改行を正規化せず drift-check sha256File（/\r\n?/g）と非対称（RED: 単独 CR dic vs LF ブロックの matchDicFile=false を実証）→ /\r\n?/g へ対称化・回帰テスト 2 件（normalizeForCompare 同一視＋matchDicFile 一致）。正当化して維持: 最小 TOML パーサ stripComment はエスケープ引用符を含む値中 # で誤剥離しうるが、入力は repo 内信頼ファイル manual-sources.toml のみ（ハッシュは hex）＝攻撃面なし — 申し送りのみ。verify 系 import 即実行構造・verify-static require_os_tmpdir は 3.60（G4）所掌につき不触。正常系外部契約（manual.yml が呼ぶ drift-check/tutorial-check の exit code・レポート形式）不変 — 実リポジトリクリーン判定（A 節）全 PASS が証拠。検証: scoped 3 テスト 58+36+10=104 passed/0 failed（ベースライン 97＋回帰 7）・workspace cargo test 1956 passed/0 failed・69 スイート（ベースライン完全一致） |
| 3.60 | book-tools-core | book/tools/*.mjs | G4+G5 | IMPROVED |  | G4: karpathy 準拠点検（フラット 9 ファイル全走査 — 過剰複雑・重複・自前実装）→ 是正 2 件（いずれも verify-static.mjs）: ① require_os_tmpdir()（TEMP/TMP/TMPDIR/'/tmp' の自前フォールバック・3.59 申し送り②）を node:os 標準 os.tmpdir() へ置換しヘルパー関数を削除 ② analyze() の allowed 判定第 3 節 `(ext==='' && ALLOWED_NOEXT_NAMES.has(base))` が第 2 節 `ALLOWED_NOEXT_NAMES.has(base)` の真部分集合＝恒等冗長につき削除（1 行化）。正当化して維持 3 件: (a) verify 系 4 本の import 即実行型→関数化＋CLI ガード分離（3.58 申し送り①）は見送り — CLI 契約（exit code・レポート形式 = manual.yml/CI 結線）の厳密保存が最優先で、スモーク（verify-scripts-test）は子プロセス実行で現構造を前提に文書化済み・テスト性向上のみが動機の 4 ファイル構造変更は karpathy「壊れていないものをリファクタしない」に反する (b) drift-check stripComment のエスケープ引用符端ケースは 3.59 で正当化済（入力は信頼ファイルのみ）— エスケープ状態機械の追加は簡素化の逆方向 (c) check() ミニアサートの 5 ファイル重複（各 ~10 行）は「依存ゼロ・スタンドアロン実行」設計の意図的重複 — 共有モジュール化は結合を増やす抽象化で見送り。G5: 境界内文書 = 各 .mjs ヘッダコメント（tools/ 直下に README なし）を実装と照合 — 9 本とも役割・exit code 規約・使い方フラグ（--no-build/--self-test）・対象外根拠の記述が現実装と一致（verify-scripts-test ヘッダ「verify-static は --self-test 内蔵」も事実一致を実走確認）。依存整合: フラット .mjs の import は node: builtin＋相対参照のみ（verify-search → bigram-index/ は境界外サブディレクトリ・manual.yml の呼出と整合）・book/package.json devDeps（jsdom/vscode-textmate 等）はフラットツール不使用＝矛盾なし・CI manual.yml の drift-check/tutorial-check 結線（exit 非ゼロで中断）と CLI 契約一致。境界外確認: book/AUTHORING.md はツール群へ言及ゼロ（執筆規約のみ）＝不整合なし。検証: verify-static --no-build --self-test 22 passed/0 failed（os.tmpdir 置換経路と allowed 判定を実走・self-test 5 件 PASS）・scoped 104 passed/0 failed（drift 58＋tutorial 36＋verify-scripts 10・ベースライン完全維持）・workspace cargo test 1956 passed/0 failed・69 スイート（3.59 ベースライン一致） |
| 3.61 | book-tools-highlight | book/tools/highlight/** | G1 | IMPROVED |  | テスト50件追加（152→202・ツール本体変更なし）・scope-map-test.mjs +7: 不正入力ガード未検証を網羅 — 非配列5種（null/undefined/文字列/array-like オブジェクト/数値）→ null・非文字列/空文字列要素のみ配列 → null・非文字列要素スキップ後の有効スコープ採用・neutralizer-test.mjs +15（run を async 化）: ケースE install 前に win.hljs 既存（L91-93 即時 neutralize 分岐・読み込み順逆転）/F win=null・undefined 早期 return/G classList 不在環境の className 文字列 fallback＋language-pastafoo 部分一致非スキップ＋hljs 再代入時の setter 再 neutralize/H 部分 API（highlightBlock のみ・highlightElement のみ・メソッド無し・null）/I preventExtensions された hljs のマーカー try/catch 吸収/J defineProperty 不可→setInterval ポーリングフォールバック（L108-117・非 configurable データプロパティで実発火・await 検出）・tokenizer-test.mjs +17: load 前 tokenizeText/SinglePass 例外・load 引数バリデーション4種・scopeName 欠落文法 JSON リジェクト（os.tmpdir 隔離）・空テキスト/空行混在の行数一致＋被覆・複数 lua ブロック分離（間の pasta 行へ lua スコープ非漏出＋ブロック毎 ruleStack）・未終端 lua ブロックの末尾 flush 経路・highlight-html-test.mjs +11: load 前例外2種・ファイルパス渡し（not a directory 分岐）・write:false dry-run（バイト不変＋集計報告）・モジュール直 export ワンショット highlightHtmlString/highlightHtmlFiles（従来 void で lint 回避のみ→factory 経由とバイト一致検証）・pasta ブロック無し HTML バイト不変・language-pastafoo \b 境界非マッチ・空 pasta ブロック安全。ミューテーション実証4件（neutralizer L91 分岐無効→suite 異常終了/highlight-html write フラグ無視→2FAIL/tokenizer 末尾 flush 除去→1FAIL/scope-map 非配列ガード除去→3FAIL・全て git restore 復元済）・不要テスト除外なし。検証: scoped 4 テスト 55+48+31+68=202 passed/0 failed・workspace cargo test 1956 passed/0 failed（ベースライン完全一致） |
| 3.62 | book-tools-highlight | book/tools/highlight/** | G2+G3+G4+G5 | IMPROVED |  | G2: lint 基盤なし（book/package.json に lint スクリプト・eslint 設定なし）→ lint 次元 N/A 記録。デッドコード点検: 全 export の消費を grep 全数照合（GRAMMAR_PATHS/createHtmlHighlighter/highlightHtmlString/highlightHtmlFiles=highlight-html-test・tokenizeTextSinglePass=tokenizer-test・SCOPE_CLASS_TABLE=scope-map-test・installHljsNeutralizer=neutralizer-test＋head.hbs ミラー）→ 唯一未消費の neutralizer.mjs `export default installHljsNeutralizer`（L121）を除去。関数本体は不変のため head.hbs インラインミラー（関数本体のみの逐語コピー）と乖離なし・G3: ハードニング挙動変更0件（点検証拠）— ①listHtmlFiles はディレクトリ symlink/junction を辿らない（Dirent=lstat 準拠で isDirectory()=false）を junction 実験（出力 dir 外への外部リンク＋自己ループ）で実証 → 境界固定テスト2件追加（リンク先 HTML の列挙・書換なし／自己ループでも無限再帰せず完走。fs.symlinkSync 'junction' で権限不要・作成不可環境は INFO スキップ）②3.61 申し送りの neutralizer.mjs L77/L80 frozen-hljs strict TypeError は head.hbs ミラー（境界外）同期必須のため既知負債として維持（実環境 hljs は非凍結・テストケース I が preventExtensions 耐性を固定済）③PASTA_BLOCK_RE のバックトラックは mdbook 出力＝信頼入力かつ build-time 限定で低リスク・変更なし・G4: karpathy 準拠是正1件 — tokenizer.mjs の clampToLine/clampLuaToLine 同型重複（16行×2・相違は末尾埋め fallback スコープ文字列のみ）を clampSpansToLine(tokens, line, fallbackScope) へ統合し呼出3箇所を直接呼びへ（-20行・等価変換）。mergeLuaIntoLine の前後ループ類似は条件相違（end<=region.start / start>=region.end）につき統合せず維持・G5: 3.61 申し送りの「PASTA_BLOCK_RE は <pre> を要求しない」を highlight-html.mjs ヘッダ＋regex コメントへ正直化し、インライン code（language-pasta クラス付き・<pre> なし）着色の挙動固定テスト2件追加（regex へ <pre> を要求するミューテーションで RED を実証後復元）。head.hbs ミラー同期確認（関数本体ロジック一致・本セルで関数本体未変更）。book/AUTHORING.md（境界外）は highlight 言及0件で乖離なし・変更不要。依存整合: book/package.json（境界外・検証のみ）の jsdom/vscode-textmate/vscode-oniguruma 全て使用中・未使用依存なし。検証: scoped 4スイート 206 passed/0 failed（59/48/31/68＝202＋4）・workspace cargo test 全69スイート 0 failed |
| 3.63 | book-tools-bigram-index | book/tools/bigram-index/**（poc/book/ の生成物を除く） | G1+G2+G3+G4+G5 | IMPROVED |  | G1: テスト33件追加（チェック 36→69・build-index-test 28→47/poc-test 8→22）— poc-test +14: tokenize 境界網羅（null/undefined/空/数値 String 化・単独 CJK 1 文字・ASCII 挟み単独 CJK・全角英数半角化＋小文字化・半角カタカナ bigram・長音ー連結・ハイフン/タブ改行区切り・CJK/ASCII 境界・拡張A bigram・特性固定: 拡張B はコード単位走査により isCjk 0x20000 分岐へ到達せず 1 語トークン=head.hbs ミラーも同一規則で索引/クエリ整合）／build-index-test +19: extractJsonLiteral 直接単体（基本形＋open/close 境界・JSON 本文中 `))` の動機ケース・エスケープ復号 \' \\ \n \r \t＋未知 \d パススルー・marker 直後空白・エラー4種=marker 欠落/二重引用符拒否/宙ぶらりんエスケープ/未終端）・serializeSearchIndex ラッパ形式＋serialize→extract ラウンドトリップ（単引用符/バックスラッシュ/改行/`))` 同梱）・resolveHashed 一時 dir 解決＋不一致明示エラー・readSearchIndex 復元・buildBigramIndex write:false ドライラン（ファイルバイト不変＋報告サイズ一致）・CLI 契約3件。ミューテーション実証2件（extractJsonLiteral の \\ 分岐除去→1 FAIL＋ラウンドトリップ suite 異常終了／tokenize 単独 CJK 分岐無効化→2 FAIL・いずれも復元済）・G2: lint 基盤なし（book/package.json に scripts なし・eslint 設定なし）→ lint 次元 N/A 記録。デッドコード2件除去 — writeSearchIndex（全リポジトリ消費者ゼロ・buildBigramIndex と serialize+write 重複）／tokenize.mjs 未使用 default export（全消費者が named import）。export 面変更につき 3.60 申し送りどおり verify-search 実走で確認（--no-build 66 passed/0 failed・head.hbs 規則整合 PASS）＋node --check 5 ファイル OK。npm 依存ゼロ（node: builtin＋mdbook 同梱 elasticlunr のみ）=未使用依存なし・G3: CLI 引数なし時の既定パスが Windows で壊れる欠陥を修正 — new URL().pathname を直接 path.resolve しており `C:\C:\home\...` ENOENT exit 1（RED 実証済）→ fileURLToPath 化（POSIX/CI は同一挙動・明示引数経路=manual.yml/verify-search は不変）＋回帰テスト「CLI: 引数なしで既定パス（book/book）に対し exit 0」。CLI 失敗契約（不正 dir→exit 1＋build-index failed 診断）もテスト固定。その他攻撃面: 索引入力は mdbook 自身の出力=信頼ビルドチェーン・extractJsonLiteral は既ハードニング済（エラー4種を本セルでテスト固定）・tokenize は null/undefined ガード済→追加ハードニング不要・G4: karpathy 準拠是正0件（serialize+write の重複は G2 の writeSearchIndex 除去で解消）。正当化して維持: tokenize 末尾 filter は到達不能だが head.hbs ミラー（境界外）との対応維持のため不変更・extractJsonLiteral の open/close 戻り値は文書化済み契約で本セルのテストが消費・buildBigramIndex write オプションはドライランテストで消費実証・G5: tokenize.mjs ヘッダ陳腐化是正 — 「searcher.js へ注入/同梱する前提」を実態（theme/head.hbs インラインミラー＋defineProperty フック・変更時 BEGIN/END ブロック同期必須・verify-search が規則一致を検証）へ正直化。book/AUTHORING.md（境界外）は search/bigram 言及ゼロ=乖離なし・build-index.mjs/テストヘッダは実装と一致確認。検証: scoped 69 passed/0 failed・verify-search --no-build 66/0・workspace cargo test 1956 passed/0 failed・69 スイート（3.62 ベースライン一致）。申し送り: isCjk の拡張B 分岐（0x20000-0x2ffff）はコード単位走査により到達不能（特性テスト固定済・是正には head.hbs ミラー同期=境界外変更が必要・索引/クエリは同一規則で整合しており実害なし） |
| 3.64 | global | TEST_COVERAGE.md, deny.toml, Cargo.lock（監査対応の更新のみ）, editors/vscode/package.json・package-lock.json（npm audit 対応のみ）, book/package.json・package-lock.json（同）, book/src/**（drift-check 同期のみ） | D7 | IMPROVED |  | cargo audit: 脆弱性0・警告7→4 — imageproc 0.26.1 の unsound 3件（RUSTSEC-2026-0115/0116/0117）を `cargo update -p imageproc`（0.26.1→0.26.2 semver 互換・監査対応のみ）で解消。ただし Cargo.lock は .gitignore 対象（リポジトリ非追跡）のため修正は dev 環境のみ・新規クローンは 0.26.2 を自動解決＝コミット対象に Cargo.lock 含まれず。残4警告は全て pasta_sample_ghost の画像処理 transitive（core2 0.4.0 unmaintained+yanked／paste 1.0.15 unmaintained — いずれも rav1e 経由・修正版なし／rand 0.9.2 unsound RUSTSEC-2026-0097 — imageproc/rav1e が 0.9 系を要求・workspace 直接依存は rand 0.10.1 で非該当）→ 対応不能と判断し記録のみ・D-6 導入: cargo-deny 0.19.8・cargo-machete 0.9.2 を dev 環境へ導入成功（cargo install・リポジトリ非変更）。cargo deny check = imageproc 更新後 advisories/bans/licenses/sources 全 ok（exit 0。警告: duplicate 9種・yanked core2・license-not-encountered は非致命・deny.toml 変更不要）。cargo machete = 未使用依存 0（全 workspace）・npm audit: vscode 15件（8 moderate/7 high・全 dev ツリー）→ `npm audit fix` 2回（package-lock.json のみ・package.json 不変）で 14件解消（undici/uuid/ajv/brace-expansion/fast-uri/lodash/markdown-it/minimatch/picomatch ほか）。残1件 esbuild<=0.24.2 moderate（GHSA-67mh-4wv8-2f99・dev ビルドツール）は 0.28.0 メジャー昇格要につき見送り＝3.56 判断維持。非破壊確認: npm run test 135/135 全緑（tmGrammar 27/debugAdapterFactory 16/toggle 17/vscodeModules 45/e2e 30）。book: 0 vulnerabilities・台帳同期: TEST_COVERAGE.md を新鮮実測値へ全面同期（申し送り全8項対応）— ①pasta_check 行新設（65）②act_test 32→36・act_grouping 23→25 ③callback_module 18→21 ④set_property 18・transfer_req_to_var 8・proxy_find_handler 12 行新設 ⑤persist_spot_position 5→8・store_save 10件明記・store_coroutine 7/store_last_global_scene 3/persistence_spec 12/second_change_thread 3/res_ok 7 行新設・virtual_dispatcher_spec 44＋thread 7 明記 ⑥shiori_act 47+7 二重計上を 43 単一行へ統合是正・sakura_builder 24→52（サブセット3行は「52 に含む」と包含明記）・global_chaintalk 登録行 6→2（粒度是正・ファイル計6注記）⑦§2.7 vscode 全面更新（wasmBridge/integration 行削除・27/16/17/45/30=135）⑧§5 サマリ全行実測化（pasta_sample_ghost 38 行新設・pasta-vscode 79→135・合計 1050+→2091 = Rust workspace 1956＋vscode 135・Lua spec 598 は runner 経由で内包・ヘッダー総数/最終更新も同期）・整合検査: drift-check OK（mappings 10・ドリフト0・リンク切れ0 → book/src 変更不要）・tutorial-check OK（5 dic 逐語一致）・検証: cargo test --workspace 1956 passed/0 failed（imageproc 更新後の新鮮実行・ベースライン完全一致）・lua_unittest_runner 45 スイート全 pass・申し送り（境界外・記録のみ）: lua_specs/README.md 陳腐化（3.50〜3.53 指摘 — 本セル境界にも含まれず未対応・Task 5 ドキュメント同期所掌へ）・editors/vscode/CHANGELOG.md 0.2.x 欠落（release-workflow で要検討）・esbuild メジャー昇格は将来の依存更新サイクルへ |

## 領域発見ログ（Task 2 — 2026-06-10 実行時発見）

### 資産カテゴリ検出（マニフェスト存在による検出 — 固定リスト不使用）

| マニフェスト | 資産カテゴリ | 検出結果 | 検証エコシステム |
|---|---|---|---|
| `Cargo.toml`（workspace members = `crates/*`） | Rust クレート | pasta_check / pasta_core / pasta_dsl / pasta_lsp / pasta_lua / pasta_sample_ghost / pasta_shiori（7 クレート） | cargo test / clippy / cargo-audit |
| `package.json`＋`package-lock.json` | Node/TS/JS | editors/vscode（TS 拡張）・book（JS ツール群 book/tools） | npm scripts / npm audit / node *-test.mjs |
| `.luacheckrc`（crates/pasta_lua/）＋ lua_unittest_runner | Lua 資産 | pasta_scripts/・tests/lua_specs/ | luacheck（CLI 未導入 — D-6）/ lua_test（cargo 経由） |

検出した品質検証インフラは Task 1 の正準コマンド表（tasks.md Implementation Notes）と突き合わせ済み。各セルのスコープ検証コマンドは正準コマンド表からの逐語コピー（`{crate}` 置換のみ）。

### 領域分割（src 実測行数 2026-06-10・約 3,000 行超はサブモジュール境界で細分化）

- pasta_lua/src 26,714 行 → 13 領域へ細分化（クレートルート＋code_gen＋enc-log-search＋loader×2＋runtime＋sakura_script＋debug×6）
- debug サブモジュール 14,869 行（フラットなファイルモジュール構成）→ ファイルモジュール境界で 6 分割。wiring.rs（5,021 行）と session.rs（2,584 行）は単一ファイルにつきこれ以上細分不能（約 2,000 行規則の例外として Notes に明記）
- loader 3,156 行 → ファイルモジュール境界で 2 分割（loader-core 1,633 / loader-io 1,523）
- tests/lua_specs 10,009 行（37 ファイル・フラット）→ テーマ別 5 領域（act 2,475 / runtime 2,062 / property-global 1,591 / persist-dispatch 1,888 / sakura-shiori 1,993）

### セル配分規則（本実行インスタンス）

- 〜約 2,400 行: 2 セル（G1 / G2+G3+G4+G5 — design.md「matrix.md スキーマ」の pasta_core 例示と同形）
- 約 2,400 行超: 3 セル（G1 / G2+G3 / G4+G5）
- FFI/unsafe 重点（pasta_shiori）: 5 セル（G1〜G5 全分割）
- テスト資産（lua_specs 系）: 1 セル（G2+G4+G5 — G1/G3 は N/A）
- 極小領域（book-tools-bigram-index 678 行）: 1 セル（全グループ統合）
- 横断 D7: 全体 1 セル（最終セル）
- 領域内の実行順序は常に G1→G2→G3→G4→G5（昇リスク順）

### 対象外（理由付き）

- `crates/pasta_lua/scriptlibs/**`: vendored ライブラリ（lua_test・luacheck）
- `book/tools/bigram-index/poc/book/**`: mdbook 生成物（1,945 行）
- `editors/vscode/out/**`・`node_modules/**`・`target/**`: ビルド生成物・外部依存
- `benches/`: ソースなし（PERFORMANCE_DELETED.md のみ）
- `doc/`・`img/`・`release/`・ルート Markdown（GRAMMAR.md 等）: ドキュメント/配布資産 — セル化せず各領域 G5 と Task 5（固定タスク）で同期確認
- 既知負債メモ: clippy 警告（research.md 実測 117 件）は各領域 G2 セルが所掌。pasta_lua の expect 多用（実測 346 箇所）は G3 セルが所掌。これらの領域の NO_CHANGE は最終レポートで「要注意」扱い（design.md レポート必須セクション④）

### 集計

- 領域数: 31（うち横断 1）/ セル数: 64
- 全セル PENDING で初期化（実行開始 2026-06-10T20:55:19+09:00・ベースライン 4027097）
