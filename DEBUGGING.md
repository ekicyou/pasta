# pasta デバッグガイド（VSCode / LuaJIT）

VSCode から pasta の埋め込み LuaJIT をデバッグするための運用ガイドです。本書の中心テーマは
**「ブレーク中はホスト（SHIORI/SSP）応答が止まる」という構造的制約とその回避策**です。
まず仕組みの全体像を簡単に押さえたうえで、この制約と緩和策を詳述します。

---

## 1. デバッグの全体像（オリエンテーション）

- **方式**: VSCode のデバッグ拡張から、Rust 側のデバッグバックエンドへ **アタッチ（attach）** して操作します。
  デバッグ本体（停止・ステップ・変数 inspect・DAP プロトコル）は Rust 側に実装されており、
  生成された `.lua` レベルでブレークポイント・ステップ実行（over/into/out）・コールスタック表示・
  変数 inspect が行えます。
- **接続先**: 既定で `127.0.0.1:9276`（TCP・ローカル接続）。
- **有効化**: デバッグは **既定で無効** です。次のいずれかで有効化します。
  - `pasta.toml` の `[debug]` セクション
    ```toml
    [debug]
    enabled = true   # 既定: false
    port = 9276      # 既定: 9276
    ```
  - 環境変数（設定ファイルより優先）
    - `PASTA_DEBUG`（truthy で有効化）
    - `PASTA_DEBUG_PORT`（待ち受けポートの上書き）
- **本番ゼロコスト**: 無効時はデバッグ用フックを設置せず、接続待ち受けポートも開かず、
  Lua の `debug` 機能もスクリプトへ露出しません（サンドボックス維持）。本番実行には一切の追加コストがありません。

> **注（範囲）**: `.pasta` ソースレベルのデバッグ（`.pasta` 行でのブレークポイント・コールスタック提示）は
> 本仕様では **実験的（feature gate 付きの実証スライス）** に留まり、本番品質の提供は将来の別仕様に委ねられます。
> 既定で提示されるソースは生成 `.lua` です。

---

## 2. ブレーク中はホスト応答が止まる（構造的制約）

**これは既知かつ意図された構造的な振る舞いです。** デバッグ中に「ゴーストが固まった」「無反応になった」ように
見えても、不具合ではありません。

### 何が起きるか

pasta の SHIORI リクエストは `Arc<Mutex>` により **直列** に処理され、リクエスト処理は VM スレッド上で
**ブロッキング** で実行されます。ブレークポイントで実行が停止している間、**VM ホストスレッドは復帰せず、
そのまま SHIORI リクエストの `Arc<Mutex>` を握り続けます**。

その結果:

- **現在処理中のリクエストの応答が保留される** だけでなく、
- **後続のすべての SHIORI リクエストも、実行を再開（continue）するまで待機します。**

つまり、ブレーク中はホスト（SHIORI/SSP）に対する応答全体が、実行再開まで一時停止します。これは
Mutex による直列化と「停止中は VM が復帰しない」という設計上の帰結であり、想定どおりの挙動です。

---

## 3. 緩和策・運用上の注意（SSP タイムアウトを避ける）

ブレークを長く保持すると、SSP 側がリクエストのタイムアウト（「応答なし」表示など）を起こす可能性があります。
根本解決はできない（次節参照）ため、以下の運用で **ブレーク保持時間を短く保つ** ことが要点です。

- **デバッグ専用の起動・プロファイルを用意する。**
  本番運用中のゴーストにそのままアタッチして長時間ブレークするのではなく、デバッグ用のラウンチ構成／
  プロファイルで作業し、影響範囲を限定します。
- **ブレークは短く保つ。** 必要な変数・スタックを確認したら、**速やかに continue（続行）** して実行を再開します。
  停止したまま放置しない。
- **時間に敏感なイベント処理中のブレークを避ける。**
  時報・タイマー・連続的な定期イベントなど、ホストが短い間隔で応答を期待する処理の最中にブレークすると、
  タイムアウトを誘発しやすくなります。確認したいロジックは、可能なら時間制約の緩いイベントで再現して
  ブレークします。
- **長時間の停止は「応答なし」を招くと理解しておく。**
  ステップ実行や変数確認に時間がかかりそうな場合は、停止箇所を絞る・確認項目を事前に決めておくなどして、
  1 回の停止を短くまとめます。
- **複数リクエストの滞留に注意。** ブレーク中は後続リクエストも待機列に積まれます。再開後にまとめて処理されるため、
  長く止めるほど再開直後の挙動が一気に流れる点に留意してください。

---

## 4. スコープ外（根本解決について）

ブレーク中のホスト応答停止（SSP タイムアウト）の **根本解決は本仕様の対象外** です。本書が提供するのは
上記の **緩和策のみ** であり、構造的制約そのものを取り除くものではありません。

根本的な解消には、ホスト側を **非同期化するアーキテクチャ**（ブレーク中でもホストへ即時応答を返し、
リクエスト処理と停止制御を分離する設計）が必要となり、これは別途の取り組みとして見送られています。
現時点では、本書の運用上の注意・緩和策に従って **ブレーク保持時間を短く保つ** ことで対処してください。
